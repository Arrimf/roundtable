**Берём `live.py` как ядро комнаты-прототипа; Orca — только поздний транспорт/вьюер ленты; запасной AG2 `GroupChat` как эталон протокола; главный риск — две истины (`live.jsonl` против `orchestration.db`) и слепота без границы доверия ОС.**

**Расхождения.** Codex в слепой фазе предлагал «брать Orca поверх live.py», в rebut1 отозвал это и перешёл на `live.py`-ядро. Claude, Grok, Kimi и DeepSeek с самого начала считали Orca фермой/DAG/mux без общей ленты. Запасные варианты не сошлись: Hermes Kanban (Codex), fusengine (DeepSeek — отозвал), LangGraph (Gemini), OpenClaw (Grok), AG2 (Kimi; DeepSeek поддержал после ревизии). После rebut1 остался AG2 как протокольный эталон; OpenClaw поддержки не получил. Фактология: Gemini перепутал DeepSeek Harness с `lm-evaluation-harness` и приписал fusengine `engine.py` — Kimi проверил: это разные проекты. Звёзды Orca «~24k» у Claude неверны — на дату 52 648. Codex отдельно уточнил: в DeepSeek Harness есть experimental Agent Teams, но private и выключен из релизов, поэтому вывод «не готов» сохраняется.

**Единогласно.** Ни одна из четырёх не является готовой комнатой; fusengine — policy/hooks, не рантайм. Блокеры: нет общей ленты, фаз/раундов, изоляции сбоя участника, строгого `ПАС/упал/не звали`, физического слепого барьера. `live.py` уже содержит fork/join, раздельную фиксацию ошибок и timeout, поэтому прототип — он. Тонкий интерфейс: не трогать `post`/`deliver(blind)`/`turn`/`pick_voices`, добавить адаптер «голос → CLI»; встройка в Orca — только транспорт/транскрипт, не второй журнал. Промолчали в rebut1: Claude, Gemini, Grok. Сменили мнение: Codex (Orca-хост → `live.py`), DeepSeek (запасной fusengine → AG2), Kimi (риск «переписывание под Orca» → «две истины»).

**Уникальные идеи**

| Автор | Идея |
|---|---|
| Claude | «Безрукий голос»: участник без ФС должен пройти без worktree |
| Codex | Crash-drill: убить третий голос между ответом и публикацией |
| Codex (rebut) | Red-team слепоты: nonce, шестой ищет чужое в `/proc/*/fd`; утечка = нужен UID/container |
| DeepSeek | «Пустая комната»: 6 no-op × 100 сообщений |
| DeepSeek (rebut) | Commit-reveal: слепой ответ — `sha256(reply+nonce)` |
| Gemini | Отказ узла 3: таймаут/невалидный JSON; остальные 5 завершают |
| Grok | Readonly `sqlite3` по `orchestration.db`: каких полей комнаты нет |
| Kimi | Пин коммита и дата доступа для каждой репозиторной претензии |
| Kimi (rebut) | Batch `gh api` по всем ссылкам из ответов; 404/mismatch — вычёркивать |

Слабее прочих — стресс-тест Gemini: дублирует crash-drill/пустую комнату и не проверяет слепоту.

**Нерешено.** Протокол и смысл «шестеро» автором не зафиксированы — «готовность» остаётся допущением. Не выбран механизм enforce слепоты: commit-reveal или отдельные UID/контейнеры. Полная пин-сверка всех URL/issue/PR не сделана; факт-карты требуют до-сводной проверки. Не решены PTY, живущий между ходами в Orca, каноничность `live.jsonl` и судьба HTTP-only голосов: Orca-worktree исключает Gemini/DeepSeek по конструкции. DeepSeek Harness Agent Teams пересмотреть после публичного выпуска.