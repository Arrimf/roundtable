# Заготовка: DeepSeek в каноне состава стола

Черновик правок `CLAUDE.md` (корень AiSandbox). Вписывает Клод — по слову
Автора, от 2026-08-22. Составил DeepSeek (dsh-web), текст Автором одобрен
к передаче. Git не трогать: коммит — за Автором/Клодом.

Три правки, все в файле `AiSandbox/CLAUDE.md`.

---

## 1. Состав стола (строка 26)

Найти:

```text
· **Кими** (Moonshot, с 2026-08-14 — полноправный участник, не гость) · в `Film/` дополнительно **Сол** (GPT в приложении, через человека).
```

Заменить на:

```text
· **Кими** (Moonshot, с 2026-08-14 — полноправный участник, не гость) · **DeepSeek** (с 2026-08-22 — шестой голос, первая не западная лаборатория; принят по экзамену раундом «пары») · в `Film/` дополнительно **Сол** (GPT в приложении, через человека).
```

## 2. Версии (строка 28)

Найти:

```text
2026-08-16: gemini 0.55.1, claude 2.1.233.
```

Заменить на:

```text
2026-08-16: gemini 0.55.1, claude 2.1.233. 2026-08-22: deepseek-http 1.0 (deepseek-v4-pro).
```

## 3. Блок вызовов (внутри ```bash, после строки Кими)

Найти:

```text
~/.kimi-code/bin/kimi -m moonshotai/kimi-k3 -p "<промпт>"   # продолжить: kimi -c -p "текст"
```

Сразу после неё вставить:

```text
# DeepSeek — ПРЯМЫМ HTTP, как Джемини (CLI `dsh` — агентная оболочка, столу не нужна)
deepseek-http --prompt-file q.md                    # deepseek-v4-pro, effort=max; exit 3 = кончился баланс
deepseek-http --model deepseek-v4-flash -p "текст"  # быстрые поручения — одна модель, один флаг
```

---

Справка для проверяющего (не для вставки в канон):

- Голос `deepseek` уже живёт в `Choir/choir.py` (VOICES, с 2026-08-22) и в
  `Choir/live.py` (вписан DeepSeek через dsh-web, проверено Клодом):
  `no_files: true` — из кандидатов в контролёры на первоисточнике
  исключён, как Джемини (правило 14.5); `quota_exit: 3` — exit 3 адаптера
  `deepseek-http` = кончился предоплаченный баланс.
- Раскладка: слепые фазы — `deepseek-http` (deepseek-v4-pro, effort=max,
  30–90 с), агентные задачи контролёра — `dsh`.
