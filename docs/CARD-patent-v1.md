Сначала прочитаю полный запрос и ответы раунда — свод должен опираться на фактические позиции, а не на обрезок.# Свод «патент-v1»

**Не годится ни как основа заявки, ни как защитная публикация в нынешнем виде.** После открытой фазы так сказали все пятеро. Вслепую kimi и gemini ещё держали «годится как основа публикации после чистки / протокола»; после довода claude про п. 2 оба это сняли. ПАС и выпавших нет.

**Единогласно.** Формулы нет: п. 1–12 — каталог продукта и регламента. П. 2 («ответы слепой фазы не пишутся на диск вообще») ложен: CLI пишут историю, `recover` её читает, пользователь тот же — слепота через obscurity. §0 («журнал защищён от подмен») — `open(..., "a")`, без chain/hmac. Локальный файл prior art не создаёт. Публиковать ложное описание — enablement неработающего способа и карта дыр конкуренту.

**Кто менял.** kimi: чистки чисел мало. gemini: не годится даже как publication. codex: главный дефект сместил с журнала на п. 2. claude и grok вывод не меняли.

**Где разошлись** — здесь содержание раунда:

| Спор | Позиции |
|---|---|
| Что чинить первым | claude: отдельный `HOME` сессий. grok: сжать п. 2+3+4+8+9 в способ и дать дату. gemini: публиковать код, не черновик |
| Атака на жребий | claude: хвостовые пробелы в вопросе под уже взятую подпись (14 попыток → любой ведущий). grok/codex: повторный `pick` и затирание `role: lot`. Формула жребия и целостность файла — разные дыры |
| Цифры | grok: медианы 223/219 на срезе 226 строк сходятся. kimi: реплик голосов в live 85, не 50. claude: «все 12 затравок >2500» — grok опроверг (есть 1974 и 2459) |
| п. 10 | grok: в этой слепой фазе пять разных `prompt_sha` из-за `role_preamble`. Остальные это не закрыли |

**Свои идеи.** claude — drand как timestamp; commit-reveal (sha вопроса в R, жребий по R+k). codex — повтор маяка; тест non-interference канарейкой. gemini — Problem-Solution Approach; «ловушка дефектного раскрытия»: конкурент патентует рабочую изоляцию как преодоление ваших же дыр. grok — числа скопированы из комментариев кода, не из журнала; `recover` штатно вынимает то, чего «на диске нет». kimi — sha256 `PATENT-MATERIAL.md` (`45f7bd45…`) в `room.jsonl` нет.

**Нерешено.** Чинить механику или формулировку? Текст или репозиторий как prior art? Нужен dossier аналогов (codex) — запрошенных gemini `ДОСЬЕ-ТЕСТЫ.md` / `ДОСЬЕ-АНАЛОГИ.md` на диске нет (claude). Без даты, commit-hash среза и публичного размещения это ни заявка, ни prior art.