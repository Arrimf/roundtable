Сначала прочитаю полный пакет раунда — в запросе обрезан текст ответов.**Переезд v1 — GO: чинить стык уже живого окна с `live.py`, замораживать существующую SCHEMA=1 и `live.post`, не вторую схему. Полный такт `choir.py` в окне — открытый спор, не консенсус.**

### Где разошлись

| Вопрос | Позиции |
|---|---|
| `choir.py` в окне | **Грок, Дипсик (после витка), Джемини (после):** без такта раунды остаются в чате. **Кими, Клод:** v2 — хватает `say`/`ask`, такт из терминала. **Кодекс** чинит доставку, раунды не закрывает. |
| Что цементировать | **Кодекс:** логический конверт + `act_id`, не HTTP. **Грок:** ещё маршруты и argv. **Джемини/Дипсик вслепую:** `role/content…` — в коде этого нет. |
| Готовность | kill-9 ленты (**Дипсик, Джемини**) vs день без возврата (**Кими, Клод**) vs choir-такт в окне (**Грок**). |

### Единогласно после витка

Ядро уже есть: `say`/`ask`, SSE, жребий v2, лента через `live.post`. До переезда закрыть дыры `roundtable.py`: `stderr=DEVNULL`, рендер без `kind`, `/act` без `--project`/`--dossier`. Комната владеет записью; окно шлёт команды и читает события. `live.jsonl` и `room.jsonl` не сливать. В v2: markdown, поиск, правки, ветки, комнаты, мобильный, auth, ACP, скины. Хранение раньше косметики UX.

### Кто сменил мнение

- **Кодекс:** в must-have добавил `--project`/`--dossier`.
- **Дипсик:** снял «CLI из одного файла»; принял три дыры и вызов `choir.py`.
- **Джемини:** блокируют не «нет БД», а стык окна; WAL оставил.
- **Кими:** отказ голоса уже в ленте (`kind=error`); дыра — рендер и глотание stdout/stderr.
- **Грок:** ядро не менял; хвост SSE из 80 строк и стирание черновика принял как v1.
- **Клод** вошёл только открытым: «пробел ровно один» опроверг (четыре дыры); kill-9 окна как критерий уже пройден.

ПАС и выпадов нет. Слепые: пятеро. Открытая: плюс Клод.

Поздний сход трёх (**Клод, Кодекс, Дипсик**): заморозка — падающий тест, не список полей.

### Свои идеи

- **Кодекс:** квитанция `act_id` + диапазон `event_id`; золотой журнал.
- **Дипсик:** trapdoor kill-9; эталонные JSONL-фикстуры.
- **Джемини:** WAL до сети; `import-seed` блока `legacy_context`.
- **Грок:** канарейка `kind=error` при старте; приёмка = тот же такт, что этот раунд.
- **Кими:** `retreat` в ленте, ноль семь дней — без нового кода.
- **Клод:** контрактный тест SCHEMA=1; `/state` ходит в drand под замком.

### Нерешено

1. Входит ли полный `choir.py` в v1 — нужен наказ Автора.
2. Замораживать ли HTTP и `act_id` сверх SCHEMA=1.
3. Рубеж смок-ленты vs работа (**Кими**) и сироты process group (**Кодекс**) спором не закрыты.
Finishing session…