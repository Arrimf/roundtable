# темы-на-столе-v1 — темы на столе и вер1 как инструмент (вер1, 2026-09-06)

Вопрос Автора: «план гипотез и остальные раунды пора добавлять на стол,
чтобы можно было переключаться в обсуждении по разным темам»; там же
замечено, что «стола вер1» в репозитории нет. Формат: вер1 — шесть
параллельных read-only вызовов по одному пакету, без слепой фазы и
витков; агентные голоса (grok, kimi, claude, codex) могли читать код,
HTTP-голоса отвечали по пакету. Ответы дословно ниже (правило 3).
Свод — Клод (дирижёр журналирует, не судит); решение за Автором.

## Свод (≤400 слов)

**Сошлись (5 из 6):** тема — это поле у события в ОДНОЙ ленте
`journal/live.jsonl` (`topic`/`topic_id`, у старых событий пусто или
`main`), а не отдельная лента на тему и не синоним раунда: раунды
вер1/вер2 принадлежат теме, тема живёт между ними. grok и kimi нашли,
что зачаток уже есть: `cmd_ask` пишет событие `kind=topic` с полем
`topic`, но дальше поле не наследуется. Список тем — фильтр над той же
лентой в боковой панели; вкладки «обещают изоляцию, которой нет»
(grok). Все шестеро: вер1 нужен как код, но старые скрипты дирижёра
не переносить; результат вер1 должен попадать в `room.jsonl`, чтобы
окно рисовало карточку той же функцией.

**Где разошлись:**

| Вопрос | Позиции |
|---|---|
| Хранение темы | поле в одной ленте — codex, grok, kimi, gemini, claude; отдельные `journal/topics/<имя>.jsonl` с состоянием голоса на тему — deepseek (единственный) |
| Нить голоса | одна сессия на голос, курсор на пару (голос, тема) — grok, kimi, gemini, claude; сессия на пару (голос, тема), для слепого раунда чистая — codex; состояние на тему — deepseek |
| Остывание 7.5 | глобальное, «микрофон один» — grok, kimi, gemini, codex; на пару голос+тема — claude (и делает из этого свою идею) |
| Вер1 как код | режим/подкоманда `choir.py` — grok («`choir.py v1`, не флаг у run: SCHEMA иначе врёт про жребий и слепую фазу»), kimi (`--no-rebut --no-verdict`, `mode: "v1"`), deepseek (`--mode v1`), gemini (`--flat`), claude (`--coils 0 --seed packet`); отдельный тонкий `chamber/v1.py` — codex |
| Что уходит голосу при переключении | дельта только этой темы плюс свод/хвост — codex, grok, deepseek, gemini, claude (потолки от 5–8 реплик до ~2–3 тыс. знаков); дельта всей ленты плюс заголовок темы (+~20 токенов) — kimi |

**Что ломается, по коду (grok):** `say`/`pass`/`error` поле не
наследуют; `build_prompt` выкидывает `kind=topic` из дельты; `deliver`
читает `since=cursor` по всей ленте; `round_view` кладёт в карточку
только `phase == "blind"` — вер1 с другой фазой покажет пусто.

**Первый вечер (сошлись):** наследовать `topic` в `post` после
текущего события `kind=topic`; фильтр ленты по теме в окне; в `deliver`
дельта только этой темы; в `state.json` курсоры по темам; научить
`round_view` фазе вер1 и дописать готовый `план-гипотез-v1` в
`room.jsonl`. **Не делать:** `topics/*.jsonl`, сессию на тему, `#`
как единственный вход, флаг `--no-blind` у `run`, миграцию старой
ленты, git-подобное ветвление тем.

**СВОИ ИДЕИ:** codex — явное событие `topic_bridge` для переноса мысли
между темами (межтематическая память как проверяемый вход, а не утечка
через сессию); grok — поле `session_topics: [A, B]` в событии хода:
слепоту темы ломает не jsonl, а CLI-нить, и смешанный контекст должен
быть виден в журнале; kimi — тема умеет «засыпать» (`last_active`), и
целеполагатель может предложить вернуть спящую; deepseek — «шёпот
тем»: одна строка в пакете о новом доводе в соседней теме; gemini —
«диффузный мост» `#инсайт <тема> <event_id>`, одиночный вывод из
соседней темы как внешний факт; claude — переключение темы и есть
остывание по 7.5, а отдельно на тему живёт только файл свода.

**Время ответов:** gemini 14 с, codex 35 с, kimi 47 с, claude 52 с,
deepseek 84 с, grok 161 с; отказов нет.

## Вопрос (пакет, одинаковый для всех)

ВОПРОС СТОЛУ ВЕР1 (read-only, 300–450 слов, вывод первой строкой; агентные голоса могут открыть файлы RoundTable/, HTTP-голоса отвечают по пакету; инструменты не вызывать — ответ текстом).

Тема: ТЕМЫ НА СТОЛЕ. Автор (2026-09-06): «план гипотез и остальные раунды пора добавлять на стол — чтобы можно было переключаться в обсуждении по разным темам». Сейчас комната (chamber/live.py) — одна лента journal/live.jsonl без тем: каждая реплика — хвост одного разговора; нити голосов (journal/voices/*/state.json) держат одну сессию на голос; пакет голосу — дельта «что он ещё не видел» плюс цель целеполагателя. Раунды вер2 (chamber/choir.py: жребий → затравка → слепые ответы → витки → свод) лежат записями в journal/room.jsonl и показываются в окне карточкой под событием ленты. Раунды вер1 (параллельные read-only вызовы codex/grok/kimi/deepseek/gemini/claude по одному пакету, без слепой фазы и витков) вообще не инструмент: это скрипты дирижёра во временной папке, результат — файл journal/rounds/<проект>/<тема>-v1.md; Автор заметил, что «стола вер1» в репозитории нет.

Подвопросы:
1. Что такое «тема» на столе: отдельная лента на тему (journal/topics/<имя>.jsonl), поле topic у события в одной ленте, или тема = раунд (вер1/вер2), а комната — обсуждение вокруг него? Для каждого варианта — что ломается: правило 1 (один пакет всем), 8.5 (слепота отсутствием данных), 7.5 (остывание говорившего), дельта пакета, нити голосов (одна сессия на голос — общая на все темы или своя на тему?), карточки раундов в окне.
2. Переключение темы в окне: как выглядит для Автора (список тем сбоку, вкладка, команда «#тема» в реплике?) и что уходит голосу при переключении — вся история темы, дельта, или свод плюс последние N реплик. Цена в токенах и во внимании Автора (правило 16).
3. Вер1 как инструмент: нужен ли chamber/v1.py (пакет → параллельные вызовы → файл и карточка в окне), или вер1 — это choir.py run без слепой фазы и витков, или вер1 не нужен как код вовсе. Что записать в журнал, чтобы вер1-раунд был виден в окне карточкой, как вер2.
4. Минимальный первый шаг (один вечер работы), который не придётся переделывать, и чего НЕ делать.
СВОЯ ИДЕЯ: одна мысль, которой, по-вашему, не будет ни у кого другого.
Критерии хорошего ответа: конкретно по коду (пути, поля событий, команды), честно про то, что ломает слепоту, с ценой; расхождения ожидаются в п. 1 (лента на тему против поля) и п. 3 (нужен ли вер1 как код).


## Статусы вызовов

```
gemini rc=0 14s
codex rc=0 35s
kimi rc=0 47s
claude rc=0 52s
deepseek rc=0 84s
grok rc=0 161s
ALL DONE 161s
```

## Ответы дословно


=================== codex
Вывод: тема — самостоятельное обсуждение с `topic_id` в общей ленте; раунды вер1/вер2 принадлежат теме, но не определяют её.

Опираюсь на предоставленный пакет; файлы не открывал. Ниже — предлагаемые поля и команды, а не описание существующего API.

**1. Хранение и границы.** Отдельные `journal/topics/<имя>.jsonl` упрощают чтение истории темы, но требуют общего порядка событий, маршрутизации карточек и решения, куда писать переключения. Правила 1, 8.5 и 7.5 сами файлы не обеспечивают; дельта становится курсором внутри конкретной ленты.

Поле `topic_id` в `journal/live.jsonl` сохраняет единый порядок и существующий журнал. Старые события можно читать как `topic_id="main"`. Ломается прежняя семантика «ещё не видел»: одного курсора голоса недостаточно, нужен курсор `(voice_id, topic_id)`. Фильтрация должна предшествовать вычислению дельты, иначе чтение одной темы отметит другую прочитанной. Карточки связываются через `topic_id` и `round_id`, а не только визуальное соседство.

«Тема = раунд» выглядит экономно, но смешивает длительное обсуждение с конечной процедурой. План гипотез может пережить несколько раундов обеих версий; продолжение потребует искусственного нового раунда или переоткрытия завершённого.

Во всех вариантах общая сессия голоса переносит сведения между темами. Предпочитаю сессию на `(voice_id, topic_id)`; для слепого раунда — отдельную чистую сессию. Уже увиденные ответы нельзя скрыть последующей фильтрацией: правило 8.5 нарушено памятью сессии. Правило 1 требует единого зафиксированного пакета раунда: персональные дельты ему не эквивалентны. Остывание по 7.5 стоит сохранить на уровне комнаты, чтобы переключение темы не позволяло немедленно вызвать говорившего снова.

**2. Переключение.** Автору — список тем сбоку, активная тема над полем ввода, карточки внутри её истории. Команда `/topic <id>` может дублировать интерфейс; `#тема` в свободном тексте неоднозначна.

Переключение лишь меняет просмотр, без вызова моделей. При следующем вызове существующая сессия получает дельту своей темы; новая — общий закреплённый свод плюс ограниченный хвост. Полная история дороже пропорционально накопленному тексту; свод дешевле, но теряет детали. Для раунда граница истории и версия свода фиксируются одинаково всем. По правилу 16 Автор не должен вручную собирать контекст после каждого клика.

**3. Вер1 нужен как инструмент.** Предлагаю тонкий `chamber/v1.py`: единый пакет → параллельные вызовы → Markdown и регистрация результата. Общие вызовы и запись карточек следует переиспользовать из доступных механизмов, но не изображать вер1 стадиями вер2. В `journal/room.jsonl` нужны предлагаемые поля `round_id`, `topic_id`, `version: 1`, `status`, `package_hash`, `artifact_path`; событие комнаты ссылается на этот раунд. Точный формат адаптируется к существующему рендереру.

**4. Один вечер.** Добавить совместимое чтение `topic_id`, регистрацию готового вер1-файла и показ карточки в выбранной теме. Не переносить журналы, не переписывать `choir.py`, не включать тематические вызовы до разделения курсоров и сессий.

**Своя идея:** перенос мысли между темами оформлять явным событием `topic_bridge` со ссылкой на источник. Тогда межтематическая память становится проверяемым входом пакета, а не незаметной утечкой через сессию.
=================== grok
Читаю живые файлы комнаты, журнала и окна, чтобы ответить по коду, а не по пересказу.Дальше смотрю схему событий, дельту пакета, нити и как окно рисует карточки раундов.**Тема — поле `topic` у событий одной ленты `journal/live.jsonl`, не отдельный jsonl и не синоним раунда; вер1 в код нужен как писатель в `room.jsonl` + карточка в окне, не как `choir.py run` без слепой фазы.**

**1. Что такое тема.** Третий вариант (тема = раунд, комната вокруг) ломает наказ Автора: план гипотез и «остальные раунды» — закрытые артефакты (`journal/rounds/<проект>/*-v1.md`, вер2 — записи `room.jsonl` с `role=lot|seed|answer|summary`), а переключаться надо в живом разговоре. Раунд уже вешается на ленту полем `round` у `act_status`/`note` (`roundtable.py` `add()` → `roundCard`). Смешать ось «о чём говорим» с осью «закрытый такт» — два смысла в одном ключе.

Отдельные `journal/topics/<имя>.jsonl` ломают устав («одна истина — `live.jsonl`»), монотонный `id` (`live.post` / `next_id`), курсор нити (`voices/*/state.json`), flock одного файла и SSE окна (`FEED`). Слепоту (8.5) файлы не дают: агент читает любой путь из кода. Правило 1 при этом формально живо (пакет собирается из «своего» файла), но несравнимо: разные ленты — разные входы.

Поле `topic` на одной ленте — уже зачаток: `cmd_ask` пишет `kind=topic` с `topic=`, `thread` для адресных веток — тот же приём. Что ломается, если только завести поле и не чинить читателей: (а) `say`/`pass`/`error` поле не наследуют — фильтр пуст; (б) `build_prompt` выкидывает `kind=topic` из дельты; (в) `deliver` берёт `read_events(since=cursor)` по всей ленте — чужая тема в пакете, правило 1 врёт «один пакет» внутри темы; (г) остывание 7.5 (`_quietest`, `COOLDOWN=2`) глобально — это **оставить**: микрофон один, иначе говоривший в А сразу лидирует в Б; (д) нить CLI одна (`session` / cwd `voices/<имя>/`) — фильтр jsonl не стирает память `grok -c` / `kimi -c`. Карточки раундов не ломаются: они ключятся полем `round`, не темой.

Нить: **сессия одна на голос, курсор — на пару (голос, тема).** Дробить cwd на тему — вечер в никуда (Грок/Кими привязаны к каталогу).

**2. Переключение.** Автору — список тем сбоку (уникальные `topic` + `kind=topic`), лента фильтруется; `#тема` в реплике может *ставить* поле, но не быть единственным UI (вкладки обещают изоляцию, которой нет). Голосу при смене — не вся история и не сырая дельта всей ленты: цель (`kind=goal`) + хвост этой темы с её курсора (потолок как `COLD_TAIL=40`, лучше ~2–3 тыс. знаков). Полный архив уже мерили: холодный старт запасной линии — 59 635 vs 8 171 символов. Цена Автору (правило 16) — боковая колонка и одна лента, не шесть рекапов.

**3. Вер1.** Не `choir.py run` без слепоты: журнал SCHEMA=2 тогда врёт «был жребий и слепая фаза». Не нужен новый протокол. Нужен тонкий раннер (хоть `choir.py v1`): тот же `ask_one`, пакет один, ответы **в памяти до закрытия** (агент иначе прочтёт чужой ответ в `room.jsonl` — тот же 8.5, что у вер2), затем разом: `role=answer, phase=v1` + свод; в ленту — `note`/`act_status` с `round=`. Сейчас `round_view` кладёт в карточку только `phase=="blind"` — вер1 с `phase=v1` кнопка «ответы» покажет пусто. Кнопки витка/свода и так скрыты без `conductor`. Markdown в `journal/rounds/` остаётся производным, не второй истиной.

**4. Вечер, который не переделывать.** Наследовать `topic` в `post` после текущего `kind=topic`; фильтр ленты в окне; в `deliver` — дельта только этой темы; в `state.json` — `cursors: {тема: id}`. Для вер1 — научить `round_view` фазе `v1` и дописать в `room.jsonl` уже готовый `план-гипотез-v1`. Не делать: `topics/*.jsonl`, сессию на тему, `#` как единственный вход, флаг `--no-blind` у `run`.

**СВОЯ ИДЕЯ:** слепота темы ломается не jsonl, а CLI-нитью. Фильтр дельты без сброса сессии — театр, если `context_sha` хеширует только дельту. Писать в событие хода `session_topics: [A,B]` (темы, жившие в этой сессии): поле обеспечено `state.json`, и смешанный контекст через месяц не прочтётся как чистая позиция по Б.

=================== kimi
• Гляну в три файла, чтобы отвечать по коду.

• **Вывод: тема — это не лента и не поле-декорация, а якорь: событие `topic` в одной ленте плюс обязательное поле `topic` у всех последующих событий; вер1 становится тонкой обёрткой в choir.py, а не отдельным кодом.**

  **1. Что такое тема.** Против отдельной ленты `journal/topics/<имя>.jsonl`: ломается правило 1 (один пакет всем) — дельта «что голос ещё не видел» сейчас считается по одному потоку; с N лентами у каждого голоса N курсоров, и «один пакет» превращается в N пакетов или в лукавство. Против «тема = раунд»: раунд — событие с жребием и сводом, а тема «план гипотез» живёт неделями между раундами. Остаётся поле: в `live.py:1547` уже есть `post(HUMAN, "topic", a.text, topic=...)` — команда `ask` и есть смена темы, просто поле `topic` не проброшено дальше. Добавить `topic` (slug) в каждое событие ленты до следующего `topic`-события. Слепота (8.5) не ломается: слепой первый ход уже привязан к `ask`. Остывание (7.5) не ломается: считается по голосу, не по теме — и правильно, иначе переключение темы станет обходом остывания. Нити голосов (`state.json`, одна сессия на голос) — общие: своя сессия на тему убьёт `--resume` для claude и раздует state; голосу достаточно в пакете видеть границу темы.

  **2. Переключение в окне.** Список тем сбоку — это фильтр по полю `topic` над той же лентой, а не новый источник. Голосу при переключении — не вся история темы: дельта по ленте целиком плюс заголовок активной темы. Полная история темы — по требованию (как сейчас файл раунда). Цена: +~20 токенов на пакет (slug темы), против сотен за пересборку истории. Внимание Автора (правило 16): список тем — четыре-пять строк, не вторая лента.

  **3. Вер1 как инструмент.** Не `chamber/v1.py` — третий исполнитель параллельных вызовов это вторая истина. Но и «не нужен вовсе» неверно: Автор сказал, скрипты во временной папке теряются. Вер1 — это режим choir.py: фаза ask без витков и свода, запись в `room.jsonl` с тем же SCHEMA (флаг `mode: "v1"`, `rebut_phases: []`). Тогда `round_view()` в roundtable.py:342 подхватит его карточкой бесплатно — карточки уже строятся из записей комнаты. Плюс `feed_append` в ленту с ссылкой на `journal/rounds/...` — как сейчас у вер2.

  **4. Минимальный шаг за вечер.** (а) Пробросить `topic` из `ask` в события ленты и в пакет. (б) В choir.py — флаг `--no-rebut --no-verdict`, пишущий тот же формат. (в) В окне — фильтр тем. НЕ делать: topics/*.jsonl, сессии на тему, отдельный v1.py, миграцию старой ленты (пусть у старых событий topic пустой).

  **Своя идея:** тема должна уметь «засыпать»: slug + `last_active`, и целеполагатель раз в N событий может предложить вернуть спящую тему — тогда переключение инициирует стол, а не только Автор, и «план гипотез» сам всплывёт, когда созреет.


=================== deepseek
Вывод: темы — отдельные ленты `journal/topics/<name>.jsonl`, переключение — `/topic <name>`, вер1 — режим `choir.py --mode v1` с записью в `room.jsonl`; поле `topic` в общей ленте не вводить.

1. Тема:  
- Отдельная лента: события темы в `journal/topics/<name>.jsonl`; общая `journal/live.jsonl` остаётся для системных событий (`topic_switch`, карточки раундов). Правило 1 сохраняется: пакет шлётся только из активной темы всем голосам. 8.5 не ломается, если состояние голоса per-topic (`journal/voices/<voice>/topics/<name>.json`) — он видит только свою тему. Дельта — от курсора в файле темы. 7.5 — остывание per-topic. Карточки раундов — события в `room.jsonl` с полем `topic`.  
- Поле `topic` у реплик в одной ленте — ломает дельту (фильтр и курсор на тему), 8.5 (голос видит чужие темы в том же файле) и требует переделки нитей.  
- Тема = раунд: не покрывает свободное обсуждение между раундами; раунд — событие внутри темы, а не сама тема.

2. Переключение: Автору — команда `/topic <name>` (или список слева). Голосу при переключении: если у него уже есть сессия темы — дельта (новые реплики с его `last_seen`); если нет — свод (автосвод ≤1К токенов) + последние 20 реплик. Цена: первое вхождение ~2–3К токенов на голос, повторное — только дельта. Для Автора — активная тема + бейджи непрочитанного; правило 16 — не показывать все темы сразу.

3. Вер1: отдельный `chamber/v1.py` не нужен. Это `choir.py run --mode v1` (без слепой фазы и витков): один пакет → параллельные вызовы → файл `journal/rounds/<project>/<topic>-v1.md` → запись в `room.jsonl`: `{type:"round_v1", topic, project, path, summary, participants, ts}`. Карточка в окне — та же, что для вер2, по `type`. Старые скрипты дирижёра — legacy, их не переносить.

4. Минимальный шаг: в `chamber/live.py` — команда `/topic <name>` (создать `topics/<name>.jsonl`, переключить активную тему, записать `topic_switch` в `live.jsonl`); в пакет голосу добавить `topic` и `last_seen`; состояние голоса — словарь `{topic: last_seen}`. Не делать: не мигрировать старую ленту, не строить UI вкладок, не вводить `topic` в каждую реплику `live.jsonl`, не заводить общий state на все темы.

СВОЯ ИДЕЯ: «шёпот тем» — в пакет при переключении добавлять одну строку: «в соседней теме X появился новый аргумент: ...» (20–30 токенов). Это сохраняет правило 1, не раскрывает чужую слепоту и мешает голосам дублировать идеи.

=================== gemini
**Вывод:** Тема должна быть раундом-якорем с контекстной фильтрацией в общем `journal/live.jsonl` через поле `topic_id`, а «Стол вер1» — плоским режимом `choir.py --flat` без отдельного скрипта.

1. **Что такое «тема»:**
Оптимальный вариант — **тема как раунд-якорь плюс поле `topic_id` у событий в `live.jsonl`** (по умолчанию `topic_id: "main"`). 
* *Файлы `topics/<имя>.jsonl`*: ломают единый аудит-лог и усложняют файловые вотчеры.
* *Поле `topic_id` в общем файле*:
  - **Правило 1 (один пакет)** и **8.5 (слепота):** не страдают, изоляция соблюдается на уровне сборщика пакета.
  - **Правило 7.5 (остывание):** остаётся глобальным (голос остывает по физическому времени стола, а не по теме).
  - **Дельта и нити:** в `journal/voices/<voice>/state.json` поле `last_seen_id` превращается в карту `{"last_seen": {<topic_id>: <event_id>}}`. Сессия HTTP-моделей остаётся сквозной, но дельта фильтруется по текущему `topic_id`.

2. **Переключение темы:**
* **UI для Автора:** Вкладки или список карточек раундов в сайдбаре окна; в консоли — префикс `#<topic_id>` или команда `/topic <id>`.
* **Пакет голосу при переключении:** `Сводка (summary) якорного раунда + цель + последние N реплик темы (delta)`. Полная история темы не передаётся.
* **Цена:** ~1.5–2.5k токенов за смену фокуса против раздувания контекста всей комнаты. Внимание Автора (правило 16) сохраняется: на экране только актуальный срез без чужого шума.

3. **Вер1 как инструмент:**
Создавать `chamber/v1.py` **не нужно**. Вер1 — это `choir.py run --flat` (или `--turns 0 --no-blind`), выполняющий веерный read-only опрос.
Чтобы вер1 отображался карточкой, в `journal/room.jsonl` (или `live.jsonl`) пишется стандартный JSON:
`{"event": "round", "ver": 1, "topic_id": "hyp-1", "seed": "...", "voices": {"codex": "...", "gemini": "..."}, "summary_file": "journal/rounds/p1/hyp-1-v1.md"}`. Окно рендерит карточку по типу события `round`.

4. **Минимальный шаг на вечер:**
1. Добавить в `chamber/live.py` поддержку тега `topic_id` в событиях и команду `/topic <id>`.
2. В `chamber/choir.py` добавить флаг `--flat` для запуска опроса вер1 с записью карточки в `journal/room.jsonl`.
* **ЧЕГО НЕ ДЕЛАТЬ:** Не дробить физические файлы на `journal/topics/*.jsonl`, не реализовывать сложное ветвление/слияние тем (git-like) и не делать независимые сессии на каждую тему внутри голосов.

**СВОЯ ИДЕЯ:**
*«Диффузный мост» между темами:* команда `#инсайт <from_topic_id> <event_id>`, которая выдергивает одиночный вывод из соседней темы и подает его в текущую как «внешний факт от Автора». Это позволяет связывать гипотезы без объединения контекстов и без сброса дельты у голосов.

=================== claude
**Вывод: тема — поле `topic` у события в одной ленте live.jsonl, одна нить на голос, вер1 — режим choir.py, а не отдельный файл.**

**1. Что такое тема.**
- *Лента на тему* (`journal/topics/<имя>.jsonl`). Ломает дельту: указатель «что голос видел» в `voices/*/state.json` сейчас один на ленту, придётся хранить `seen[topic]`. Ломает нити: одна сессия на голос тащит контекст темы B в разговор о теме A, а сессия на тему — умножение сессий на число тем, цена в токенах кратная. Карточки раундов в room.jsonl теряют привязку к событию ленты.
- *Поле `topic`* в одной ленте. Правило 1 не ломается: пакет — та же дельта, отфильтрованная по теме. 8.5 не страдает: слепота — про ответы других в раунде, а не про соседние темы. 7.5 (остывание) становится счётчиком на пару голос+тема. Отсутствие поля читается как `main`, старый журнал не мигрируется. Слабое место — сессия голоса помнит чужую тему; лечится тем, что пакет открывается строкой «тема: X, свод».
- *Тема = раунд*. Не годится: план гипотез — тема без раунда, а раунд — точка, не нить.

**2. Переключение.** Команда `#тема` первым словом реплики в окне, текущая тема видна в строке ввода; список тем сбоку — просто `distinct topic` по ленте, без состояния. Голосу при первом касании темы в сессии уходит свод последнего раунда темы плюс последние 5–8 реплик, дальше — обычная дельта. Цена: один свод на голос на переключение, а не история. Внимание Автора (16): один маркер в строке, ничего запоминать не надо.

**3. Вер1 как код.** Отдельный `chamber/v1.py` не нужен: вер1 — это choir.py с нулём витков и затравкой из пакета Автора, без жребия. Достаточно флага `run --coils 0 --seed packet` и записи в room.jsonl события того же вида, что у вер2, с `ver: 1`, `topic`, и путём к `journal/rounds/<проект>/<тема>-v1.md`. Окно рисует карточку той же функцией: чего нет — не показывает.

**4. Первый вечер.** Добавить `topic` в события live.jsonl и room.jsonl, фильтр по теме в сборщике пакета, `#тема` в окне. Не делать: отдельных лент, сессий на тему, боковой панели, миграции старого журнала.

**Своя идея.** Переключение темы — и есть механизм остывания по 7.5. Сейчас остывание — это молчание говорившего, а молчание в комнате стоит внимания. Если счётчик 7.5 ведётся на пару голос+тема, то Автор, переходя к другой теме, даёт разгорячившемуся голосу остыть, не выключая его из разговора вовсе. Тогда тема — не раздел архива, а линза, а единственное, что живёт на тему отдельно, — файл свода `journal/topics/<имя>.md`, заменяющий голосу историю при возврате.
