• Остаюсь в роли фактчекера — начинаю с проверки якорей и чисел по первоисточникам.

• Проверю, не лежат ли JSON-результаты прогонов где-то ещё — для верификации чисел 24/96/192 ч.

• Фактчек завершён. Якорь `04fe85d3fcc20e5f` бьётся с sha256 файла `ВОПРОС-ПАРЫ.md`, числа сверены с `LEDGER.md`, `coint.py` и файлом происхождения — есть одно расхождение и одна неверифицируемая группа чисел. Ниже ответ в рамках роли, по формату раунда.

  ---

  **Решение: регистрацию открывать условно — регистрировать замороженный подтверждающий эксперимент на новых данных, а не торговую гипотезу; часть чисел вопроса журналом не подтверждается и основанием служить не может.**

  Фактчек. sha256 `04fe85d3fcc20e5f` совпадает с `ВОПРОС-ПАРЫ.md`. Совпали с `LEDGER.md` и файлом происхождения: +4.9% на 48ч, by-pair +5.4%/z=3.15, матчинг +4.9%/z=2.55, экономика +0.071σ/z=1.83. Не совпал контроль доминанты: в вопросе +3.3%/z=2.02, в обоих источниках +3.7%/z=2.34 — числа из разных прогонов. Горизонты 24/96/192 и z=3.43 для 48ч невоспроизводимы: профили `h1_h24/h1_h96/h1_h192` в `coint.py:66-71` есть, но каталога `results/` на диске нет, записи в журнале нет.

  **1.** Да, условно. Объект — подтверждающий эксперимент (замороженная спека + один прогон), не торговая гипотеза: экономический эффект z=1.83 издержки не переживёт. Расхождение 3.3/3.7 и отсутствие результатов горизонтного среза доказывают: exploratory-числа — не основание, регистрировать можно только пересчёт на зафиксированном коде.

  **2.** Один primary endpoint: разница долей возврата на 48ч, единица — пара, окно 240 баров, пороги 2.0/0.5σ, критерий объявлен до прогона (diff ≥ +3 п.п. при z ≥ 2.58). Выборка — период после 21.08, код по sha. Горизонты 24/96/192 — secondary с поправкой Холма либо диагностические. Правило остановки: один прогон, доборы запрещены. Главный риск ложного вывода — winner's curse: горизонты выбраны по максимуму разницы post hoc, старые z завышены отбором и в успех не засчитываются.

  **3.** Симуляция эпизода: две ноги, комиссия и спред, равный риск на эпизод, выход — касание середины или тайм-стоп 48ч. Primary — средний PnL на эпизод после издержек против нуля. Диагностики — доля «ушёл дальше», max adverse excursion, 95%-квантиль убытка, turnover. Сравнение с некоинтегрированными — механистический контроль, но торговое решение принимается по знаку PnL.

  **4.** Затухание — отдельная проверяемая гипотеза о границе режима, но сперва неустановленный факт: числа нужно воспроизвести. Вывод изменит воспроизведение той же формы затухания на новых данных — тогда это граница режима, а не артефакт подбора. Опровергнет мою позицию: diff ≥ +3 п.п. при z ≥ 2.58 и PnL после издержек > 0 на замороженной спеке — тогда переводить ветку в торговую регистрацию.

  **СВОЯ ИДЕЯ:** прослеживаемость как в метрологии — число без цепочки «sha кода → sha данных → файл результата» считается отсутствующим, а не слабым; расхождение 3.3%/z=2.02 против журнальных 3.7%/z=2.34 — первый пойманный случай, где такая цепочка уже нужна.