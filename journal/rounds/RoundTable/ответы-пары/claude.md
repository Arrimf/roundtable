РОЛЬ: фальсификатор · `eyes: files` (читал `LEDGER.md`, `coint.py`, `controls.py`, доску и `git log` в Cursor_W — не досье)

**Условно: регистрировать не находку, а её опровержение — предрегистрированный тест на *времени до возврата* с кластеризацией по инструментам; торговую гипотезу — нет, экономика уже ответила.**

**Факты из файлов, которых нет в затравке.** Каталога `results/` не существует, ветка не в git — артефактов прогона на диске нет ни одного. Времена правки: `LEDGER.md` 22:55, `controls.py` 23:08, `coint.py` 23:15 — журнал описывает прогон кодом, который потом меняли. Профили `h1_h24/h1_h96/h1_h192` есть в `coint.py:66-71`, но в LEDGER четырёх горизонтов нет вовсе: числа из вопроса Автора нигде в ветке не записаны. Контролей в `controls.py` **три**, а не четыре: докстринг обещает «время», кода нет, и +5.7%/+5.0% невоспроизводимы. Контроль без доминанты: LEDGER +3.7%, z=2.34; затравка +3.3%, z=2.02 — два источника, разные числа. Регистрировать сегодня нечего: нет ни замороженного кода, ни снимка данных, ни JSON.

**(2, 4) Горизонты вложены — «затухание» артефакт метрики.** Вход один (|z|>2, окно 240), меняется только длина наблюдения; `episodes()` (`coint.py:135`) считает возврат как *хоть одно* касание 0.5σ. Доля возврата монотонно не убывает по горизонту, обе группы насыщаются → разница обязана идти к нулю. 96/192 — не граница режима и не свидетельство ложного открытия. Primary endpoint: медиана времени до первого касания 0.5σ и hazard ratio (лог-ранг), а не доля; вывод тогда читается как «возвращаются быстрее, а не чаще», что согласуется с +0,071σ.

**(2) Кластеры.** 66 коинтегрированных пар собраны из 22 инструментов (6A — в 17). z=3,15 «по парам» считает 65 независимых наблюдений; независимых кластеров ≤22. Код это признаёт сам (`coint.py:231`). Кластерная поправка по инструменту — единственный непроведённый контроль, способный убить эффект.

**(3) Тавтологичность не снята.** `spread_vol = std(diff(resid_te))` — волатильность приращений, а возврат нормированного z определяется variance ratio уровня, то есть тем же, что меряет ADF: матчинг матчит не ту величину. Плюс `ADF_LAGS=1` на часовках даёт over-rejection — 66 из 229 пар «коинтегрированы» при номинале 5%.

Первичная метрика торгуемости — не доля, а нетто-σ после издержек на оборот при удержании до 48ч; +0,071σ должно быть первичным, доля возврата — диагностикой.

**Главный риск ложного вывода:** согласие трёх контролей измеряет качество одной процедуры, а не факт. **Опровергло бы меня:** hazard ratio ≥1,3 при кластеризации по инструменту, на замороженной спеке, на данных после 21.08.

**СВОЯ ИДЕЯ:** ряды — склеенные фьючерсы (сегодняшний коммит `8fe9ac8` про CONTFUT); проверить, не живёт ли эффект 24–48ч на синхронности *календарей ролловера*, а не на экономике: выбросить окна ±2 дня вокруг склейки — если эффект исчезает, мы измерили расписание биржи.