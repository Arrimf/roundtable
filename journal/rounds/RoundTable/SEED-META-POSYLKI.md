# МЕТА-РАУНД: исследование посылок наших исследований

Год работы, четырнадцать пре-регистраций. Мы ни разу не смотрели на них
ВМЕСТЕ — только по одной, изнутри каждой. arr предложил посмотреть на
посылки: не «верна ли спека X», а «во что мы верили каждый раз, не
записывая этого в спеку».

## Счёт исходов (по журналам веток)

- **PASS: 2.** `p2hero` (β=+0.478, z=9.59 — с оговоркой самого автора о
  тавтологичности) и `anomaly-quiet` ES (−11.9 пп, эмпирический
  p=0.0069, BH выжил).
- **УБИТО НАМЕРЕННО: 2.** `analog_knn` (KILL на dev), `sweep_reclaim`
  (N мал И знак против).
- **INCONCLUSIVE: 6+.** `vwap_touch` (+4.0% при объявленном пороге 5),
  `box_cohort_phase_a`, `reg5_fade` (38<50), `reg4` (H1 p=0.16),
  `vpoc` (Δ=−0.85 пп), `pairs_coint` (эффект был, контроль variance
  ratio не прошёл).
- **ЗАКРЫТО БЕЗ СИГНАЛА: 1.** `momentum_precursor` (18 проб, 2
  выживших, оба захоронены).
- **В РАБОТЕ: остальные.**

То есть: из четырнадцати честно доведённых регистраций — два PASS,
один из которых сам автор считает тавтологичным.

## Два объяснения, и они несовместимы

**(A) Рынок эффективен** в тех местах, где мы искали, и счёт 2/14 —
это правильный ответ природы. Тогда наша машина работает: она честно
хоронит пустое.

**(B) Наши приборы систематически слепы** к тому классу эффектов,
который есть. Тогда 2/14 — свойство конструкции, а не рынка, и мы
год отлаживали прибор, который по построению видит только то, чего
почти не бывает.

Различить их изнутри одной регистрации нельзя. Поперёк четырнадцати —
может быть, можно.

## ВОПРОСЫ (отвечать по пунктам)

1. **Какие посылки повторяются во ВСЕХ или почти всех спеках и ни разу
   не были предметом проверки?** Не про статистику вообще — про наши
   конкретные тексты. Примеры того, что я подозреваю сам, но хочу
   независимой проверки: бинарный исход «коснулось / не коснулось» за
   фиксированный горизонт; матчинг по времени суток как способ убрать
   всё сразу; «эффект» = разница средних, а не изменение формы
   распределения; горизонт H задавался удобством данных, а не теорией.

2. **Что систематически превращало результат в INCONCLUSIVE?**
   Гипотеза для проверки: наши пороги мощности заданы так, что живой,
   но умеренный эффект попадает в серую зону почти всегда. Если это
   так — назовите, в каких именно ветках это видно по числам.

3. **Чего мы не делали НИ РАЗУ за четырнадцать регистраций?** Класс
   вопросов, метод, контроль, тип данных — что-то, чьё отсутствие
   заметно только если смотреть на все спеки сразу.

4. **Какая ОДНА посылка, если её снять, открывает наибольший класс
   новых гипотез?** С обоснованием, почему именно она.

5. **Назовите 2–3 конкретные новые регистрации**, вытекающие из
   найденного: гипотеза, механизм, контроль, который её рвёт. Не общие
   пожелания — то, что можно заморозить и посчитать.

## Правила ответа

- Каждое утверждение о наших спеках — с указанием ветки. Кто читает
  файлы, читайте живые: `~/Cursor_W/strategy_engine/research/*/`
  (SPEC/LEDGER) и `~/Cursor_W/research_board/` (254 карточки
  обсуждений).
- Разделяйте «вижу в тексте спеки» и «предполагаю». Второе помечайте.
- Не щадите: цель раунда — найти, где мы себя обманывали системно.
- ≤600 слов.

## ПРИЛОЖЕНИЕ: выжимка спек (машинная, первые содержательные строки
каждой + вердиктные строки журнала)


## analog_knn_killtest
**спека:** `strategy_engine/research/analog_knn_killtest/SPEC.md`
  # PRE-REGISTRATION v1.2 — Analog-kNN kill-test (ЗАМОРОЖЕНО ДО ВЗГЛЯДА НА РЕЗУЛЬТАТЫ)
  **Неделя (заморожено):** календарная ISO-неделя, ПН 00:00:00 UTC .. ВС 23:59:59 UTC. WFA/bootstrap
  оперируют ТОЛЬКО полными неделями; частичные ведущие/хвостовые дни вне диапазона отбрасываются.
  Цель: попытаться УБИТЬ гипотезу «causal-feature аналоговая (k-NN forward-return) модель даёт
  net-of-cost OOS edge на EURUSD M15». H0 (дефолт, prior art за него): edge нет. Спека и критерии
  фиксируются ЗДЕСЬ; любое изменение = НОВАЯ регистрация. v1.2 = финальные sign-off-правки Codex+Grok:
  границы недель (ISO ПН-anchored UTC), SEALED как правило (=103 нед), DEV-gate на net(KNN−B2), B1 = диагностика (не KILL).
  ## 0. Данные (заморожены)
  - EURUSD, M15, dukascopy-node публичный datafeed, **bid OHLC + tick-volume**, UTC.
  - Файл-источник + его sha256 фиксируются в header отчёта ДО sealed-прогона.
  - Диапазон: 2020-01-01 .. 2026-04-30 (~221k баров).
  - **DEV span:** 2020-01-01 .. 2024-04-30 (для написания/дебага; сюда смотрим).
  - **SEALED (правило, не магическое число):** ВСЕ полные ISO-недели (ПН-anchored, UTC, §Неделя) с
    Monday ≥ первый ПН ≥ 2024-05-01 (= **2024-05-06**) до последней полной недели в данных (последнее ВС ≤
**исход (из журнала):**
  - **Данные (заморожены):** EURUSD M15, dukascopy-node публичный datafeed,
  - **Данные заморожены** (`data/eurusd_m15_2020_2026.csv`, gitignored — пинится хэшем):
  ### ВЕРДИКТ: KILL на DEV (2026-07-22)
  разрушать) и не меняет вердикт; остановлен ради ресурсов. Реальный результат на
  решающий и совпал с вердиктом.
  `research/analog_knn_killtest/`: SPEC.md (заморожена v1.2), LEDGER.md (этот файл),

## anomaly_phase
**спека:** `strategy_engine/research/anomaly_phase/PHASE_SPEC.md`
  # ANOMALY-PHASE v1.0 — спека (ПОДПИСАНА СТОЛОМ; заморозка = SHA-коммит)
  Подписи (2026-08-22): claude SIGNED · codex SIGNED+CONFIRM ·
  grok SIGNED-CONFIRM · deepseek SIGNED-CONFIRM · gemini SIGNED-CONFIRM ·
  kimi — отсутствовал (дневной лимит токенов, зафиксировано).
  Текст после подписей НЕ правится; заморозка наступает SHA-коммитом
  вместе с pre-freeze артефактами (§7).
  Статус: кандидат на заморозку. Ревизия по единогласному NEEDS-REVISION
  четырёх рецензентов (codex, grok, claude, deepseek; kimi — дневной лимит
  токенов, gemini — квота). Журнал правок — в конце файла.
  ## 0. Что это и чем НЕ является
  Новая регистрация ночного поглощения на оси фазы сессии. **Не наследует
  ES PASS**: p=0.0069 принадлежит старому UTC-прибору. Новый прогон
  стартует без статуса. Числа старого matchable-множества печатаются как
  **описательный benchmark** и на вердикт не влияют.
**исход (из журнала):**
  1. Календарный файл закрытий по семьям §1 (DST, ранние закрытия).
  ранние закрытия 2019–2027) · движок `run_phase.py` по спеке · 10
  названа. Худший случай: ES зима при сдвиге закрытия +15 мин — якорей
  **51 → 3**: зимние якоря сидят в первых минутах после закрытия, сдвиг
  негодными: (1) мощность мерилась sign-flip p, тогда как вердикт по спеке
  /proc/PID/stat и ложный таймаут. Всё закрыто; результаты, посчитанные

## box_cohort_phase_a
**спека:** `strategy_engine/research/box_cohort_phase_a/SPEC.md`
  # PRE-REGISTRATION v3.2 — Phase-A: Compression-Box Cohort, GBPJPY (+funnel)
  Регистрация #3, Phase-A = exploratory-but-frozen (association/PPV, НЕ симуляция
  сделок; Phase-B kill-тест = отдельная спека v3.9x на A-hold). v3.2 = v3.1 (все правки sign-off:
  Codex 1–7, Grok 1–6; §7 фаннел arr; §R) + 6 операционализаций lock-раунда
  (Codex L1–L6; Grok дал SIGN-OFF на v3.1). Версия СПЕКИ (контракта), не движка.
  ## 0. Данные
  - GBPJPY M15 bid+ask (dukascopy-node), UTC; warm-up 2019-12. M1 в Phase-A не нужен
    (исход = max-экскурсия, порядок касаний не требуется). Гейты качества reg#2.
  - Спаны по времени ЯКОРЯ: DISCOVERY [2020-01-01, 2023-07-01); INTERNAL-CHECK
    [2023-07-01, 2024-05-01) — ровно один взгляд ПОСЛЕ hash-freeze кандидата;
    A-HOLD/SEALED-3 [2024-05-06, 2026-04-27) — объявлен, не смотреть.
  - tick_size GBPJPY фиксируется в manifest. Manifest (sha256 данных+спеки+кода+
    календаря) ДО первого расчёта на discovery; ledger append-only; правка после
    взгляда = новый цикл.
**исход (из журнала):**
  замороженными знаками; CR2/Holm/LOWO/j*-hash; permutation- и time-shift-nulls;
  primary-вердикт только GBPJPY.
  ГОТОВ: 6 семейств, 290 событий, §5-аудит PASS 20/20 gold, max Δ 0.0 мин
  - [x] 3. Код-ревью втроём: 2 батча (Codex 20 + Grok 14 дефектов закрыты),
  - Протокол exploratory-прогона заморожен ДО взгляда на исходы:
  (13:00,21:00]; EIA-P3; roll-guard; правило вердикта КАНДИДАТ/НЕТ/

## momentum_precursor_pilot
**спека:** `strategy_engine/research/momentum_precursor_pilot/PILOT_SPEC.md`
  # MOMENTUM-PRECURSOR PILOT v1.0 — feasibility на XAUUSD dev (ЗАМОРОЖЕНО)
  СТАТУС: feasibility-пилот (НЕ регистрация, НЕ подтверждающий тест — окно
  дважды использовано прошлыми kill-тестами). Гипотеза arr (2026-07-27):
  большие 1–3-дневные ходы предсказываются состоянием рынка накануне
  (дневной моментум из M15 и др.). Решения arr: шкала ATR-primary + пипсы
  как sensitivity; primary = |ход| БЕЗ знака. Дизайн = консенсус стола
  (тред momentum-precursor: затравка Claude, карточки Grok и Codex; ловля
  Codex: якорь эпизода без будущего, embargo, block-null, калибровка).
  Всё ниже заморожено ДО взгляда на исходы; правки после запуска запрещены
  (новый вопрос = новый протокол).
  ## 0. Данные и окно
  - XAUUSD M15 bid+ask (killdata2 reg#2: xauusd-m15-{bid,ask}{-warmup,}.csv),
    загрузка reuse `sweep_reclaim_killtest.data.load_pair`; warm-up с
    2019-12-01. **Жёсткий срез < 2024-05-01 сразу после загрузки** — sealed

## p2hero
**спека:** `strategy_engine/research/p2hero/P2HERO_SPEC.md`
  # P2-HERO v1.0 — vol-expansion → big move, мульти-инструментный пул (ЗАМОРОЖЕНО)
  СТАТУС: exploratory-протокол (не регистрация с sealed). Герой = P2 из
  momentum-пилота (там был КОНТРОЛЕМ, z=+2.05 без прав): отношение
  ATR5/ATR20 накануне предсказывает большой ход. Решение arr 2026-07-27:
  проверить на других инструментах; таймфреймы собрать из скачанных минуток.
  Все выборы заморожены ДО взгляда; правки после запуска = новый протокол.
  ## 0. Данные (bid-only; ассоциация, не симуляция сделок — без cost-гейтов)
  E1 (дневки): EURUSD M15 yearly (killdata/yearly) 2020-01-01..2024-05-01
  (жёсткий срез ДО sealed EURUSD); LIGHTCMDUSD M15 (killdata3) 2020-01..
  2023-01; USDCHF M1 parquet (trading_gui_app/data) 2024-12-30..2026-03-30;
  AUDUSD M1 parquet 2025-02-24..2026-04-06. GBPJPY/XAUUSD/газ не участвуют
  (Phase-A, пилот, box-вердикт). Окна сжигаются как exploratory (LEDGER).
  E2 (H4): USDCHF, AUDUSD, AUDCHF, USDCAD M1 parquet — их полные спаны.
  ## 1. Бары
**исход (из журнала):**
  p_one из двустороннего _norm_sf (отчётная математика; вердикты обоих
  Спека `P2HERO_FUT_SPEC.md` заморожена до взгляда; механика v1.0
  **Вердикт по замороженной формулировке: PASS.**
  **ОГОВОРКА, которую нельзя терять при чтении вердикта.** P2 =

## pairs_coint
**спека:** `strategy_engine/research/pairs_coint/PAIRS_SPEC.md`
  # ПАРЫ-КОИНТЕГРАЦИЯ v1.0 — спека (ЗАМОРОЖЕНО 2026-08-21)
  Заморожено до написания кода движка v2 и до единого взгляда на его выход.
  Все правки после этого момента = новый протокол с новым номером.
  ## 0. Честный статус: это SECOND LOOK, не подтверждение
  Обе половины истории уже израсходованы: на первой отбирались пары, на
  второй проверялся возврат, и так четырежды — по числу перебранных
  масштабов. Третьей, нетронутой части в прошлом **не существует**; стол
  `pary-v1` указал на это прямо, и притворяться, что подтверждение
  возможно на этих же данных, нельзя.
  Поэтому регистрация фиксирует **два разных объекта**, и путать их
  запрещено:
  - **A. Second look** — прогон по исправленному дизайну на всей доступной
    истории. Даёт оценку величины эффекта, но **не даёт права на вердикт
    «подтверждено»**.
**исход (из журнала):**
  **Вердикт: INCONCLUSIVE, не FAIL.** Коинтегрированных пар всего 10, на них
  это единственный осмысленный способ довести ветку до настоящего вердикта.
  **Статус: exploratory-находка, НЕ вердикт.** Спека не была заморожена до
  ## 2026-08-22: РЕГИСТРАЦИЯ v1.0 — прогон по замороженной спеке. FAIL
  Спека `PAIRS_SPEC.md` заморожена до написания движка `run_v2.py` и до
  единого взгляда на его выход. Все пять дефектов разведки закрыты: отбор

## papa_retest_pilot
**спека:** `strategy_engine/research/papa_retest_pilot/SPEC.md`
  # PAPA-RETEST PILOT v1.0 — XAU-dev M1 (ЗАМОРОЖЕНО, консенсус стола)
  Механика Папы: пробой границы вчерашнего дня → закрепление → лимитка на
  границу → ретест → продолжение. Event-study БЕЗ PnL. Полигон: XAUUSD M1
  bid+ask [2020-01-01, 2024-05-01) (dev, многажды сожжён; sealed не трогаем).
  ## Заморозка (карточки Codex 1320 + Grok 1618; слияния помечены)
  - Сессии 17:00 America/New_York; валидна ≥900 M1-баров. Уровни PDH/PDL =
    max(mid_h)/min(mid_l) предыдущей валидной сессии.
  - Гэп: open сессии вне [PDL, PDH] → drop day (доля в отчёте).
  - ПРОБОЙ (side-aware, Codex): long = первый M1 ask-close > PDH; short =
    первый M1 bid-close < PDL. Только ПЕРВЫЙ пробой любой стороны в сессию.
  - ЗАКРЕПЛЕНИЕ: k=3 подряд M1-закрытий за границей (primary; сетка {3..6}
    report-only), глубина каждого ≥ max(1·spread_bar, 0.05·ATR14_M1);
    касание границы противоположной стороной ДО конца hold → сетапа нет
    (день закрыт).

## reg4_vol_heroes
**спека:** `strategy_engine/research/reg4_vol_heroes/SPEC.md`
  # РЕГИСТРАЦИЯ №4 v1.0 — «Три героя волатильности» (ЗАМОРОЗКА ДО ВЗГЛЯДА)
  Решение arr 2026-07-29: «регистрация №4 с тремя героями — погнали; прогони
  в т.ч. на недокаченном газе». Структура НЕобычная и декларируется честно:
  у героев НЕТ dev-фазы внутри этой регистрации — они отобраны раундами
  стола на СОЖЖЁННЫХ окнах нефти/XAU (коммиты d26beff, e5b2493, 2142d8b;
  эволюция: P2 5/5 выживаний, кросс-P2 из A-pair, gap-share = первый
  формальный выживший волны-2). Reg#4 = чистая CONFIRMATION-структура:
  замороженные герои, неминированные данные.
  ## 0. Герои (заморожены, знаки β>0, одностороннее)
  - **H1 «P2»**: ATR5/ATR20 по дневкам, значение t−1, непрерывный z.
  - **H3 «gap-share»**: gap5/(gap5+intra5), где gap=|o−c_prev|, intra=h−l,
    средние по 5 валидным дням; значение t−1, z. (Доказательства H3 были
    НА КОГОРТЕ повышенных дней; здесь тест безусловный — жёстче, честно.)
  - **H2 «кросс-P2»**: P2 соседнего инструмента (t−1), z — в ЭТОМ прогоне
**исход (из журнала):**
  закрытие предыдущего). Это не провал героя, а несуществование признака
  - **ВЕРДИКТ по замороженному правилу: НЕ ПОДТВЕРЖДЕНО; sealed S1/S2/S3

## reg5_fade
**спека:** `strategy_engine/research/reg5_fade/SPEC.md`
  # РЕГИСТРАЦИЯ №5 v1.0 FINAL — «FADE сильного сюрприза» (ЗАМОРОЖЕНО)
  Драфт: карточка 20260730-1238_claude_reg5. Sign-off: Codex SIGN-OFF
  (manifest/seed/drop-аудит — внесены); Grok «нужны правки» — ВСЕ внесены:
  1. FX-провенанс: у M1-кэшей GUI нет ask → mid невозможен; используется
     единственная серия кэша как есть (R1/R2 — отношения внутри одной
     серии, сторона консистентна). ЗАДЕКЛАРИРОВАНО как ограничение FX-клеток.
     M1→M15: бины по open-time, бар валиден при ≥12 M1-строк из 15;
     ЛЮБОЙ невалидный/отсутствующий бар в [96 баров до T0 .. T0+3ч45] →
     drop события (строго, без fill).
  2. Семейства (MQL5-ID): nonfarm-payrolls, consumer-price-index-mm,
     producer-price-index-mm, retail-sales-mm, ism-manufacturing-pmi.
     T0 — ТОЛЬКО из data-date календаря (ISM 14:00/15:00 UTC — из данных).
     Bundle: ≥2 tracked-релизов в одну UTC-минуту → событие вне primary
     (11 минут в v2). FOMC исключён.
**исход (из журнала):**
  - ПЕРВЫЙ ПРОГОН: INCONCLUSIVE по гейтам состава — в когорту попал ТОЛЬКО

## retriever
**спека:** `strategy_engine/research/retriever/gate4_v2_spec.md`
  # Гейт §4 v2 — спека, замороженная ДО написания кода
  Первая версия объявлена пройденной и **отозвана** (карточки
  `20260819-0400` и `0500`) после того, как стол показал: измерялась
  бухгалтерия детектора, а не форма. Выдав Ridge один бит — тип убранной
  вершины, восстановимый из чередования ZigZag, — мы поднимали отношение
  0.728 → 0.950 (ES) и 0.723 → 0.927 (NQ) при пороге 0.90, а на случайном
  блуждании той же волатильности получали 0.647–0.674 против 0.703–0.728
  на рынке.
  Отсюда три требования, названные столом, и они встроены в САМ гейт, а не
  пристроены проверками сбоку.
  ## 1. Конкуренты видят то же, что kNN
  Все конкуренты получают признак `kind[j]` — тип убранной вершины, — потому
  что kNN читает его даром из сбоя чередования. Конкурентов теперь три:
  - **Ridge + kind[j]** на тех же координатах;

## sprt_retro
**спека:** `strategy_engine/research/sprt_retro/spec.md`
  # Ретро-SPRT: спека, записанная ДО расчёта
  **Идея** — Джемини, раунд `ветки-v1`: жёсткий гейт «N ≥ 500» тратит данные
  впустую. Последовательный критерий отношения правдоподобия (Вальд) умеет
  останавливаться раньше, как только правдоподобие накопилось.
  **Опровержение, объявленное автором идеи:** ретро-применить к веткам,
  закрытым как INCONCLUSIVE. Если ни одна не останавливается раньше
  фактического N — выигрыша нет, идея снимается.
  ## Параметры, замороженные до прогона
  Границы Вальда: принять H1 при `llr ≥ log((1−β)/α)`, принять H0 (KILL)
  при `llr ≤ log(β/(1−α))`. Первый выход за границу — точка остановки.
  ## Две части, они НЕ равноценны
  **А. Настоящее ретро** — там, где сохранилась хронология эпизодов.
  Единственная такая ветка — `vpoc` (`events_real.parquet`, 284 события с
  метками времени). Это факт: SPRT прогоняется по реальному порядку.

## sweep_reclaim_killtest
**спека:** `strategy_engine/research/sweep_reclaim_killtest/SPEC.md`
  # PRE-REGISTRATION v2.2 — Sweep-Reclaim @ Prior-Day H/L, XAUUSD (kill-test)
  H1: «после liquidity-sweep prior-day-экстремума и быстрого reclaim направление
  reclaim имеет положительный net edge при структурном тейке к противоположному краю
  prior-day диапазона». H0: edge нет. Регистрация №2. ОДНА ячейка. v2.2 = драфт +
  все замены Codex(1-28) и Grok(1-18) из sign-off раунда, конфликты разрешены (§R).
  ## 0. Данные
  - XAUUSD, dukascopy-node, bid И ask: M15 + M1, UTC. **Warm-up 2019-12-01..31**
    (только для уровней/ATR; сделок/статистики до 2020-01-01 нет).
  - Сплиты по UTC-времени ВХОДА: **DEV = [2020-01-01, 2024-05-01)**; буфер
    2024-05-01..05-05 ни в одной оценке; **SEALED = [2024-05-06, 2026-04-27)**.
  - Бар с timestamp t покрывает [t, t+Δ), его OHLC доступен в t+Δ. «Следующий M1» =
    следующая календарная минута с баром.
  - Качество: уникальные синхронные bid/ask timestamps; ask.close<bid.close → бар
    бракован; пропуск M1 между sweep и вертикальным выходом → кандидат DATA_INVALID
**исход (из журнала):**
  - [x] 5. Placebo → DEV → **ВЕРДИКТ: INCONCLUSIVE-KILL** (см. ниже)
  ### ВЕРДИКТ: INCONCLUSIVE-KILL на DEV (2026-07-23, dev_report.json)
  **Формально:** N=299 qualifying < 500 (гейт §8.4) → INCONCLUSIVE-KILL, ветка
  закрыта БЕЗ расширения сетки (замороженное правило).
  crit1/2/3 провалены все. При N≥500 вердикт был бы KILL.
  Регистрация #2 закрыта (INCONCLUSIVE-KILL на dev, 2026-07-23); её sealed

## vol_freshness
**спека:** `strategy_engine/research/vol_freshness/SPEC.md`
  # VOL-FRESHNESS v1.0 — гипотеза Claude (ЗАМОРОЖЕНО до взгляда)
  АВТОР ГИПОТЕЗЫ: Claude (по приглашению arr, 2026-07-29). Exploratory на
  СОЖЖЁННЫХ окнах; выживет → регистрация на свежем пуле после газа.
  **Гипотеза**: эффект vol-expansion (P2=ATR5/ATR20 ↑ → большой |ход| завтра;
  P2-hero E1 z=+4.4) концентрируется в СВЕЖЕМ расширении — первые 1–2 дня
  серии повышенных дней; у застарелого (3+ дней подряд) вероятность события
  откатывается к базе. Механизм: кластеризация волы сильнее всего сразу
  после информационного шока; старое расширение = уже отторгованные новости.
  ## Данные (сожжены ранее)
  Дневки 17:00 NY, bid, механика p2hero: LIGHTCMDUSD M15 2020-01..2023-01;
  XAUUSD M15 2020-01..2024-05-01 (срез ДО sealed). Газ НЕ трогаем (ждёт
  box-вердикта), FX-кэши коротки для дневного условия.
  ## Определения (все PIT: P2_t из ATR*_{t−1})
  - Событие: |c_t − c_{t−1}| ≥ 1.5·ATR20_{t−1}, первое пересечение, embargo 1

## vwap_touch
**спека:** `strategy_engine/research/vwap_touch/VWAP_SPEC.md`
  # VWAP как уровень — спека v1.0 (ЗАМОРОЖЕНО ДО ВЗГЛЯДА НА ИСХОДЫ)
  Дата заморозки: 2026-08-21. Статус: **exploratory**, окна сжигаются.
  Разведка (только частоты) сделана до спеки: `results/recon.json`.
  ## 0. Гипотеза в проверяемом виде
  Из подборок: «институциональные деньги работают по средневзвешенной
  цене, поэтому касание VWAP — точка входа по тренду». Проверяемая
  формулировка:
  > После того как цена отдалилась от VWAP и вернулась к нему, она чаще
  > **отскакивает** (уходит обратно в сторону, откуда пришла), чем
  > проходит уровень насквозь.
  Ожидание направленное: доля отскоков выше, чем у контрольного уровня.
  ## 1. Данные и бары
  Минутки из банка, 22 инструмента с объёмом. Сессия — 17:00 America/New_York
  (как во всех наших дневных расчётах); сессия валидна при ≥900 минутных
**исход (из журнала):**
  Спека `VWAP_SPEC.md` заморожена до взгляда на исходы; разведка
  **Вердикт по замороженной §6: INCONCLUSIVE** — разница положительна, но
  **Что стоит отметить помимо вердикта.**

---

*(Приложение 2 — оригинальные реплики arr, с которых начиналась каждая
ветка, — лежит рядом: `ПРИЛОЖЕНИЕ-2-ОРИГИНАЛЫ-ARR.md`. Оно будет подано
отдельно, в витке критики: сначала вы смотрите на спеки, потом на то,
чем эти спеки были до формализации.)*
