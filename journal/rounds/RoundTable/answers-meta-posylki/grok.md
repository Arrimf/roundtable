Читаю полный промпт и живые спеки — роль фактчекера, `eyes: files`.Собираю по живым SPEC/LEDGER: вердикты, горизонты, бинарные исходы, матчинг и пороги мощности.РОЛЬ: фактчекер. `eyes: files`

**Сначала про счёт.** В живых журналах он не совпадает с преамбулой.

- `pairs_coint` v1.0 (`LEDGER.md` 2026-08-22): **FAIL**, не INCONCLUSIVE. Часовки +6.5 ч vs порог 8 ч, CI накрывает 0; 15м взяли 8 ч, но **§6.2 variance-ratio** съёл эффект до +2.0 ч.
- `reg4_vol_heroes`: гейты **пройдены** (N_valid=722, events=31≥25). Вердикт **НЕ ПОДТВЕРЖДЕНО** (H1 Holm-p=0.16, z=1.40 < null q95=1.74), не INCONCLUSIVE.
- `p2hero` PASS — **exploratory** (`P2HERO_FUT_SPEC.md`: «не регистрация с sealed»).
- `sweep_reclaim`: ярлык INCONCLUSIVE-KILL (N=299<500), числа — KILL (θ=−0.149 R; при N≥500 был бы KILL).
- `vpoc` #2: INCONCLUSIVE по гейту 279∈[150,400), Δ=**−0.85 пп** — при N=400 это FAIL.
- «18 проб, 2 выживших» — **не** `momentum_precursor` (там 4 прекурсора, вердикт «СИГНАЛА НЕТ»). Это волны `hypo_duel` (карточка `20260730-1207`: N2/G2). Пилот: 43 события, герои мертвы; ожил только **контроль P2** (z=+2.05).

ES PASS: реплики s=−0.1189, p_one=0.0019 (`20260813-0130`); **p=0.0069** — эмпирический хвост 290 негативных контролей (`negcontrol_es_balanced.json`, s=−0.1008). Оба в файлах.

---

**1. Посылки почти во всех текстах, ни разу не проверялись как объект**

**Вижу в тексте.** (а) Исход — бинарный first-passage / resolved-only за календарный H: `vwap_touch` (отскок vs пробой до конца сессии, цензура *исключается*), `vpoc` (480 мин, TIMEOUT вне primary), `anomaly_quiet1` (H=30 мин, freeze/touch), `papa_retest` (1R vs fail к следующей сессии), `sweep_reclaim` (гонка до min(t+480, 20:45 UTC)), `box_cohort` (Y=1[MFE≥7s] к 21:00 UTC). Исключения: `analog_knn` (непрерывный return, h=4 бара), `p2hero`/`reg4`/`vol_freshness` (бинарный *день*, не касание). (б) Эффект = Δ средних/долей/`β` логита — везде primary; формы распределения нет ни в одной спеке. (в) H не из теории удержания: knn h=4; quiet 30 мин; vpoc/sweep 480 мин; box 07–21; pairs RMTT cap 192 ч; p2hero = завтра. (г) День = 17:00 NY: momentum, p2hero, vol_freshness, papa, vwap, reg4. Не все: knn — ISO-неделя UTC; box — якорь 07:00; quiet — бакеты 21/07/13.

**Подозреваю, не «все».** Матчинг TOD как «убрать всё» — **не универсален**. Он primary в `anomaly_quiet1`/`confirm` (TOD±15) и частично в `sweep` (UTC-час+DoW+терцили). `vpoc` матчинга **нет** (raw vs U-level). `vwap` — парная разность VWAP−TWAP. `pairs` — variance ratio уровня.

---

**2. Что делало INCONCLUSIVE**

Гипотеза «умеренный эффект → серая зона» **верна выборочно**.

Серая зона живого/умеренного: `vwap` +4.0 пп при пороге **5** (z=+1.55); NG-confirm s=−0.113, p=0.172, N_res=21; реплика 6E −3.85 пп, p=0.065; `reg5` q_fade=0.579 vs 0.60 **и** cohort 38<50; `papa` fills=183∈[80,199], при этом P(≥1R)=0.443 < null 0.5; `wrb_nr` N_res 23–27 vs powered 30.

**Не** серая зона: `vpoc`/`sweep` — INCONCLUSIVE-ярлык на нуле/минусе; `box` gas — 0 случаев Y≥7s (хвост слишком редкий); `vol_freshness` events_elev=15, знак против (fresh 3.8% < stale 6.3%); `reg4` — мощность по спеке была.

Порог «+5 пп / N≥400–500» **систематически** съедает +3…+4 пп (`vwap`, pairs exploratory +4.9%). Это конструкция, не рынок.

---

**3. Ни разу за четырнадцать**

- Primary = **форма/квантиль/хвост**, не среднее.
- **Знак** хода как гипотеза после смерти `analog_knn` (остальное — |ход| или отскок).
- Проверка **самого матчинга** (съедает ли TOD сезонный эффект).
- L2/TBBO как доведённый вердикт (Hawkes FROZEN, holdout сдвинут, Stage-1 TDA **FAIL**).
- Volume-time; совместные несколько H; издержки как co-primary (кроме knn/sweep).

---

**4. Одна посылка**

Снять **«эффект = Δ долей/средних на бинарном resolved-исходе за удобный календарный H».**

Она связывает слепоту прибора: цензура conditioning on resolution (`vwap` LEDGER: абсолютные 82% нечитаемы), H без теории, 5пп-ворота, и невозможность увидеть интенсивность/время-до/величину. `pairs` уже показал: смена измерителя (доля → RMTT) **меняет картину**. `box` k=7 обнулил Y. Два PASS это не опровергают: p2hero — ARCH-тавтология на |ходе|; quiet — редкий случай «где/когда», всё ещё бинарный freeze@30м.

---

**5. Три регистрации**

**A. VWAP-RMTT.** H1: после возврата к VWAP *время* до +0.5 ATR-отскока короче, чем у TWAP, на ≥X мин (X заморозить pre-freeze квантилем издержек), даже если доли близки. Контроль-разрыв: уравнять остаточный диапазон сессии; если ΔRMTT умирает — это не объём, а «сколько дня осталось».

**B. P2×знак.** H1: P2 предсказывает |ход| (уже PASS) и **не** предсказывает sign(close_t−close_{t−1}). Kill: shuffle знаков при фиксированных |ходах| — любой signed-α обязан умереть; иначе «кластеризация волы» была дырой.

**C. Overcontrol TOD на ES.** Три замороженных эстиманда A|NIGHT|T0: (i) нынешний TOD±15, (ii) только бакет NIGHT без TOD, (iii) unmatched vs все ночные бары. H1: (i) жив **и** (ii)−(i) ≈ 0. Kill: если (i)≈0, а (ii)/(iii) велики — «аномалия» была часами, матчинг её съел.