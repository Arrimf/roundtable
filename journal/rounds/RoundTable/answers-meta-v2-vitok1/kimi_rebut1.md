• Проверю исправленные артефакты v2 на диске перед ответом.

• Проверю ещё три детали: статус E1, отчёт `p2hero_fut`, и мощность ES при se из p=0.00195.

• Все опорные числа витка проверил по диску. Отвечаю в роли.

  **РОЛЬ: фактчекер — остаюсь.**

  **Верификация v2.** Вижу: `research/mde_retro/results/mde_retro_v2.json` — медиана 0.534, ниже 50% ровно 4 из 11 (пересчитал сам) ✓; `run_mde2.py:45` — формула `1−Φ(1.6449 − MDE/se)`, односторонняя; `p2hero/results/p2hero_report.json` — E1: se=0.1118, verdict INCONCLUSIVE; E2: se=0.1137, β=−0.0008 ✓. Три находки против таблицы v2 — ниже.

  **Фокус витка.**

  (1) **Порядок возврата — меняю.** В слепой фазе моя тройка была ES / reg4 / vwap. При 67% и 62% мощности слабые исходы reg4 и vwap — это ответы, а не непроверенность; возвращать их нет оснований. Остаются четыре. Первой — `anomaly_quiet_es`: на весах единственный живой PASS, а прибор имел мощность 39% (и ниже — см. находку 1): цена ошибки в объявленном вердикте максимальна, N=127 дёшев. Второй — `reg5_fade` (0.579 при 0.60, N=38, события дешёвые). `papa_retest` и `wrb_nr` — в хвост: у papa знак точечной оценки против (0.443<0.50), добор мощности лечит разрешение, а не знак. «Что перестроить в первую очередь» не меняется: блок «мощность до заморозки», посчитанный тем же кодом, что выносит вердикт.

  (2) **«Смешанно» принимаю, но v2 завышен в трёх точках — все проверены:**
  - **ES: se=0.0369 недокументирован и несостоятелен.** Из `replica_es.json`: s=−0.1189, p_one=0.00195 → z=2.886 → se=0.0412. Нулевой se знакого теста при n_res=149: √(0.25/149)=0.0410. Два независимых пути дают ≈0.041 → мощность **33%, не 39%**. А 0.0369 в точности равен √(0.25/183) — это se ветки `papa_retest`. Подозреваю копипасту в `registry.py:84`.
  - **MDE_LOGIT=0.40 назначен постфактум** (`registry.py:96`) и почти равен наблюдённому β (E1 0.490, пул 0.478). «97%» у p2hero — во многом пересказ увиденного эффекта; это retrospective power, претензия В (виток-1, голос В) снята не полностью.
  - **«Живой se» у reg5/vpoc/wrb/papa — не живой**: это null-модельный √(p(1−p)/n), а не отчётная ошибка ветки. Мелочь для prop при базе 0.5, но слово «живой» неточно.

  (3) **Discovery-контур, механика первого шага на примере ES.** Замораживается до первого взгляда: (а) hash-сплит: dev-годы 2020–2022 против нетронутого confirm-окна — как sealed в reg#2; (б) словарь допустимых операционализаций: что такое quiet-episode, диапазон k, статистика s — фиксируется *пространство поиска*, не гипотеза; (в) бюджет взглядов: каждый запрос к dev — append-запись в LEDGER со счётчиком, долг множественности копится; (г) promotion-гейт: из разведки выходит ровно одна ячейка с estimand, MDE, обоснованным ДО confirm-окна, и расчётом мощности впрыском в вердиктный конвейер (`anomaly_quiet1/poscontrol.py` уже умеет). Подгонкой это не станет, потому что выход контура — заявка на заморозку, а не вердикт.

  **Пять пунктов.**

  1. **Изменил:** порядок возврата (было ES/reg4/vwap → стало ES/reg5/…): reg4 и vwap разрешающие, их INCONCLUSIVE содержателен. **Не изменил:** требование «мощность считает вердиктный код» — v2 его усилил.
  2. **Принимаю:** Голос А — se E1≈E2 → E2 хорошо разрешённый ноль; проверил: 0.1118 против 0.1137, v2 подтвердил. Голос Б — «единица ветка не определена»: исключения box/momentum/vol_freshness из медианы тоже post hoc, хоть и обоснованы.
  3. **Противоречие:** «E2 — настоящий отрицательный результат» — преждевременно. В том же `p2hero_report.json` у E2 `leak_suspect: true` и `block_null.pass: false` (z по δ=+1…+4: 8–11). Ноль при собственном флаге утечки — ноль прибора, который сам себя не прошёл. И в `registry.py:35` E1 по-прежнему подписан «PASS (exploratory)», хотя verdict в файле — INCONCLUSIVE; PASS принадлежит `p2hero_fut.json` (z=9.59, 394 кластера), которого в таблице нет. Замечание А витка-1 в v2 не исправлено.
  4. **Упустили все:** формула v2 односторонняя (z=1.6449) для всех строк, но вердикт ES несёт *эмпирическое* p=0.0069 из `negcontrol_es_balanced.json`, а не z — мощность считается не для того теста, которым вынесен PASS.
  5. **СВОЯ ИДЕЯ:** к каждому вердикту обязательное число **MDE80** — эффект, который тест ловил с мощностью 80%: MDE80 = (z_α+z_0.8)·se. У ES это (1.6449+0.8416)·0.041 ≈ **0.10 при объявленном пороге 0.05** — прибор в принципе не мог подтвердить собственный порог. Одна строка в вердикте, и «приборная резолюция» читается сразу, без ретро-раундов.