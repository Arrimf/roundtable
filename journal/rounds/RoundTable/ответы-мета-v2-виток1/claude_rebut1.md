РОЛЬ: фальсификатор (сохраняю) · eyes: files

**Первой строкой:** v2 выдерживает paired-проверку — на общих 11 строках медиана v1 38% → v2 53%, рост не композиционный, — но её самая важная строка, единственный PASS, стоит на se, которого нет ни в одном артефакте.

**ФОКУС ВИТКА**

(1) Порядок меняю: не «четыре возвращаются», а «одна перепроверяется, две возвращаются, одна ждёт». Первой — `anomaly_quiet_es`, не как гипотеза, а как репликация PASS: единственный объявленный вердикт, снятый слепым прибором, и прибор слабее, чем в таблице (см. 2). Второй — `reg5_fade`: единственная из четырёх со знаком ЗА (0.579 при базе 0.50, порог 0.60; `registry.py:25`), события дешёвые. Третьей — **`reg4`, которого в четвёрке нет**: вижу в `reg4_vol_heroes/SPEC.md:60-62` критерий «ПОДТВЕРЖДЕНО ⇔ β>0 ∧ Holm-p<0.05» — чистая значимость, порога эффекта спека не замораживала; «67%» посчитаны на +2 пп, которые в `registry.py:28-30` стоят без ссылки на источник. `wrb_nr` слепая (27%), но знак смешанный: primary s = −0.021 ES, +0.100 NQ, −0.073 6E (`wrb_nr/results/explore_*.json`) — возвращать только бесплатными событиями. `papa` — знак ПРОТИВ (0.443 < 0.50) — в конец.

(2) «Смешанно» принимаю (в слепой фазе я это и предсказывал), с двумя поправками в разные стороны. **Вверх завышена строка ES.** se=0.0369 (`registry.py:84`, note «s=−0.1189 при p=0.0019») не воспроизводится из своего источника: из p_one=0.00195 (`replica_es.json`) выходит 0.0412 → мощность 33%; из вердиктного p_empirical=0.0069 → 0.0483 → **27%**; из s_sd нулевого распределения (`negcontrol_es_balanced.json`) 0.0383 → 37%. Зато 0.0369 совпадает до четвёртого знака с se `papa` под H0: √(0.25/183)=0.0370. **Предполагаю** копипаст между строками LIVE. Там же вижу ks_stat=0.0951 > ks_crit=0.0799 — нормальность нулевого распределения s отвергнута самой веткой, а v2 считает мощность через Φ. **Вниз занижена медиана.** MDE 0.40 — не произвол: logit(base+0.02)−logit(base) = 0.403 для reg4, 0.392 для E2, 0.529 для E1 (E1 → 99.9%). А `momentum` удалён вместо пересчёта: по `pilot_report.json` (n=1074, events=43, base=0.040) на +2 пп выходит ~96%, с ним медиана ~57%. Итог: нули E2 и momentum — настоящие, reg4 не проверен, ES слабее таблицы.

(3) Механика на `reg5_fade` — три заморозки, ни одна не требует гипотезы: (а) календарный разрез: до даты D всё сожжено (38 событий), после D события копятся в подтверждающий пул, который разведка не открывает; (б) словарь: ≤5 разрезов (тип релиза, знак сюрприза, величина, сессия, инструмент) и один исход q_fade — разрез вне списка = новая запись в журнал; (в) бюджет взглядов: 10 запросов, sha каждого пишется в LEDGER ДО выполнения, одиннадцатый невозможен. Выход — не вердикт, а заявка: разрез, SESOI, N_req и дата, когда подтверждающий пул его наберёт.

**1. Изменил** порядок возврата и отношение к v2: paired-проверку прошла, проверку по строке ES — нет. Не изменил: E2 — хорошо разрешённый ноль (se E1 0.1118 = se E2 0.1137, `p2hero_report.json`). Поправка себе: в слепой фазе я приписал wrb 18% — это pairs, у wrb было 31%.

**2. Принимаю** Голос Б: единица «ветка» не заморожена, mde_retro сам — продукт гибкого отбора. v2 подтвердила: 14→11 строк, дефект momentum «исправлен» удалением (`registry.py:90` пишет «считать надо её» — и не считает). И Голос Д: тест мерит согласованность ветки с собой — у reg4 не оказалось даже «себя».

**3. Противоречие** с Голосом Г: возврат `p2hero` опровергнут. Вижу: beta E2 = −0.0008 при se 0.1137 — верхняя односторонняя 95%-граница β<0.19, эффект больше +0.9 пп исключён; возвращать нечего. «Мощность 31% в P2HERO_FUT_SPEC.md» фактически неверно — число из `mde_retro.json` v1, спека мощности не содержит. С фокусом: «4 из 11 → возвращать четыре» — бинаризация по 50% повторяет болезнь бинарных порогов: vpoc 51% и ES 39% не по разные стороны чего-либо содержательного.

**4. Упустили все:** (а) невоспроизводимость se ES и совпадение с чужой строкой — реестр, докстринг которого предупреждает о «выдуманных числах», сам их содержит; (б) v2 сняла кластерное допущение, но не нормальность, а у единственного PASS нормальность отвергнута KS-тестом самой ветки — Голос Д видел p_theoretical/p_empirical «вдвое», но не связал с формулой v2; (в) `registry.py:35` по-прежнему зовёт E1 «PASS (exploratory)» при «INCONCLUSIVE» в `p2hero_report.json`.

**СВОЯ ИДЕЯ:** для веток без замороженного SESOI (reg4, p2hero, momentum — правила только по значимости) ретро-мощность не определена по построению; вместо неё колонка «что тест ИСКЛЮЧАЕТ» — верхняя граница CI: E2 исключает β≥0.19 (+0.9 пп) → ноль; reg4 не исключает ничего ниже β=0.59 (+3 пп) → не проверена. Это число без назначенного MDE — его нельзя ни назначить, ни подогнать.