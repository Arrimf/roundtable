РОЛЬ: синтезатор-ревизионист.

**Discovery, первый шаг.** Замораживать до гипотезы надо не гипотезу, а четыре контура: (i) пространство поиска — словарь признаков, исходов, масштабов; (ii) хэш-сплит discovery/unseen, непересекающиеся окна; (iii) look-бюджет — число переборов семейств, append-only журнал; (iv) promotion-гейт. Гипотеза остаётся выходом. Пример `anomaly_quiet_es`: до первого взгляда фиксирую исход excursion, сетки «quiet» и горизонта, максимум 8 трансформаций; открываю `discovery_ledger.json` до запуска; изменение словаря = новое окно. Подтверждение — только на невидимом split, иначе PASS подтверждён не будет.

**1. Изменил мнение.** Да. Главное: p2hero_E2 я ранее считал «артефактом n_eff»; исправленная таблица даёт 97% и beta=−0.0008 — это настоящий ноль, моё прежнее сомнение снимаю. Перестроить первым делом теперь нужно не весь конвейер, а трек донабора: реальный дефицит мощности у четырёх веток, остальные либо разрешены, либо слабо-надпороговые. Возврат: первой `anomaly_quiet_es` — единственный живой PASS при мощности 39%, порог −0.05 против pooled −0.0445 (`anomaly_quiet1/`, `negcontrol_es_balanced.json`). Вторая `reg5_fade` — 34%, N=38, q=0.579 при пороге 0.60; добор дёшев. Третья `papa_retest` — 39%, N=183, но с пересмотром MDE. `wrb_nr` — не как есть: 27% при N=27; нужен редизайн с расширенным пулом.

**2. Принимаю.** Голос Д: мощность считалась на порогах, которые ветки назначили сами, поэтому v2 — верхняя оценка согласованности с собой, а не prospective-качество. Это распространяю на MDE «0.40 беты»: назначен автором, не спеками, поэтому 34%/39% могут быть ещё ниже. Вывод «смешанно» принимаю; «приборы слепы» больше не защищаю.

**3. Противоречие.** «Возвращать только четыре» не означает «остальные проверены». `vwap` 62% и `vpoc` 51% — не разрешение, а слабо-надпороговая монета; я бы закрыл их до нового механизма, но не записал в «проверено». И не согласен с любой очередью, ставящей vwap выше anomaly: статусная ставка разная — текущий PASS дороже лежачего INCONCLUSIVE.

**4. Упустили.** Асимметрию: настоящие нули получились при 97% мощности, а единственный цитируемый PASS — при 39%. Риск не в доле маломощных веток, а в том, что самый публичный положительный результат несёт самая слабая процедура. Конвейер должен эту асимметрию считать явно.

**5. СВОЯ ИДЕЯ:** Очередь возврата считать по «стоимости удержания статуса»: неопределённость PASS активна и дорога; INCONCLUSIVE/OPEN тихо лежит. Поэтому anomaly первая, даже если чистая арифметика мощности ставит выше papa или reg5.