РОЛЬ: скептик-фактчекер

1. **Изменил мнение.** Отказываюсь от диагноза «приборы в основном слепы»: корректнее **«смешанно»**, но точные 53% и 4/11 пока не принимаю. Перестраивать первым надо контракт мощности: SESOI с источником → точный вердиктный код → инъекция эффекта. Возврат: (1) `anomaly_quiet_es` — независимая репликация наиболее важна для слабомощного PASS; (2) `reg5_fade` — положительная оценка 0.579 и уже выделенный prospective S1 ([SPEC.md](~/Cursor_W/strategy_engine/research/reg5_fade/SPEC.md:16)); (3) WRB — только после исправления строки; (4) `papa_retest` фактически последним: 0.443 против требуемых 0.55 — знак и величина уже против H1 ([pilot_report.json](~/Cursor_W/strategy_engine/research/papa_retest_pilot/results/pilot_report.json)).

Для discovery `reg5`: первым коммитом заморозить границу burned-DEV/недоступный S1, словарь допустимых входов и исходов, максимум 20 outcome-взглядов и append-only журнал запросов. На DEV разрешить менять estimand; кандидат выходит только как новая формула плюс power-кривая, а S1 до этого не открывается.

2. **Принимаю довод голоса А:** нужны две мощности — реальной процедуры и идеального теста на тех же данных. V2 приблизил вторую, но не первую: [run_mde2.py](~/Cursor_W/strategy_engine/research/mde_retro/run_mde2.py:43) везде подставляет нормальную формулу, тогда как ES решается эмпирическим null, `reg5` — составными гейтами и permutation, WRB — sign-flip плюс BH.

3. **Оставшееся противоречие.** Не согласен с вводной «возвращать имеет смысл только четыре»: низкая мощность — не критерий возврата, а `papa` уже движется против SESOI. «Смешанно» принимаю качественно; 53% — лишь скрининг. Единый назначенный `β=0.40` не является замороженным MDE ([registry.py](~/Cursor_W/strategy_engine/research/mde_retro/registry.py:94)); нужна кривая по β либо перевод исходного порога доли через baseline каждой ветки.

4. **Что мы все упустили.** Строка WRB в v2 фактически неверна: `se=0.0962` взята из **NG, n=7, OUT-OF-FAMILY** ([explore_ng.json](~/Cursor_W/strategy_engine/research/wrb_nr/results/explore_ng.json:108)), хотя строка объявлена для N=27. Более того, primary WRB — непрерывная средняя `y_exp` со sign-flip, не доля ([run_explore.py](~/Cursor_W/strategy_engine/research/wrb_nr/run_explore.py:41)). Поэтому пока не доказаны ни 27%, ни «ровно четыре».

5. **СВОЯ ИДЕЯ:** выдавать discovery не сырые исходы, а ограниченные «токены взгляда»: каждый токен открывает одну заранее хэшированную проекцию данных и безвозвратно записывается. Это делает бюджет взглядов техническим ограничением, а не обещанием исследователя.