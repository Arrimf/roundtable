Сначала прочитаю полный промпт и сверю исправленную таблицу мощности по живым файлам.Нашёл `mde_retro` в Cursor_W — читаю v2-результаты и чужие ответы витка.Сверяю живые SE и ключевые артефакты четырёх слабомощных веток.Ещё одна сверка: источник SE у anomaly и статус wrb — затем ответ.**РОЛЬ: фактчекер. `eyes: files`**  
(слепого ответа нет — путь свой, по диску)

**Вывод:** «смешанно» по направлению верно, но **всё ещё завышено**. v2 чинит четыре дефекта v1, однако сама таблица v2 несёт минимум один битый `se_live` и **назначенный** MDE логит-веток.

### Фокус витка

**Вижу** (`research/mde_retro/results/mde_retro_v2.json`): медиана ρ=1 = **0.534**, ниже 50% — **4/11**: `wrb_nr` 27%, `reg5_fade` 34%, `papa_retest`≈39%, `anomaly_quiet_es`≈39%. `p2hero_E2`: `beta_p2=−0.0008`, `se_p2=0.1137`, мощность ≈97% (`p2hero/results/p2hero_report.json`) — настоящий ноль. `LEDGER.md` mde_retro это фиксирует.

**(1) Что перестроить / кого вернуть.**  
Приоритет конвейера **не меняю**: по-прежнему нет discovery-контура (только confirmation). Меняется **очередь возврата**. Критерий: цена уже объявленного статуса × мощность<50% × дешевизна добора.

1. **`anomaly_quiet_es`** — единственный PASS на приборе ~39% (`mde_retro_v2.json`; `negcontrol_es_balanced.json` p_emp=0.00687).  
2. **`reg5_fade`** — q=0.579 при цели 0.60, N=38 (`confirm_report.json`); события дешёвые.  
3. **`papa_retest`** — P(≥1R)=0.443, fills=183 (`pilot_report.json`).  
`wrb_nr` — четвёртым: сам пишет `NOT-POWERED` (`wrb_nr/LEDGER.json`).  
`vwap`/`vpoc`/`reg4` при v2 уже ≥50% — это другая очередь (разрешённые inconclusive/near-miss), не «непроверенные четыре».

**(2) «Смешанно».** Принимаю **как скрининг**, не как вердикт. Чем завышен:

- **`MDE_LOGIT=0.40`** — только `registry.py:96`, в `reg4`/`p2hero` SPEC этого порога **нет**. При MDE=наблюдённой β=+0.27 мощность reg4 падает ≈**40%** (пересчёт `1−Φ(1.645−0.27/0.1929)`), и «разрешающий» статус зыбок.  
- **`anomaly_quiet_es.se_live=0.0369`** бит-в-бит как у `papa_retest` (`registry.py:73,84`). У papa это √(0.25/183). У ES из `replica_es.json` (s=−0.1189, p_one=0.00195) следует se≈**0.0412** → мощность ≈**33%**, не 39%. Плюс registry пишет N≈127, а файл — n=171 / n_resolved=149.  
- Формула по se — всё ещё не впрыск в вердиктный код (`run_mde2.py` сам: «screening_only»).

**(3) Discovery — первый шаг на `reg5_fade`.**  
До любого взгляда на исходы заморозить **не гипотезу**, а:

1. **сплит релизов**: hash-список будущих surprises, не пересекающийся с cohort=38;  
2. **словарь семейств** уже из SPEC (ISM/PPI/RETAIL — как в `confirm_report.json`);  
3. **бюджет взглядов K≤3** estimand’ов, каждая строка в `research/ПАСПОРТ-СЕЛЕКЦИИ.md` с меткой ДО;  
4. **выход** = заявка (estimand + MDE из экономики издержек + N_req), не PASS/FAIL.

### Вокруг затравки (кратко)

1. Медиана **39%** — брак; v2 устойчива к ρ, **не** к назначенному MDE и битому se.  
2. Чинить: discovery + паспорт + мощность тем же кодом, что вердикт.  
4. `§power` с источником каждого числа; при недостижимом N — monitoring/SPRT/редизайн, не «подождать 500» у sweep с θ=−0.149R.  
5. **Не видим:** кто и по какому правилу выбирает SESOI, когда SPEC молчит — иначе mde_retro меряет согласованность с дирижёром.

### Расхождения

- С возвратом `vwap`/`vpoc` первыми по v1-мощности: при v2 они **не** в четвёрке слепых.  
- С «ни одна из 26 не считала мощность»: **ложно** — `wrb_nr` NOT-POWERED; `anomaly_phase/PHASE_SPEC` требует pre-freeze poscontrol (реализация была негодной).

### СВОЯ ИДЕЯ

Каждому `se_live`/`mde` в registry — **указатель `path#json_key` + sha значения**, не голый float. Пара `papa`/`anomaly` с одним `0.0369` тогда невозможна: ломается сверка источника. Назначенный MDE без такого указателя помечается `assumed` и **не** входит в медиану «приборов».