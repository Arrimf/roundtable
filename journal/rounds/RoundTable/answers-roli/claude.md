Слепая фаза почти закрыта: Codex (37 с), Grok (47 с), Kimi (52 с), Gemini (82 с) ответили — ждём только голос `claude`. В журнал ответы попадут разом, из карантина.

План записан в `~/.claude/plans/eager-humming-magpie.md`. Коротко:

**1. Довести слепую фазу** — дождаться `claude`, проверить, что все пять записей перелились в `room.jsonl`. Упавших не перезапускать поодиночке (правило 4).

**2. Витки критики** — `rebut` повторять до всеобщего ПАС, но не больше **трёх витков**. Это мой собственный предел до того, как стол ответит на вопрос 2 затравки; несошедшийся спор идёт в свод отдельным разделом, а не «дожимается».

**3. Свод пишет Codex**, не я — `summarize --by codex`. Пункт В затравки предлагает ротацию именно против перекоса в мою сторону; первый же свод за моим авторством обессмыслил бы проверку. Свою сводку напишу отдельным файлом и после, как реплику участника.

**4. В устав** — правила 11+, переносит Автор, я готовлю формулировки.

**5. Четыре дыры в `choir.py`**, найденные чтением кода, чинить **после** свода — стол может назвать их сам:

- `choir.py:447` — метки А/Б/В назначаются по порядку завершения вызовов, поэтому **«Голос А» в `rebut1` и в `rebut2` — разные модели**. Голос, принявший «довод Голоса Б», через виток ссылается не на того. Того же рода дыра, что нашёл Grok в прошлом раунде: свойство объявлено полем в журнале, но не обеспечено механикой.
- `choir.py:434` — сказавший ПАС **выбывает из спора навсегда**: следующий виток берёт только `status == "ok"`. «Спор до всеобщего ПАС» на деле работает как выбывание по одному.
- `choir.py:600` — `summarize` подмешивает ролевую преамбулу: свод, написанный «адвокатом дьявола», уже не свод.
- `choir.py:233` — `РОЛЬ:` ловится в любом месте текста, а затравка `roles-v1` сама содержит примеры такой строки. Голос процитирует пример — дирижёр запишет цитату как объявление роли.

Скажите, если менять что-то в порядке или в выборе сводящего (альтернатива Codex — Grok), — иначе по получении ответа `claude` веду раунд дальше по этому плану.