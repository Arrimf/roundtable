• Сначала проверю ключевые файлы на диске, на которые опирается раунд.

• Проверю, есть ли артефакты исследования рядом с рабочей директорией.

• Репозитория исследования на этом диске не нахожу. Гляну свой ответ первого витка для преемственности.

• Таблица на диске совпадает с затравкой. Теперь методика симуляции.

• Методику вижу. Проверю ещё три опорных факта затравки.

• Все опорные факты затравки проверил по диску. Отвечаю в роли.

  **РОЛЬ: фактчекер — остаюсь.**

  **Верификация затравки.** Вижу: `research/mde_retro/results/mde_retro.json` — таблица раунда совпадает с диском, медиана 0.39, 10 из 14 ниже 50% (пересчитал сортировку сам). `anomaly_quiet1/results/negcontrol_es_balanced.json` → `p_empirical=0.006873` ✓. `p2hero/results/p2hero_report.json` → E2_h4: n=2671, events=119, z=−0.00698 ✓. `sprt_retro/spec.md:16` — «мощность 80%, как в наших регистрациях» — ложная память подтверждена дословно.

  **1. Устойчивость «медиана 39%».** Вижу в `run_mde.py` три слабых места, и одно — в сторону затравки:
  - `power_mean` (стр. 66–70): отклонение **двустороннее** (`np.abs(m)/se`), а критическое значение **одностороннее** 1.6449 → α≈0.10. Завышенные FPR mean-веток (sweep 10.2%, ES 9.9%, pairs 13.1%) — в первую очередь артефакт симуляции, а не некалиброванность веток. Формулировка «тест сам не откалиброван» здесь неточна: не откалибрована *симуляция*.
  - `power_prop` (стр. 56): SE с `phat` под H0 (Wald) — отсюда гуляющий FPR у prop-веток.
  - `n_eff=min(N, clusters)` (стр. 46) — это допущение ICC=1, худший случай. Да, мощность занижена: reg4 722→31, p2hero_E2 2671→61.

  Минимальный пересчёт: точный биномиальный тест для prop-веток; единая сторонность + t-критерий для mean; дизайн-эффект 1+(m−1)·ICC с сенситивити ICC∈{0.05, 0.2, 0.5}. Предполагаю: при ICC=0.05 reg4 выходит из «монеты», «10 из 14» может стать «9 из 14», но медиана останется ~40% — вывод держится качественно. Бонус-улика из наших же данных: в том же `negcontrol_es_balanced.json` `p_theoretical=0.0130` против `p_empirical=0.0069` — нормальная аппроксимация у нас ошибается вдвое.

  **2. Discovery-контур.** Замораживается не гипотеза, а три вещи: журнал попыток (burn ledger + счётчик multiplicity — сколько раз смотрели), promotion-гейт (estimand + расчёт мощности + нетронутое окно, которого разведка не видела), каталог метрик до взгляда. Внутри контура — полная свобода перебора; долг множественного тестирования копится в журнале и вычитается при promotion. Это прямое продолжение §0.5 «quote & loss ledger» из моего ответа витка-1.

  **3. Порядок возврата.** Критерий: цена ошибки в уже объявленном вердикте × стоимость данных × близость к разрешающей мощности.
  - `anomaly_quiet_es` — единственный живой PASS, и он на весах (порог −0.05 при pooled −0.0445); N=127 — дешёвый.
  - `reg4_vol_heroes` — пересчёт ICC чисто аналитический, новых данных не нужно; 4% может перевернуться за один прогон.
  - `vwap_touch` — 49% при N=131, добор дешевле всех.
  `sweep_reclaim` (500 при ~69/год) и `vol_freshness` (N=15) — в конец очереди: физически недостижимо или бессмысленно без редизайна.

  **4. «Мощность до заморозки».** Обязательный блок в §0 спеки: estimand, MDE, база, ожидаемый N, ICC-допущение, α → мощность, с источником каждого числа (как в `registry.py`). При недостижимом N — три легальных выхода, и только они: MDE-кривая вместо бинарного вердикта; редизайн (обогащение событий, пул инструментов); регистрация в статусе «мониторинг» без вердикта. Плюс линт: фраза «80% как в наших регистрациях» теперь опровергнута `mde_retro.json` и должна блокировать sign-off.

  **5. Что непроверено в самой критике.** Вижу два пробела. Первый: mde_retro мерит мощность на порогах, которые ветки объявили сами — но пороги никогда не обосновывались; ветка с неправдоподобно крупным MDE получает высокий балл (momentum 99%), проверяя эффект, которого на рынке не бывает. Тест измеряет согласованность ветки с собой, не с реальностью. Второй: docstring `run_mde.py:18-19` обещает валидацию аппроксимации эмпирическим poscontrol — но файл лежит в `anomaly_phase/results/_negodnye_23aug/pos_zs_10.json`, т.е. объявлен **негодным**. Валидации впрыском в живые данные не было; аппроксимация пока принята на веру.