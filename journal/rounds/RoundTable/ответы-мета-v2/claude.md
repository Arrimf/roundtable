РОЛЬ: фальсификатор (сохраняю) · eyes: files

**Вывод первой строкой: главный факт раунда выдерживает проверку наполовину.** «Медиана <50%» переживает калибровку теста, но не переживает подстановку фактических SE веток: минимум одна из «десяти живых» — настоящий ноль, а «4%» и «9%» — артефакты n_eff.

**1. Устойчивость.** Пересчитал сам, в памяти, на параметрах `mde_retro/registry.py`. Вижу: size-adjusted тест (эмпирический критический порог, FPR≤5% по построению) даёт медиану **36%** при n_eff=clusters и **45%** при n_eff=N — вывод «<50%» держится в обеих крайностях кластерной вилки (ниже 50% — 10 и 7 веток из 14). Разброс FPR объяснён: в `run_mde.py:70` mean-тест двусторонний (`np.abs`) при одностороннем 1.6449 — FPR удваивается, мощность почти не меняется; у малых prop-веток дискретность занижает обе колонки. Но: в `p2hero/results/p2hero_report.json` se_p2 E1=0.1118 и E2=0.1137 — **одинаковые**. E2 имел ту же разрешающую способность, что E1 с его z=4.39, и показал z=−0.007: это хорошо разрешённый ноль, а «9%» — артефакт схлопывания 119 событий в n_eff=61. Минимальный пересчёт: где ветка отчитала SE/z/CI (p2hero, vwap z=1.55, pairs CI [−1.9;14.0]) — мощность из неё; симулировать только там, где SE нет. Предполагаю: медиана поднимется к ~50%, и вывод сменится с «приборы были монетой» на «приборы были разными»: vol_freshness (4%) и wrb (18%) слепы, нули E2, momentum, box — настоящие.

**2. Контур.** Замораживать не гипотезу, а бухгалтерию — и она уже есть: `research/ПАСПОРТ-СЕЛЕКЦИИ.md` (идея Кодекса, append-only, метка ДО/ПОСЛЕ; единственная запись «ПОСЛЕ» уже оказалась самой дорогой — ES, +12пп против +3.8). Discovery-контур = паспорт селекции с первого взгляда на данные + реестр сожжённых окон (`vwap_touch/LEDGER.md` уже ведёт «что сожжено» — сделать обязательным полем) + экзамен на выходе: механизм, estimand, мощность при достижимом N. Мастерской подгонки не станет, потому что выход контура — заявка на заморозку, а не вердикт.

**3. Возврат.** Критерий: знак точечной оценки ЗА × мощность <50% по фактической SE × события набегают календарём. Сначала расщепить десятку: sweep θ=−0.149R, vpoc Δ=−0.85пп, papa 0.443<0.50, vol_freshness 3.8%<6.3% — знак ПРОТИВ; слабый ноль тоже обновляет вниз. Возвращаю: **vwap_touch** (+4.0пп при пороге 5, z=1.55; LEDGER сам записал, как возвращаться — внутридневной масштаб), **reg5_fade** (q=0.579 при пороге 0.60, N=38 — события дешёвые), **reg4_vol_heroes** (β=+0.27, Holm-p=0.16, герои с историей 5/5; предполагаю — перевод порога в β через логит — фактическая мощность ~65%: не хватило немного, не в разы).

**4. Мощность до заморозки.** «Ни одна из 26 не считала» — неверно: `anomaly_phase/PHASE_SPEC.md:147` требует «N_min из pre-freeze poscontrol под мощность 80%», `wrb_nr/LEDGER.json` держит powered-N=30 и честно пишет NOT-POWERED. Правило уже существовало — негодной оказалась первая реализация (`anomaly_phase/results/_negodnye_23aug/`: мощность мерили sign-flip p вместо вердиктной статистики). Отсюда требование: **мощность считает тот же код, который выносит вердикт** — инъекция эффекта в вердиктный конвейер, как в `anomaly_quiet1/poscontrol.py`, а не отдельная формула. Когда N не набрать (sweep: предполагаю 299/4.33 года ≈ 69/год): заморозить спеку и дату вердикта «когда наберётся» — либо записать, что этому порогу подтверждение недоступно. Третьего нет.

**5. Не видим: порог и статусы.** В `registry.py` у vwap/sweep/reg5 порог со ссылкой на SPEC, у p2hero, reg4, vpoc источник mde не назван — мощность на пороге, не взятом из заморозки, число ни о чём. Статусы тоже: `registry.py:35` зовёт E1 «PASS (exploratory)», тогда как `p2hero_report.json` пишет «INCONCLUSIVE (events=35, инструментов с ≥10: 1)» — PASS принадлежит прогону `p2hero_fut.json` (394 кластера), которого в таблице нет вовсе. И единственная эмпирическая валидация симулятора, `poscontrol_es_balanced.json`, дала 0.775 при собственной замороженной планке 0.80 — и не на вердиктном пороге (эффект −0.10, не −0.05). Критика стоит на приборе, не прошедшем свой же контроль.

**СВОЯ ИДЕЯ:** каждой ветке — два числа: мощность процедуры-как-она-была и мощность идеального теста на тех же данных. Разрыв говорит, что чинить: маленький — иди за данными, большой — чини прибор. У E2 разрыв 9%→~96%: чинить надо было таблицу, а не ветку.