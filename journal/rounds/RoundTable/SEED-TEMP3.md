# Sign-off: тест темпа v3 — PASS по союзу ваших критериев. Подписывать ли §2?

Ответ ≤ 300 слов. Первой строкой — SIGNED или NEEDS-REVISION на бандл
«dr14 **θ=0.25** + оконно-локальный ZigZag + первый сегмент вон + span
вне метрики, DR14 заморожен по исходному ряду» как §2 спеки фигур
(`research_board/20260818-1500`).

## Честная хронология (проверяйте по паспорту: `research/ПАСПОРТ-СЕЛЕКЦИИ.md`)

1. После `темп-v2` исправлены все пять дефектов v2 (ev_nospan; один
   знаменатель; DR14 по исходному ряду; семьи ОДНОГО знака — 2 и 4
   отката; нуль-прогон без фигуры). Критерии — союз всех ваших
   предложений, ни одно не отброшено — заморожены ДО прогона
   (`gate_results/tempo3_prerun_criteria.json`). Три новых seed.
2. Прогон v3 при θ=0.10: **7 из 8**, провален только hit−cross_same
   ≥ 0.25 (Клод) — во всех ячейках. `tempo3.json`.
3. Затем пересчитал предикат §3 на бандле — как требовал Грок. **При
   θ=0.10 он ПРОВАЛЕН**: медиана 24–28 сырых пивотов в окне 96 на живых
   ES/NQ/CL/NG/6E. Дескриптор брал последние 8 — описывал дрожь последних
   ~30 баров, а не окно. Грок это называл в sign-off («C мельче, n=23»),
   я прошёл мимо.
4. θ пересчитан по правилу паспорта (медиана ∈ [4,8], максимум
   покрытия): **0.25·DR14** — медиана 7–8, покрытие 83–91%. При таком
   пороге вшитый флаг v3 (6·ATR ≈ 0.58·DR14) был наполовину ниже порога:
   тест мерял фигуру ниже живого масштаба. Фигура переведена в доли
   DR14: h = 1.0·DR14, ретрейсы U(0.35, 0.60). Паспорт: θ — ДО по
   структурному правилу; масштаб фигуры — post-diagnosis. **Восемь
   критериев не менялись.**
5. Прогон v3 при θ=0.25 (`tempo3_theta025.json`):

```
                                  все 6 ячеек × 3 seed
hit@20, знаменатель все пары      87–95 %     ✓
выход пар                         100 %       ✓
|Δn| ≤ 1                          100 %       ✓
двойник бьёт impostor той же семьи 75–84 %, LB95 > 0.5   ✓
hit − cross_same ≥ 0.25           41–54 пп    ✓  (при 0.10 — 5–30, провал)
кросс МЕЖДУ семьями (один знак)   0 %, ранг ~1500   ✓
ранг двойника ≤ ½ ранга impostor  5–8 против 17–21   ✓
нуль-прогон (дрейф без фигуры)    0 %         ✓
ВЕРДИКТ v3: PASS
```

## Вопросы

1. SIGNED / NEEDS-REVISION на бандл как §2 — с учётом того, что θ и
   масштаб фигуры менялись между двумя прогонами v3 (см. п. 3–4).
2. Что спека ОБЯЗАНА объявить ограничением: тест разрешения M15↔H1
   провален (H1-двойник в топ-20 в 0–11%; H1 теряет внутричасовые
   развороты — это не темп, а разрешение). Формулировка?
3. Своя идея — одна.
