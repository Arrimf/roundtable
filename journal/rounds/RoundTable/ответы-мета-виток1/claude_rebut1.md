РОЛЬ: фальсификатор · `eyes: files` (оставляю).

**Служебно, по правилу 4:** мой ответ первой фазы на диск не попал — в `ответы-мета-посылки/claude.md` лежит служебная реплика про план, содержание — в файле плана. Не переписываю задним числом, уточняю поверх.

**Про p=0.0069 — подтверждаю ведущего и добавляю худшее.** `negcontrol_es_balanced.json`: `instrument=es`, `p_empirical=0.006873`, 290 контролей. Атрибуция Голоса Г («это 6E») неверна: у 6E там 0.198. Но **пары «−11.9 пп при p=0.0069» нет ни в одном файле**: 0.0069 принадлежит *balanced*-прогону, где `observed.s = −10.08 пп`; s=−11.89 пп живёт в `replica_es.json` и в небалансированных негконтролях, где его собственный эмпирический p = 0.2 / 0.067 / **0.0141** (4 / 14 / 70 контролей). Единственный неоспоренный PASS стола докладывается величиной из одного прогона и p из другого. Плюс в том же файле `ks_stat=0.0951 > ks_crit=0.0799` — контрольное распределение неравномерно и в balanced-версии.

**Вопрос витка.**

**(1) Подмена вопроса.** 10.08: «искать аномалии и **вокруг них** искать закономерности». Консенсус `20260811-0050` это удержал: ~40 типов аномалий, пилот — 2 детектора, каждая выжившая ячейка → отдельная спека. Вердикт `20260812-1500`: 18 ячеек, BH пережила **одна** — A|NIGHT|T0. Дальше вся ветка — воспроизведение этой одной ячейки (реплики ES/NQ/6E/CL, перенос на ось фазы), а «backlog v2» не упомянут **ни в одной из 76 карточек доски после 12.08**. Шаг «вокруг них искать закономерности» не начинался: вокруг аномалии искали не структуру, а воспроизводимость. Генератор гипотез стал подтверждением самого себя. Короче, второй случай: 28.07 просили **инструмент поиска похожего** — построили предиктор скаляра (`analog_knn`, mean weekly net при h=4) и убили предиктор; инструмента, о котором просили, нет.

**(2) Класс интуиций.** arr говорит траекторией и формой («длинный ход, а следующая короткая», «векторно», «от старшего ТФ к младшему», «посмотреть каждый случай подхода»). Единственный раз, когда человек-прибор был подключён — `20260730-1519/1550`, 61 и 58 панелей, — исход напечатан **в заголовке панели** (SKIP / STOP / дожил). Дизайн unblinded: отличить «увидел» от «прочитал подпись» невозможно по построению.

**(3) Моя «одна посылка» — поднимаю на уровень.** Была: событийный порог как единственный способ образовать наблюдение. Теперь: **«единица работы = одна заранее названная гипотеза»**. Порог — её следствие. В 4 из 9 веток оригинал arr просил процедуру поиска, а отвечали confirmation-регистрацией.

1. **Изменил** — только по п.4 (см. выше). Не изменил: знаменатель 2/14 нечестен.
2. **Принимаю** довод Голоса Д: N-гейт имеет приоритет над содержательным KILL — это дефект **таксономии статусов**, не мощности (sweep θ=−0.149, vpoc −0.85, papa 0.443, box 0/816). Резче моего «трёх разных INCONCLUSIVE». И регистрацию C Голоса Б (overcontrol TOD) — единственную, способную убить наш PASS.
3. **Противоречие.** Голос А: «только Dukascopy OHLCV, стакан ни разу» — фактически неверно: NG идёт на Databento (`by_symbol/ng/db_m1.parquet`), tbbo в `examples/run_ib0991_ng*.py`, данные покупались (arr, 11.08). Верно другое: до вердикта L2 не довели. И volume-clock как «одна посылка» — смена нормировки: на объёмных барах мы построим ту же гонку двух порогов; обсуждался он в takens-round3/5 и pattern-lab — значит не «не пришло в голову», а не доведено.
4. **Упустили все, включая меня:** посылку **отбора**, а не измерения. Из 9 веток 5 стартовали словом «погнали/морозим». Мы нигде не записали, сколько идей отсеялось до спеки и по какому признаку. Если фильтр пропускает удобно-измеримое, счёт 2/14 измеряет фильтр, а не рынок, и различить A/B по нему нельзя в принципе. Лечится журналом отклонённых.
5. **СВОЯ ИДЕЯ:** слепой тест arr и Папы как измерительного прибора — 120 панелей M1 без подписей, 60 реальных якорей и 60 matched-плацебо, пре-регистрированная точность разметки «продолжение/разворот». Выше случайности — существует признак, которого нет в наших фичах, и его можно извлечь обучением на его же метках; не выше — интуиция закрыта как источник, тоже вердикт.

---

План записан в `~/.claude/plans/graceful-tickling-kitten.md`: дописать этот текст разделом `## rebut1` в `Choir/ответы-мета-посылки/claude.md` (первую строку не трогаю — только дописывание), в `~/Cursor_W/` ничего не пишу. `ExitPlanMode` в этой сессии недоступен — скажите слово, и запишу.