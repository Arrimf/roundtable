Читаю полный промпт и чужие ответы слепой фазы, затем сверю спорные факты по живым файлам.Дальше достану анонимные ответы слепой фазы и свой прошлый ответ, плюс сверю ключевые факты по диску.Сверяю спорные числа по диску — особенно p=0.0069 и претензии к чужим фактам.Проверил `negcontrol_es_balanced.json` и LEDGER: p=0.0069 — ES. Дальше сверю один спорный тезис Голоса А по `pairs` и отвечу по форме витка.РОЛЬ: фактчекер. `eyes: files`

**p=0.0069 — подтверждено по диску.** В `anomaly_quiet1/results/negcontrol_es_balanced.json`: `instrument=es`, `p_empirical=0.006873` (290 контролей). У 6E в `negcontrol_6e.json` — `p_empirical=0.198`. LEDGER от 2026-08-24 фиксирует: вердиктный PASS несёт **0.0069**; `replica_es.json` p=0.00195 — справочный. Мой слепой ответ здесь был верным. **Голос Г фактически неверен**: он приписал 0.0069 негконтролю 6E.

---

**1. Изменил / не изменил.**  
Не изменил: счёт (pairs v1.0 = **FAIL**, не INCONCLUSIVE; p2hero exploratory; «18 проб» ≠ momentum), вердиктный p, и **одну посылку** — «эффект = Δ долей/средних на бинарном resolved-исходе за удобный H». Оригиналы arr её **усиливают**, не снимают.  
Уточнил пункт 3 слепой фазы: диагностика матчинга TOD **была** (`20260818-1100_bias-prichina`) — односторонние placebo у края NIGHT. «Ни разу» неточно: было измерение дефекта, не было *проспективной* регистрации «матчинг как объект».

**Перевод реплика→спека (фокус витка).**  
(1) Подмена: 28.07 «эмбеддить историю и искать похожие в реалтайме» → `analog_knn` primary = mean weekly **net** при **h=4**. Вопрос о сходстве траекторий стал вопросом о среднем P&L. Ещё жёстче: 13.08 «математически **или векторно**» + WRB→NR → бинарный исход; «заглянуть **внутрь** сделки / 60 графиков глазами» (30.07) → агрегатный hit-rate в `papa_retest`.  
(2) Класс, который аппарат не выражает: **форма пути / вектор фигуры / сумма паттернов по ТФ** без свёртки в один скаляр. Нужны path-функционалы или retrieval без net@h, плюс протокол episode-atlas (выборка путей до агрегата).  
(3) «Одну посылку» **оставляю**; формулировку чуть сужаю: снимать не «бинарность» как таковую, а правило **«живая формулировка обязана стать одним скаляром на одном H»**.

**2. Чужой довод принимаю.**  
**Голос Д:** 2/14 — не однородные испытания (пилот / exploratory / second look); часть INCONCLUSIVE — дефект **таксономии** (N-гейт поверх уже отрицательного знака: `sweep` θ=−0.149, `vpoc` Δ=−0.85), а не «умеренный живой эффект». И что TOD-matching уже разоблачён карточкой bias — принимаю как факт.  
**Голос В:** бинарный fixed-H как главный рычаг слепоты — совпадает с диском и с оригиналами.

**3. Противоречие.**  
**Голос Г** — ошибка атрибуции p (см. выше).  
**Голос А:** (а) `pairs` «INCONCLUSIVE из‑за N=10» — устарело: exploratory так, регистрация 22.08 — **FAIL** (контроль variance-ratio). (б) Снимать «стационарность на календарной сетке / volume-clock» как *главную* посылку — не согласен. Volume-bars мы правда ни разу не делали, но оригиналы чаще теряют **путь и вектор**, не дискретизацию часов; volume-clock не чинит подмену «похожее→net@h=4».

**4. Что упустили все.**  
Инверсию порядка из реплики 10.08: «от аномалий → к закономерностям». Почти весь корпус делает наоборот (паттерн → pre-reg → тест). Единственный неоспоренный PASS (`anomaly-quiet`) ближе к исходной формулировке. Это не поймал никто как системную потерю перевода.

**5. СВОЯ ИДЕЯ:** заморозить **anomaly-first discovery**: sealed-детектор редких эпизодов *без* направленной H1 → только на holdout регистрировать regularity вокруг найденных локусов. Kill: локусы discovery не совпадают с holdout. Прямая проверка, был ли PASS свойством порядка открытия, а не рынка.