• Сначала читаю приложение с оригиналами arr и проверяю факт про p=0.0069.

• Проверки закончены, пишу ответ витка.

  РОЛЬ: фактчекер — остаюсь.

  **Фактчек-заметка по p=0.0069 (поручение витка).** Подтверждаю по диску: `anomaly_quiet1/results/negcontrol_es_balanced.json` → `instrument=es`, `p_empirical=0.006873`. `replica_es.json` → `p_one=0.00195`, вердиктное — эмпирическое, это уже зафиксировано в `LEDGER.md:15-22`. Одно уточнение к формулировке ведущего: «у 6E там 0.198» — 0.198 лежит в **не**-balanced `negcontrol_6e.json`; в `negcontrol_6e_balanced.json` p_empirical=0.117. Оба справочные, на вердикт не влияют, но ссылаться надо на конкретный файл.

  **Ответ на фокус-вопрос — что терялось при переводе.**

  (1) **Случай подмены.** analog_knn: arr 28.07 — «взять историю, как-то её эмбендить (**вот это самый главный вопрос**) и искать похожие паттерны реалтайм». Главный вопрос заказчика — качество эмбеддинга — в спеке не стал вопросом вообще: эмбеддинг зафиксирован по удобству, а проверялся mean weekly net при h=4. Удобный для измерения вопрос («есть ли mean-edge у kNN») заменил заданный («несёт ли соседство в эмбеддинге информацию»). Второй случай: papa_retest — arr просил «заглянуть внутрь сделки, посмотреть каждый случай глазами, 60 графиков»; стал event-study с бинарным 1R. Глазной разбор нигде не формализован и не проведён.

  (2) **Класс интуиций, который аппарат не выражает.** Видно по приложению прямым текстом: **выходы и затухание** — «входы отрабатывают неплохо... надо выходить при затухании движения» (03.08). Ни одна из 14 спек не тестирует выход/затухание: все — вход/событие. Рядом **объёмы**: «мечтал достать настоящие объёмы и протестировать» (30.07) — tick-volume остался прокси. И **мультимасштабная композиция**: «фрактальность... плясать от старшего ТФ к младшему, искать суммы в одну сторону» (13.08) — cross-scale aggregation не регистрировалась. Для первого нужен curve-valued endpoint (hazard/MFE-профиль во времени), для второго — данные, для третьего — дизайн с суммированием сигналов across ТФ.

  (3) **Одна посылка — сохраняется и усиливается.** Бинарный исход с фиксированным H. Оригиналы показывают механизм потери: arr всегда спрашивает «как ведёт себя цена после X» — это вопрос о **кривой**; спека отвечает «доля за H» — скаляр. Подмена кривой скаляром и есть главный translation loss, повторённый 14 раз.

  **1. Изменил/не изменил.** По посылке — не изменил. Изменил оценку «слепоты стола»: объёмы и выходы — не наша самодеятельная слепота, а **отложенные запросы заказчика**, зафиксированные в транскриптах и потерянные при формализации.

  **2. Принимаю.** Голос А (volume/L2 как несделанное) — приложение подкрепляет цитатой arr; теперь это не гипотеза о пробеле, а документированный долг. Голос Д: «определение события считается безошибочным» — приложение добавляет улику: «сжатие ×1.6 — оптимизируемый коэффициент», т.е. исходные определения сами были подогнаны, а спека их замораживала как данность.

  **3. Противоречие.** С Голосом А по приоритету: он предлагает volume-bars как «посылку на снятие», но настоящих объёмов нет — его регистрация А упирается в данные, которых не существует в контуре. Снятие посылки без закупки данных — декларация. Также отмечаю: ответ Голоса Е содержательных доводов не несёт (процедурная запись), спорить не с чем.

  **4. Упустили все.** Ни одна спека не содержит раздела «исходный вопрос заказчика и что мы в нём изменили». Потеря перевода невидима в момент потери именно потому, что перевод не фиксируется артефактом.

  **5. Своя идея.** Ввести обязательный §0.5 спеки «quote & loss ledger»: дословная реплика-источник + явный список отброшенных смыслов («объёмы — нет данных», «выходы — вне скоупа»); каждый отброшенный смысл автоматически становится backlog-гипотезой со своим номером. Дешево, замораживается тем же SHA, и мета-раунд через год читает не транскрипты, а сами спеки.