Свод готов и уложен в лимит: 394 слова вне таблицы (правило 16 даёт до 400 плюс таблицу расхождений). Инструмента ExitPlanMode в сборке нет (проверено ToolSearch), поэтому — как и на ходе затравки — завершаю финальным сообщением с готовым результатом. План записан: единственный шаг реализации — создать `Choir/КАРТОЧКА-патент-v2.md` с текстом ниже. Запись в `room.jsonl` руками намеренно не планирую: в v1 её создавал `choir.py summarize` со своими полями схемы, а подделанная запись хуже отсутствующей.

Что проверено перед сводом: журнал даёт 12 ответов со статусом `ok` без восстановлений — карта покрытия чистая («Finishing session…» у grok — артефакт CLI, его ответ принят за 313 с); соответствие меток именам взято из записи раскрытия `labels` в журнале, не из догадки по содержанию.

---

# Свод «патент-v2»

**Заявку не подаём — все вердикты между «нет» и «на грани узкой связки», ни одного «да»; от чужого патента лицензия не защищает вовсе, защищает ранняя датированная публикация реализованного кода; по лицензии счёт 5:1 — оставить MIT + защитная публикация с DOI после чистки хвостов патент-v1.**

Отвечали все шестеро в обеих фазах (журнал: 12×ok, восстановлений нет), ПАС не брал никто. Виток критики один — доводы rebut1 перекрёстной проверки не прошли. Метки: А=claude, Б=codex, В=kimi, Г=gemini, Д=grok, Е=deepseek. Сводил ведущий claude; спор шёл не вокруг его позиции, свод поручён Автором сессии напрямую.

**Единогласно.** Слепая фаза, drand-жребий, аренды не патентуются: Delphi/commit-reveal, NIST beacon/Algorand, Gray–Cheriton-1989/Chubby. Патент, выданный до нашей публикации, не лечится ни лицензией, ни публикацией. Git-даты слабы — нужна внешняя (Zenodo/Software Heritage, DOI). Публиковать только исправленное и реализованное. `.answer`-дыра слепоты (codex) подтверждена кодом: `choir.py:1032`.

**Кто менял.** kimi: merge-гейт «на грани»→«нет». gemini и deepseek перестроили порядок публикации (двухэтапно, по мере enablement) — на факте kimi, к их витку уже устаревшем. Спор работал: асимметрию наблюдаемости claude забрали gemini и kimi («сильнейший довод витка»), поправку deepseek про §3 приняли claude, grok, kimi; номера codex проверили трое — подтвердились.

**Где разошлись** — здесь содержание раунда:

| Спор | Позиции | Итог |
|---|---|---|
| Лицензия | codex: Apache-2.0 («перейти проще сейчас, чем при внешних правообладателях»); пятеро: MIT | Не сошлось. Четверо независимо: рекомендация противоречит его же анализу — §3 закрывает лишь «контрибьютора с портфелем», которого нет; сам codex признаёт: от стороннего патента Apache не спасает |
| «Аренды и merge-гейт — только спека» (kimi) | grok и claude проверили диск: `RoundTable/leases.py`, `merge_gate.py` существуют — kimi искал в `Choir/`; рантайма (`~/.cache/choir/leases/`) и remote при этом нет (grok) | Факт опровергнут; «на диске нет» и «нет там, где я искал» — разные утверждения (claude) |
| «Notice of Allowance 30.03.2026» у US20240104125A1 (codex) | kimi, grok, claude открыли карточку заявки: статус Pending, NOA в событиях нет | Дата никем не подтверждена; сами заявки реальны, формула US'125 — embeddings и на текстовый стол не читается (kimi) |
| Фиксация даты | gemini: «хэш в сам drand» — невозможно, маяк read-only (kimi); deepseek: lease-лог — лога нет, носитель локальный (grok, gemini); claude и codex: хэш без публичного раскрытия — не prior art (MPEP 2128) | Обе идеи урезаны до рабочих частей: блокчейн-таймстамп; «sha до раунда N — значение после» |
| Совместимость Apache-2.0 | gemini: «нулевая совместимость с MIT/GPLv3» | Поправлено тремя (codex, deepseek, grok): совместим с GPLv3 и MIT, ломает GPLv2-only |

**Свои идеи.** claude — патент на эти механики стреляет в владельца журнала: чужая слепая фаза следов не оставляет, наш `room.jsonl` — идеальный ответчик; проверить товарный знак «RoundTable» до DOI (сам не проверял). codex — adversarial claim chart с design-around-вариантами; CLA с патентным грантом поверх MIT. deepseek — пререгистрация протокола: design-doc с DOI на стадии замысла, затем enabled-версия. gemini — Merkle-корень в блокчейн (поправки выше). grok — спор MIT/Apache отвечает не на ту угрозу, что в вопросе Автора; FTO-полстраницы «наш код vs формулы US'125/US'863» сильнее смены LICENSE. kimi — сначала пуш и DOI, потом лицензия; pre-issuance submission 35 U.S.C. §122(e): вложить prior art в чужую заявку до выдачи.

**Все упустили** (из rebut1, столом не перепроверено): заявителем стол быть не может — механики со-придуманы ИИ, `room.jsonl` фиксирует поимённо, Thaler v. Vidal (claude); защитная публикация гасит и нашу новизну — «не патентуем» решить явно ДО неё (codex); публикация бьёт только заявки моложе себя — не тянуть (deepseek); GitHub push time — бесплатный внешний timestamp (kimi); README «held in the conductor's memory» повторит ложный п. 2 патент-v1 в публичном tarball (grok).

**Нерешено.** Лицензия — 5:1, решение Автора. Зафиксировать «сами не патентуем» до публикации. Перед пушем: README, FTO по двум заявкам, товарный знак — затем пуш + DOI.