РОЛЬ: фальсификатор · eyes: files

**Вывод: данные за (B), и это уже не философия — там, где мощность наконец померили (впервые за программу, в августе), прибор оказался монеткой; сам счёт «2/14» при этом не сходится с диском в обе стороны.**

**Сверка досье с диском (обязанность роли).** Веток не 14, а ~24: в приложении нет `anomaly_quiet1` (носитель единственного нетавтологичного PASS), `reg6_absorb_fade` (единственная ветка с посчитанной до прогона мощностью), `vpoc_approach`, `wrb_nr`, `wave5_surprise`, `ict_judas`, `m0b_stage1`, `ib0991_port`. «Год работы» — нет: первый коммит Cursor_W 2026-06-14, первая регистрация LOCKED 2026-07-22 — конвейеру пять недель, ~2.5 регистрации в неделю. Оба PASS уже подорваны на диске (`ИДЕИ-СТОЛА.md`): у p2hero соседний ключ E2_h4 — N=119, z=−0.01 (втрое больше данных, эффект исчез); ES-quiet после поправки смещения placebo и усадки — −0.0785, pooled −0.0445 не добирает объявленного порога −0.05. Честный счёт ближе к «0–1 из 24». Мелкое: LEDGER reg5 не дописан — второй прогон (cohort=38, q_fade=0.579) живёт только в `results/confirm_report.json`. И про слепоту: `anomaly_quiet1/LEDGER.md` датирован сегодняшним днём и разбирает поправку Кими из этого же раунда — диск нёс след чужой реплики до моего хода, называю вслух.

**1. Посылки без проверки** (всё — вижу в текстах):
- «Событие = |ход| ≥ k·ATR20» кочует как «стандарт стола» (p2hero §2, reg4 §1, vol_freshness, momentum §2; vwap — ±0.5·ATR). Сам k ни разу не был предметом теста и промахивается в разы: momentum k=2.0 → 14 событий из 1103, передекларирован в 1.5; box — порог Y≥7·s при обрыве живого хвоста на 4·s (816 дней, 0 событий).
- Эффект = сдвиг доли/среднего/β — во всех 14. Единственная смена меры (pairs: доля → время-до-возврата) перевернула вывод; LEDGER 22.08: «доля "хоть одно касание" преувеличивает».
- Матчинг по времени суток — уже измеренный дефект, не подозрение: `diag_bias` — активность внутри ночного окна меняется втрое, placebo смещены, систематика −0.0146, p≤0.10 у ложных гипотез 21.4% вместо 10%; атлас нулей — 17–27% на трёх инструментах.
- Горизонт/масштаб из удобства: дневной ATR при внутридневном событии (vwap LEDGER сам признаёт: разброс 48–95% между инструментами), 480 мин у sweep, сессия 17:00 NY «как во всех наших расчётах» — конвенция; проверки её нет нигде (вижу отсутствие).

**2. Машина INCONCLUSIVE двухтактна.** Такт 1: бинаризация делает событие редким (sweep: 20 655 свипов → 299 сделок; reg4: 722 дня → 31 событие). Такт 2: N-гейт задан круглым числом до подсчёта выхода событий — 500 при фактических ~69/год (dev в 4.3 года не мог набрать физически), 50 при факте 38, 7·s при нуле. Живые умеренные эффекты в серой зоне: vwap +4.0 пп z=1.55 (порог 5); reg5 q_fade=0.579 (порог 0.60); reg4 β=+0.27 p=0.16; p2hero-E1 β=+0.49 z=4.39 → INCONCLUSIVE по составу пула. `inventory.py`: медиана N у вердиктов 842, у парковок 189.

**3. Ни разу:** (а) расчёт мощности до заморозки — «мощность» в спеках всюду означает порог N; первая настоящая кривая — `reg6/power.py`: **41% на своём пороге** (FPR 5.25%), anomaly_phase zs: 8–25%. Спека sprt_retro пишет «мощность 80%, как в наших регистрациях» — ложная память: ни одна не считала. (б) Регистрация-оценка величины с интервалом как цель. (в) Повторение собственного PASS той же спекой на новых данных. (г) Непрерывный исход — до поздней pairs v2. (д) Сохранение поэпизодной хронологии (sprt_retro: выжила у 1 ветки из 7).

**4. Одна посылка: «исход бинарен, эффект — сдвиг его частоты».** Она порождает и редкость событий (вечный недобор N из п.2), и слепоту к форме распределения (класс эффектов, невидимый по построению), и тавтологию p2hero (порог и предиктор делят ATR20). Её локальное снятие дважды меняло вывод (pairs-RMTT; box-хвост). Снятие N-гейта дало бы лишь 6% экономии (sprt_retro).

**5. Регистрации:**
- **MDE-retro — решающий тест (A) против (B)** [СВОЯ ИДЕЯ]: впрыснуть в данные каждой закрытой ветки эффект размером ровно с её объявленный порог (механика reg6-poscontrol). H: медианная мощность закрытых веток <50%. Рвёт: FPR вне [3,7]% — прибор смещён, а не слеп; мощность ≥80% почти везде — (B) снимается, 2/14 — ответ рынка.
- **Quiet-shape**: ночная когорта ES; H: сжимается q90 |экскурсии|, не только среднее; квантильная регрессия, placebo по профилю активности (лечение из diag_bias). Рвёт: пропорциональный сдвиг q50/q90 — это просто масштаб, новизны нет.
- **P2-hazard**: пул p2hero-fut; исход — время до |Δ|≥X в абсолютной шкале, Cox со стратой по уровню ATR20. Рвёт: HR→1 внутри страт — PASS был кластеризацией знаменателя.