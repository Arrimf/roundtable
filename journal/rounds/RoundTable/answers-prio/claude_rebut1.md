РОЛЬ: фальсификатор (оставляю)

Перед ответом сверил факты по первоисточникам: `research_board/20260813-0130_claude_absorb-replicas-verdict.md`, `strategy_engine/research/anomaly_quiet1/{confirm.py,replicas.py}`, `strategy_engine/research/retriever/`.

**1. Изменил мнение — по объёму своей работы, не по её нужности.**
Проверка кода показала: канарейка K=20 shuffle **уже встроена** в конвейер (`confirm.py:154–183`), и сам эстиманд уже разностный против placebo-якорей (`d = y − y_placebo`). Значит моя слепая работа дня («placebo-окна, негативные контроли») наполовину уже сделана машиной — она сжимается с дня до пары часов. Приоритет дня уступаю прогону гейта: он даёт артефакт, которого физически нет (`gate_results/` не существует — подтвердил).

**2. Принимаю довод Голоса Г:** «6E — не дефицит мощности». Факт верен: N=326 против 171 у ES, тот же знак, |s|=0.0385 против практического порога 0.05. Реплики терминальны по спеке, окна 2025-08+ сожжены — добивать их нельзя по протоколу, а не «не стоит». Это снимает мой пункт «третья реплика прибавит шум»: третьей реплики и не будет.

**3. Противоречие осталось — и обострилось числом.** Все четверо считают, что ES-PASS за сутки ничего не теряет. Из той же карточки: pooled s ≈ −0.062, а ES = −0.119. Победитель семьи вдвое больше средневзвешенного при вдвое меньшем N — это учебный winner's curse. «Эффект есть» и «эффект равен −11.9 пп» — разные утверждения; в карточку записано второе, и на него сошлётся торговая спека.

Две фактические поправки. **Голос Б:** BH здесь — Benjamini–Hochberg, FDR q=0.10, порог 0.025; это не «жёсткая поправка Bonferroni-Holm». **Голос Б:** «CL PIPELINE-FAIL → упадёт память на векторах» — неверно: `PIPELINE-FAIL` в коде выставляется по статистической канарейке (`canary_ok`), это не авария среды и Track B не предсказывает.

**4. Что упустили все.** Голос А предложил час проверять, «разными ли путями кода прошли PASS и FAIL». Ответ уже написан: путь один, `replicas.py`, все четыре инструмента в одном прогоне. Важнее другое, что не назвал никто: вердикт **сам признаёт болезнь канареечной механики** на малых N («родственно C1-болезни max-ветки», кандидат-амендмент C2). Контроль качества конвейера отмечен дефектным — и именно он охраняет ES-PASS.

**СВОЯ ИДЕЯ:** записать в карточку ES **две цифры** — наблюдённую −0.119 и James–Stein-усадку к pooled с весом по N (ориентировочно −0.07…−0.08). В спортивной статистике наивное среднее лучшего отбивающего систематически завышено; строить торговую спеку надо на второй цифре, а не на первой.