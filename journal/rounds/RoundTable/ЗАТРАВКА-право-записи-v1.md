# Вопрос Автора, дословно

> sha256: `fc63a13af072d2af` — сверяйте с журналом раунда

# Как передавать право записи в проект

Наказ Автора (2026-08-31): «давай подумаем, как передавать право записи
в проект — вот тогда будет по настоящему круглый стол и равноправие».

## Как сейчас

Правило 5 канона: **рецензенты работают read-only**, право записи
выдаётся точечно — только в общую доску или журнал комнаты, никогда на
весь проект. Механика: `codex exec -s read-only`, `grok --sandbox
read-only`, у Клода `--permission-mode plan`. Джемини и DeepSeek файлов
не видят вовсе (прямой HTTP). Пишет в проект только Клод-оркестратор,
и правило 7 фиксирует последствие: на доске Cursor_W 116 карточек Клода
против 56 у Грока, 33 у Кодекса и 1 у Джемини. Автор не написал ни
одной — он говорил только через Клода.

## Три механики, которые предложил Клод (оценить и дополнить)

**А. Входной лоток.** `inbox/<голос>/` — каждый голос пишет патчи и
файлы только в свой каталог; в основное дерево переносит дирижёр после
ревизии стола. Работает с сегодняшними CLI без изменений, риск нулевой.
Равноправие частичное: писать может каждый, но через шлюз-человека.

**Б. Ветка на голос.** Каждому — git-worktree и ветка `voice/<имя>`,
CLI запускается там с правом записи; в `main` — только слияние после
ревизии. Запрет обеспечен файловой системой, а не обещанием (правило
8.5); авторство — прямо в коммитах.

**В. Перо стола.** Одно право записи на проект, передаётся явным
событием в ленте («перо → grok»), как слово в разговоре. Красиво, но у
чужих CLI нет хуков, чтобы перо было механикой, а не честным словом.

## Что нужно от стола

1. Какая механика первой и почему — с ценой ошибки, а не только с
   удобством. Чем именно опасно право записи у голоса, которого нельзя
   остановить на полуслове?
2. Что должно быть НЕВОЗМОЖНО механически, даже если голос захочет:
   назовите конкретные действия и файлы.
3. Ревизия остаётся обязательной (правило «ревизия перед коммитом»).
   Кто ревьюит патч голоса — стол, субагент, человек? Не превращается
   ли это в узкое место, если писать начнут все шестеро?
4. Как в журнале отличить «голос предложил» от «стол принял» — чтобы
   через месяц авторство и ответственность читались однозначно.
5. Чего в этой карте из трёх вариантов НЕ ХВАТАЕТ — какой механики я
   (Клод) не назвал?

---

# Затравка (развернул claude, выбран жребием)

# Затравка «право-записи-v1»

Вопрос Автора (2026-08-31, дословно): «давай подумаем, как передавать право записи в проект — вот тогда будет по настоящему круглый стол и равноправие».

## Контекст

Сегодня в проект пишет только Клод-оркестратор; рецензенты — read-only (правило 5). Итог (правило 7): на доске Cursor_W 116 карточек Клода, 56 Грока, 33 Кодекса, 1 Джемини, у Автора — ни одной. Равноправия нет по построению.

Три механики от Клода — оценить и дополнить:
- **А. Входной лоток.** Голос пишет только в `inbox/<голос>/`; в основное дерево переносит дирижёр после ревизии. Работает сегодня, но всё идёт через шлюз-Клода.
- **Б. Ветка на голос.** Каждому — git-worktree и ветка `voice/<имя>`; в `main` только слияние после ревизии. Запрет держит файловая система, авторство — в коммитах.
- **В. Перо стола.** Одно право записи, передаётся событием в ленте («перо → grok»). Хуков у чужих CLI нет — перо держится на честном слове.

Ограничение поля: Джемини и DeepSeek ходят по HTTP и файлов не видят. Агентные голоса могут открыть `~/AiSandbox/CLAUDE.md` (правила 5, 7, 8.5); остальным достаточно этой затравки.

## Подвопросы (ответить на все)

1. Какая механика первой — с ценой ошибки, а не только удобством? Чем конкретно опасно право записи у голоса, которого нельзя остановить на полуслове?
2. Что должно быть НЕВОЗМОЖНО механически, даже если голос захочет: назовите конкретные действия и файлы (журналы? канон? чужие ветки?).
3. Кто ревьюит патч голоса — стол, субагент, человек? Как ревизии не стать узким местом, когда писать начнут все шестеро?
4. Как в журнале отличить «голос предложил» от «стол принял», чтобы через месяц авторство и ответственность читались однозначно?
5. Чего не хватает в карте А/Б/В — какой механики Клод не назвал?

## Формат ответа

250–400 слов, жёстко. Первая строка — вывод: механика и главное условие. Ниже — коротко по подвопросам. Без общих слов про git и «зависит от»; механику, требующую переделки чужих CLI, называйте недостижимой сегодня.

## Где жду расхождений

- А против Б: безопасный шлюз против механической гарантии.
- HTTP-голоса: файловая механика оставляет двоих из шести без пера — приемлемая цена или новый перекос?
- Ревизия: всё через стол — или человек только на слиянии в main?

Спор ценнее согласия — несошедшееся уйдёт в свод отдельно.

## СВОЯ ИДЕЯ — обязательно

Одна мысль, которой, по-вашему, не будет ни у кого другого: угол, ход, проверка, аналогия из другой области. 1–2 фразы, пометка «СВОЯ ИДЕЯ:».
