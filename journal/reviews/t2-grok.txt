Сначала прочитаю карточку В1 и разберусь, какие вопросы стол задаёт.Карточка ясна: спрошу протокол и как сейчас устроены оба клиента DeepSeek, чтобы ответить по фактам, а не по названиям.Дочитаю канон DeepSeek и как голос заведён в `choir.py`, чтобы не путать стол и кресло исполнителя.HTTP за столом; dsh — вне комнаты (исполнитель и гейт). Гибрида в `live.py`/`choir.py` нет.

**1.** HTTP. Столу нужен один вопрос и один ответ той моделью, которой голос подписан (`choir.py`). Прослойка, которая сама решает — открыть файл, сменить модель, умолчать отказ — уже врала: Gemini CLI молча подменял модель. Правило 14.5 требует два класса зрения **на стол**, не внутри одного голоса. Тест безрукого голоса как раз держит `deepseek-http` рядом с Джемини. Замер «dsh точнее, потому что открыл файл» — не довод сменить клиент, а ровно то, зачем жребий контролёра.

**2.** Без агентности DeepSeek в комнате стол теряет только его место в жребии контролёра 14.5. Проверку пакета держат Клод, Кодекс, Грок, Кими. Сохраняется: одинаковый пакет (правило 1), `recover` по sha, `exit 3`, цена, `--effort max`. Посадить dsh за стол — несравнимые условия с Джемини, дыра в `recover`, расход без счёта и голос, который молча перестаёт быть читателем пакета.

**3.** Рычаг: не менять клиент. Если когда-нибудь DeepSeek вытянет контролёра — один `dsh --profile headless` с меткой `eyes: files`; все прочие ходы HTTP. Пока `no_files` — минус уже закрыт чужими руками. Пять секунд выигрыша dsh не рычаг: самый быстрый канал уже искажал очередь.

**СВОЯ ИДЕЯ:** смешать dsh и HTTP под одним именем `deepseek` — тот же класс бага, что подмена модели у Gemini CLI. Журнал подпишет ответ лабораторией, а условия будут разные. Нужны оба зрения — два имени, два `recover`, две метки `eyes`. Не одно кресло с двумя руками.
Finishing session…
