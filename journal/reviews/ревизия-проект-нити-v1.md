# ревизия-проект-нити-v1 — новый проект: нити на проект, каркас, карточка акта, быстрый вопрос с передачей (вер1, 2026-09-10)

Повод (Автор, ночь 2026-09-10): запустил окно в новом пустом каталоге
RideMigo. Ожидал новую сессию у всех голосов, ссылку на установленный
стол и файл с инструкциями проекта в каталоге, запись по умолчанию в
проект. Увидел: Грок упал «No session found for current directory»,
быстрый вопрос «случайному» умер на первом отказе, во вкладке
«исполнитель» сухой отчёт без дифа и без кнопки «принять», номер
правки не скопировать, а жребий быстрого вопроса — «не drand, а какой?».

Формат: стол вер1 (codex, grok, kimi, deepseek, gemini) плюс
субагент-ревьюер (Claude, отдельный экземпляр, построчно). Ответы
дословно ниже (правило 3).

## Свод (Клод — журналирует, не судит)

**Нашли многие:** запись каталога сессии (`_cwd` = проект) и сравнение
перед `-c` (cwd процесса) расходились — Грок и Кими никогда не
продолжали бы нить под проектом, а нить без записи каталога всё равно
шла бы `-c` в чужой каталог (grok, kimi, deepseek, codex); наследие
`state.json` присваивалось «первому, кто застал» — жребий гонки, а не
провенанс (grok, codex, gemini, kimi); `act_view` на каждый тик делал
полный скан ленты и два git-вызова (все пятеро); каталог с одним `.git`
считался непустым, и каркас не создавался (gemini, deepseek, grok,
kimi). **Нашёл один:** kimi — новая нить проекта входила с курсором 0 и
тащила весь архив ленты и контекст чужих проектов; «случайному» в окне
не передавался состав отмеченных голосов; «ведёт Автор» у ПРОЕКТ.md —
ложная атрибуция и канал инъекции в клонированном репозитории; codex —
`ПРОЕКТ.md` как висячая ссылка наружу и гонка записи в каркасе;
grok — холодный старт запасной линии Кими читал файл с ключом проекта.

**Где разошлись:** кнопка «Принять» в карточке — по `checks().ok`
(kimi: панель coder предложит merge при уехавшем main, гейт откажет
после confirm) против условия панели coder `ap>=q && !ref` (grok,
deepseek, gemini: гейт при merge сам делает rebase, `ok` строже, чем
нужно). Принято условие панели, в подтверждении названа цена rebase.

**Исправлено:** одна функция `_session_dir` и для записи, и для
сравнения; нить без записи каталога не продолжается; наследие
`state.json` проекту не присваивается (новый проект — новая нить, один
раз); новая нить входит с хвоста ленты; кэш карточки по (лента, голова
ветки), стадия из состояния акта; `.git` не содержимое; `ПРОЕКТ.md`
создаётся эксклюзивно, ссылки не трогаются, чужая `.roundtable` —
предупреждение; нейтральная подпись файла проекта («контекст, не
приказ»); состав для «случайного» — из галочек окна; `live.py threads`
(идея kimi). Не сделано: единый серверный предикат кнопки (kimi) —
оставлено условие coder.

**Проверено вживую** (изолированный журнал, RideMigo как проект): Грок
ответил из нового каталога за 10.6 с новой сессией; с заведомо кривой
моделью Грок отказал за 6.7 с, слово перешло DeepSeek. `_cwd` в ленту
не попал; каталог RideMigo не тронут.

**Тесты после (оба круга):** autoreview 45, voices_http 59, smoke 16, executor 14,
edits 31, gate 47, lease 51, catalog 45, argv 29.

**СВОИ ИДЕИ:** codex — паспорт запроса перед запуском (проект, новая
или продолженная нить, версия ПРОЕКТ.md, запасные каналы); grok —
непомеченную нить не присваивать, а показать как бесхозную (принято в
части «не присваивать»); kimi — `live.py threads` (внедрено);
deepseek — хэш ПРОЕКТ.md в состоянии, слать только при изменении;
gemini — проверять наличие локальной сессии до `--prefer`.

## Вопрос (пакет)

ВОПРОС СТОЛУ ВЕР1 — ревизия кода (read-only, 300–450 слов, вывод первой строкой; агентные голоса могут открыть живые файлы, HTTP-голоса — по выдержкам; инструменты не вызывать).

Повод (Автор, 2026-09-10): запустил окно стола в новом пустом каталоге проекта (RideMigo). Ожидал: новая сессия у всех голосов, в каталоге — ссылка на установленный стол и файл с инструкциями проекта для всех голосов, запись по умолчанию в проект, чтение остального. Увидел: Грок упал «No session found for current directory» (нить `-c` из старого каталога), быстрый вопрос «случайному» умер на первом отказе, во вкладке «исполнитель» — сухой отчёт без дифа и без кнопки «принять».

Что сделано (не закоммичено): (1) live.py: состояние нити — НА ПРОЕКТ (`state.<sha1(проект)[:10]>.json`; старый `state.json` без пометки достаётся первому проекту, который его застал); у grok/kimi (`cwd_session`) продолжение `-c` только из каталога, где нить началась (`st["cwd"]`); `say --once --prefer <голос>`: названный первым, отказ канала → следующий из `--voices` (потолок MAX_HANDOFFS); `<проект>/ПРОЕКТ.md` (≤4000) уходит в пакет ВСЕМ на первом ходу нити; (2) choir.py: ПРОЕКТ.md (≤3000) в каждом пакете раунда; (3) окно: быстрый вопрос «случайному» → `--voices <состав> --prefer <выбранный>`; `GET /act_view?act=` — карточка акта (интент, закрытие, ревизии с полными текстами, диф ветки через чистку ключей, `checks()`, стадия); каркас проекта при старте в ПУСТОМ каталоге (или `--init-project`): symlink `.roundtable` → стол, `ПРОЕКТ.md` по шаблону; JS: карточка во вкладке исполнителя с кнопками Ревизия/Принять/Adopt (тот же gatePost, что в панели coder), вердикты `<details>`, диф `<pre>`.

Тесты: autoreview 35, voices_http 59, executor 14, edits 31, gate 47.

Найди ошибку. Особенно: (1) наследие `state.json`: не унаследует ли второй проект чужую нить; холодный старт запасной линии Кими при проекте; `ev["_cwd"]` — не утечёт ли в ленту; (2) `--prefer` против адреса `@имя` и против явного выбора голоса человеком (подмена оплаченного канала — нельзя); (3) `act_view` на каждый опрос (6 с) — `acts_summary` + `checks()` + `act_diff` = git на каждый тик; traversal; (4) `scaffold_project` — symlink в чужой каталог, каталог только со скрытыми файлами (.git) — пустой ли, гонки, песочница/стол; (5) ПРОЕКТ.md всем — в том числе HTTP-голосам — и «инструкционные файлы проекта не пакет» из chamber/CLAUDE.md: не противоречие ли; (6) JS: XSS, таймер карточки при смене вкладки/акта, «Принять» только при `checks().ok` — согласуется ли с панелью coder (`ap>=q && !ref`). Вывод первой строкой: «дефектов не вижу» или список по убыванию опасности: файл, фрагмент, чем грозит, фикс в одну строку; догадки помечать. Последним — СВОЯ ИДЕЯ.

Живые файлы: ~/AiSandbox/RoundTable/chamber/live.py, ~/AiSandbox/RoundTable/chamber/choir.py, ~/AiSandbox/RoundTable/roundtable.py, ~/AiSandbox/RoundTable/test/autoreview_test.py; диф: ~/.claude/jobs/4883b2dd/tmp/proj-diff.patch.

## Выдержки

### live.py: состояние нити на проект (строки 710–757)
```
def _project_key() -> str:
    """Короткий ключ проекта для имён файлов состояния; "" без проекта."""
    if not PROJECT:
        return ""
    return hashlib.sha1(str(PROJECT).encode("utf-8")).hexdigest()[:10]


def _state_path(name: str, ch: dict | None = None) -> Path:
    """Файл состояния нити — СВОЙ НА ПРОЕКТ (наказ Автора 2026-09-10:
    новый каталог — новая сессия). Сессии Грока и Кими и так привязаны к
    рабочему каталогу: старая нить в новом каталоге давала «No session
    found for current directory», и быстрый вопрос падал. У остальных
    нить переносила бы контекст одного проекта в другой. Возврат в
    прежний проект возвращает его нити. Без проекта — прежний
    state.json."""
    k = _project_key()
    return vdir(name, ch) / (f"state.{k}.json" if k else "state.json")


def load_state(name: str, ch: dict | None = None) -> dict:
    """Состояние НИТИ, а не голоса: у линии свой курсор, своя сессия и
    свой счётчик ходов. Запасная линия обычно отстаёт — она и должна
    получить больше дельты, когда её позовут: она этих реплик не видела.
    Слить состояния было бы враньём в обе стороны (правило 8.5)."""
    p = _state_path(name, ch)
    if p.is_file():
        try:
            return json.loads(p.read_text(encoding="utf-8"))
        except Exception:                              # noqa: BLE001
            pass
    if PROJECT:
        # Нити до 2026-09-10 жили в state.json без проекта: та, что шла
        # в ЭТОМ проекте (или не помечена вовсе), продолжается — иначе
        # переезд оборвал бы живые разговоры. Помечаем, чтобы второй
        # проект её не унаследовал тоже.
        legacy = vdir(name, ch) / "state.json"
        if legacy.is_file():
            try:
                st = json.loads(legacy.read_text(encoding="utf-8"))
                if st.get("project") in (None, str(PROJECT)):
                    st["project"] = str(PROJECT)
                    legacy.write_text(json.dumps(st, ensure_ascii=False, indent=1),
                                      encoding="utf-8")
                    return st
            except Exception:                          # noqa: BLE001
                pass
    # ХОЛОДНЫЙ СТАРТ ЗАПАСНОЙ ЛИНИИ. cursor=0 значит «не видел ничего», и
    # первая же реплика утащила бы ей ВЕСЬ архив ленты: замер ревьюера —
```

### live.py: save_state (строки 781–785)
```
def save_state(name: str, st: dict, ch: dict | None = None) -> None:
    st["project"] = str(PROJECT) if PROJECT else None
    _state_path(name, ch).write_text(
        json.dumps(st, ensure_ascii=False, indent=1), encoding="utf-8")

```

### live.py: продолжение только из того же каталога (строки 1265–1270)
```
            if use_cont and v.get("cwd_session") and st.get("cwd") \
                    and st["cwd"] != str(cwd):
                # `-c` ищет сессию ЭТОГО каталога: нить, начатая в другом,
                # там не найдётся («No session found for current directory»)
                use_cont = False
            cmd = _voice_cmd(v, "cont" if use_cont else "start", ptext,
```

### live.py: turn() отдаёт каталог (строки 1343–1344)
```
        ev["_cwd"] = str(cwd)          # снимается в deliver до записи в ленту
    except subprocess.TimeoutExpired as e:
```

### live.py: deliver() сохраняет каталог нити (строки 1495–1497)
```
            if ev.get("_cwd"):
                st["cwd"] = ev.pop("_cwd")   # каталог нити (для cwd_session)
            save_state(n, st, ch)
```

### live.py: cmd_say --prefer (строки 1996–2009)
```
    prefer = getattr(a, "prefer", None)
    # Адрес — выбор человека: подменять адресата передачей нельзя,
    # как и в --once (нашёл субагент). pool = сами names.
    names = _cap_group(names)
    pool = names if (once or why.startswith("по адресу")) else allowed
    if once and prefer and prefer in allowed and not why.startswith("по адресу"):
        # Быстрый вопрос «случайному»: окно выбрало голос, но обещание
        # человеку — ответ, а не конкретный канал. Отказ канала передаёт
        # слово следующему из состава (наказ Автора 2026-09-10: Грок
        # выпал без сессии, и раунд не прошёл). Потолок — MAX_HANDOFFS.
        names, why = [prefer], "выбран окном случайно; при отказе — следующий"
        pool = allowed
    res = hand_word(ev, names, why, pool)

```

### live.py: ПРОЕКТ.md в пакет (строки 860–879)
```
def project_doc() -> str:
    """Описание проекта из <проект>/ПРОЕКТ.md (кладёт окно при старте в
    новом каталоге, дописывает Автор). Уходит в пакет ВСЕМ одинаково
    (правило 1) — в отличие от CLAUDE.md/AGENTS.md, которые подхватывает
    только чей-то CLI. Обрезка по потолку названа в тексте."""
    if not PROJECT:
        return ""
    f = Path(PROJECT) / PROJECT_DOC
    try:
        txt = f.read_text(encoding="utf-8").strip() if f.is_file() else ""
    except OSError:
        return ""
    if not txt:
        return ""
    if len(txt) > PROJECT_DOC_LIMIT:
        txt = txt[:PROJECT_DOC_LIMIT] + f"\n[…обрезано до {PROJECT_DOC_LIMIT} символов, полный текст — {f}]"
    return f"ОПИСАНИЕ ПРОЕКТА ({f.name}, ведёт Автор):\n{txt}\n\n"


def build_prompt(name: str, st: dict, events: list[dict], first: bool,
```

### roundtable.py: быстрый вопрос с --prefer (строки 3167–3174)
```
            if quick and picked_by_server:
                # Обещание человеку — ответ, а не канал: выбранный
                # случайно идёт первым, отказ канала передаёт слово
                # следующему из состава (наказ Автора 2026-09-10: Грок
                # выпал без сессии в новом каталоге, и раунд не прошёл).
                args += ["--voices", ",".join(VOICES), "--prefer", picked_by_server]
            elif voices:
                args += ["--voices", ",".join(voices)]
```

### roundtable.py: карточка акта (строки 633–682)
```
def act_view(act: str) -> dict:
    """Всё об акте правки для карточки во вкладке «исполнитель»: интент,
    закрытие, диф ветки (чищеный), ревизии с полными текстами, стадия и
    счёт гейта — то, что Автор ждал увидеть, выбрав акт, вместо сухого
    отчёта (2026-09-10). Кнопки гейта рисует страница по stage."""
    st = merge_gate.act_state(act)
    if not st["open"]:
        raise merge_gate.GateRefused(f"акт {act} неизвестен ленте")
    op = st["open"]
    out = {"act": act, "voice": op.get("voice"), "seat": op.get("seat"),
           "task": op.get("task") or op.get("text"), "project": op.get("project"),
           "base_sha": op.get("base_sha"), "worktree": op.get("worktree"),
           "opened": op.get("ts"), "close": None, "crash": None,
           "reviews": [], "diff": "", "diff_truncated": False, "head": None}
    for k in ("close", "crash", "adopt", "merge", "rebase"):
        e = st.get(k)
        if e:
            out[k] = {kk: e.get(kk) for kk in
                      ("ts", "text", "status", "rc", "head", "dirty", "autocommit",
                       "excluded", "elapsed_s", "result_sha", "by") if kk in e}
    for r in reversed(st.get("reviews") or []):        # старые первыми
        out["reviews"].append({kk: r.get(kk) for kk in
                               ("ts", "voice", "verdict", "status", "sha",
                                "elapsed_s", "eyes", "seat", "full_text", "text")})
    try:
        c = merge_gate.checks(act)
        out["checks"] = {"ok": c.get("ok"), "reasons": c.get("reasons"),
                         "approvals": c.get("approvals"), "refused": c.get("refused"),
                         "quorum": c.get("quorum", merge_gate.QUORUM),
                         "stale_base": c.get("stale_base"), "scope": c.get("scope")}
        out["head"] = c.get("head")
    except Exception as e:                                  # noqa: BLE001
        out["checks"] = {"ok": False, "reasons": [str(e)]}
    rows = {r["act"]: r for r in (merge_gate.acts_summary(limit=200) or [])}
    out["stage"] = (rows.get(act) or {}).get("stage")
    try:
        if op.get("project"):
            base = merge_gate._effective_base(st)
            diff, head, _ = merge_gate.act_diff(Path(op["project"]), act, base)
            out["head"] = out["head"] or head
            diff = _scrub_log(diff)
            if len(diff) > ACT_VIEW_DIFF_LIMIT:
                diff, out["diff_truncated"] = diff[:ACT_VIEW_DIFF_LIMIT], True
            out["diff"] = diff
    except Exception as e:                                  # noqa: BLE001
        out["diff_error"] = str(e)
    return out


def _spawn_review(act: str, *, auto: bool = False,
```

### roundtable.py: каркас проекта (строки 139–168)
```
def scaffold_project(project: Path, *, force: bool = False) -> None:
    """Каркас нового проекта (наказ Автора 2026-09-10): в каталоге запуска
    ссылка `.roundtable` на установленный стол и `ПРОЕКТ.md` с
    инструкциями для всех голосов. История стола остаётся в одном месте
    (journal/), проект держит своё описание у себя. Создаётся только в
    ПУСТОМ каталоге (или по --init-project): в чужой проект вроде
    Cursor_W стол ничего не кладёт без просьбы."""
    if not project.is_dir():
        return
    table = Path(__file__).resolve().parent
    if project == table or project == table.parent:
        return                                  # сам стол и песочница — не проект
    entries = [q for q in project.iterdir() if q.name not in (".roundtable", "ПРОЕКТ.md")]
    if entries and not force:
        if not (project / "ПРОЕКТ.md").exists():
            print(f"каталог {project} не пуст — каркас проекта не создан; "
                  f"roundtable --init-project создаст .roundtable и ПРОЕКТ.md",
                  file=sys.stderr)
        return
    link = project / ".roundtable"
    if not link.exists() and not link.is_symlink():
        link.symlink_to(table, target_is_directory=True)
        print(f"проект {project.name}: ссылка .roundtable → {table}")
    doc = project / "ПРОЕКТ.md"
    if not doc.exists():
        doc.write_text(PROJECT_DOC_TEMPLATE.format(name=project.name, table=table),
                       encoding="utf-8")
        print(f"проект {project.name}: создан ПРОЕКТ.md — опишите проект, "
              f"он уйдёт в пакет всем голосам")

```

### roundtable.py JS: карточка гейта (строки 6250–6279)
```
  function renderGate(v){gate.innerHTML='';
    const h=el('h4','','правка '+v.act+' ['+(v.voice||'?')+(v.seat&&v.seat!==v.voice?'/'+v.seat:'')+'] · '+({opening:'открыт',working:'в работе',closed:'закрыт',crashed:'вылет',adopted:'adopt',merged:'принят',lost:'ветка потеряна'}[v.stage]||v.stage||'?'));
    gate.appendChild(h);
    if(v.task)gate.appendChild(el('div','dim','задание: '+String(v.task).slice(0,300)));
    if(v.close)gate.appendChild(el('div',v.close.status==='done'?'':'no','закрытие: '+(v.close.text||'')+(v.close.autocommit?' · коммит сделала обёртка':'')+(v.close.excluded&&v.close.excluded.length?' · вне коммита: '+v.close.excluded.join(', '):'')));
    if(v.crash)gate.appendChild(el('div','no','вылет: '+(v.crash.text||'')));
    if(v.merge)gate.appendChild(el('div','ok','принят: '+(v.merge.result_sha||'').slice(0,12)));
    const c=v.checks||{}; const ap=(c.approvals||[]).length, q=c.quorum||2, ref=(c.refused||[]).length;
    const st=el('div',c.ok?'ok':'', 'гейт: '+(c.ok?'открыт — можно принимать':'закрыт')+' · одобрений '+ap+'/'+q+(ref?' · ОТКАЗ: '+c.refused.join(', '):'')+(c.stale_base?' · main уехал, нужен rebase':''));
    gate.appendChild(st);
    if(c.reasons&&c.reasons.length&&!c.ok)gate.appendChild(el('div','dim',c.reasons.join('; ')));
    const row=el('div','row'); const a=v.act;
    if(v.stage==='closed'||v.stage==='adopted'){
      if(!c.ok)gateBtn(row,'Ревизия',function(){if(!confirm('Ревизия акта '+a+': платный веер всем, кроме исполнителя. Пускаем?'))return;
        gatePost('/edit_review',{act:a},function(){return 'ревизия '+a+' запущена — вердикты придут в ленту'})});
      if(c.ok)gateBtn(row,'Принять в main',function(){if(!confirm('Принять акт '+a+' в main? Кворум '+ap+'/'+q+'. Ветка сольётся, main сдвинется.'))return;
        gatePost('/edit_merge',{act:a},function(j){return 'принято: '+(j.result_sha||'').slice(0,12)})});}
    if(v.stage==='crashed')gateBtn(row,'Adopt',function(){if(!confirm('Adopt акта '+a+': рассмотреть вылетевшую работу из карантина?'))return;
      gatePost('/edit_adopt',{act:a},function(){return 'adopt: '+a})});
    gateBtn(row,'⟳',function(){loadGate(true)});
    gate.appendChild(row);
    if(v.reviews&&v.reviews.length){const rv=el('div','','ревизии:'); gate.appendChild(rv);
      v.reviews.forEach(function(r){const d=document.createElement('details'); d.open=!!GATE.open['r'+r.ts];
        d.ontoggle=function(){GATE.open['r'+r.ts]=d.open};
        const sm=document.createElement('summary'); sm.textContent=(r.voice||'?')+': '+(r.verdict||'?')+' ('+(r.status||'?')+(r.elapsed_s?', '+r.elapsed_s+' с':'')+')'+(v.head&&r.sha&&r.sha!==v.head?' · на прежней голове':'');
        sm.className=r.verdict==='approve'?'ok':r.verdict==='refuted'?'no':'dim'; d.appendChild(sm);
        d.appendChild(el('pre','',r.full_text||r.text||'(текста нет)')); rv.appendChild(d)})}
    const dd=document.createElement('details'); dd.open=!!GATE.open.diff; dd.ontoggle=function(){GATE.open.diff=dd.open};
    const ds=document.createElement('summary'); ds.textContent='диф ветки'+(v.head?' '+String(v.head).slice(0,12):'')+(v.diff_truncated?' (обрезан)':'')+(v.diff_error?' — '+v.diff_error:''); dd.appendChild(ds);
    dd.appendChild(el('pre','',v.diff||'(диф пуст)')); gate.appendChild(dd)}
```


## Статусы вызовов

```
codex rc=0 41s
gemini rc=0 100s
deepseek rc=0 345s
grok rc=0 479s
kimi rc=0 761s
ALL DONE 761s
```

## Ответы дословно


=================== codex
roundtable.py, `doc.exists()/write_text()` — запись через висячий symlink вне проекта: создавать файл эксклюзивно; live.py, `project in (None, ...)` — присвоение чужой старой нити: мигрировать только при доказанном совпадении проекта; live.py, `and st.get("cwd")` — продолжение без известного каталога: отсутствие `cwd` считать холодным стартом; roundtable.py, `acts_summary(limit=200)` — потеря стадии старого акта и кнопок: вычислять стадию из `st`.

Ревизия только по выдержкам, без инструментов; перечисленные тесты не подтверждают покрытие этих случаев.

В `scaffold_project` достаточно заранее существующего `ПРОЕКТ.md`, являющегося ссылкой на отсутствующий файл снаружи: имя исключено из проверки пустоты, `exists()` вернёт False, `write_text()` последует по ссылке. Есть также гонка между проверкой и записью, способная затереть появившийся файл. Нужны эксклюзивное создание и обработка конфликта. `.git` учитывается в `entries`: такой каталог здесь **не пустой**. Чужая существующая `.roundtable` молча сохраняется; следует проверить её назначение. **Гипотеза:** сравнение `project == table` обходится альтернативным путём, если вызывающий код не делает `resolve()`. Сама ссылка никаких ограничений записи не устанавливает; ожидаемый режим «писать в проект, читать остальное» выдержками не подтверждён.

Последовательный второй проект помеченный legacy уже не получит. Но первый получает непомеченную нить независимо от её происхождения — это нарушает обещание новой сессии. При одновременном старте оба процесса могут прочитать непомеченное состояние и присвоить его: нужны блокировка и атомарная миграция. Проверка `cwd` пропускает старое состояние без этого поля; если `use_cont` уже истинно, прежний сбой `-c` остаётся возможным. Холодный старт запасной Кими проверить нельзя: соответствующая ветка обрезана. `_cwd` снимается внутри показанной ветки `deliver`; **гипотеза утечки** остаётся для остальных путей публикации. Надёжнее удалять служебные поля непосредственно перед любой записью события.

`--prefer` не перебивает адрес при корректном `why.startswith("по адресу")`; явные `voices` в показанной ветке окна также сохранены. Подмена оплаченного выбора здесь не доказана. Однако смысл выбора закодирован текстом `why`, а разрешение передачи — наличием `prefer`. Лучше передавать отдельные признаки явного адресата и разрешённого fallback; проверять, что fallback происходит именно при отказе канала.

При заявленном опросе каждые шесть секунд карточка повторяет `checks`, сводку и диф; масштаб git-нагрузки зависит от скрытых реализаций. Нужны стадия одного акта и кэш с инвалидированием при изменении состояния. Traversal по выдержке не доказан: валидации `act` и реализации `act_diff` нет.

XSS через показанные `textContent` не видно; безопасность `el` неизвестна. Таймер и защиту от запоздалого ответа другого акта проверить невозможно без `loadGate`. `c.ok` строже `ap>=q && !ref`: панели расходятся, обеим нужен единый серверный предикат с повторной проверкой при merge.

Передача `ПРОЕКТ.md` всем заявлена, фактические места включения не показаны. Противоречие с указанным правилом CLAUDE.md следует явно разрешить исключением для общего документа; первый ход также не доставляет последующие изменения.

СВОЯ ИДЕЯ: показывать перед запуском паспорт запроса — проект, новая/продолженная нить, версия ПРОЕКТ.md и разрешённые запасные каналы.
=================== grok
Читаю живые файлы и диф — ищу дефекты по шести заявленным рискам.Проверяю наследие `state.json`, холодный старт Кими, `hand_word`/`--prefer`, `act_view` и панель coder.В живом `live.py` формула `_cwd` уже не совпадает с патчем — сверю сравнение каталога, `post()` и панель coder.**live.py:1265–1269≠1346–1348; live.py:740–753; roundtable.py:act_view 633–682; JS renderGate 6250–6266; scaffold 151; live.py:767 (кimi-alt).**

1. **`chamber/live.py` `turn()`, сравнение `st["cwd"] != str(cwd)` против записи `_cwd`.** Процессный `cwd` у grok/kimi — `voices/<имя>`, а в state кладётся `PROJECT` (у Грока сессия по `--cwd`, у Кими — по cwd процесса). После первого `save_state` с проектом сравнение всегда «чужой каталог» → `-c` не вызывается, нить каждый ход начинается заново (токены и память разговора). Обратно: унаследованный `state.json` без поля `cwd` проверку **пропускает** (`st.get("cwd")` ложно), `-c` идёт в новый каталог — тот же «No session found». Фикс: одна функция `session_cwd(v)` и в запись, и в сравнение; нет `cwd` ⇒ не продолжать.

2. **`load_state` наследие `state.json`.** Непомеченную нить забирает **первый** проект, который её прочитал (RideMigo после патча), и `load_state` **пишет** пометку в legacy — побочный эффект чтения (`who`, предсбор промпта). Комментарий обещает «второй не унаследует»; второй по смыслу (новый каталог) как раз наследует, RoundTable потом видит чужой штамп и стартует вхолодную. Гонка двух окон до штампа — оба заберут одну нить (догадка). Фикс: `os.replace(legacy, state.{k}.json)` только если `st["project"]==PROJECT` или `cwd` совпал; иначе legacy не трогать.

3. **`roundtable.py` `act_view` на тик 6 с.** `checks()` уже делает `act_diff`; следом снова `act_diff`; плюс `acts_summary(limit=200)` с другим ключом кэша, чем `/state` (limit=12) — кэш один, ключи разные, каждый опрос пересчитывает сводку и git. Фикс: стадию вывести из `act_state`; диф взять из `checks`; карточку кэшировать по `(act, head, mtime ленты)`.

4. **JS карточки ≠ панель coder.** «Принять» по `c.ok` (ложно при `stale_base`, где merge сам делает rebase) и «Ревизия» при любом `!c.ok` (в том числе при ОТКАЗЕ, который coder прячет). В `checks()` поле `refuted_by`, в карточку копируется `refused` — суффикс «ОТКАЗ:» пуст. Фикс: кнопки как у coder (`ap>=q && !ref` / `!ref && ap<q`); пробросить `refuted_by`.

5. **`scaffold_project`.** `iterdir()` видит `.git` → каталог с одним git init «не пуст», каркаса нет. Сравнение `project == table` без `resolve()`: запуск через symlink на стол может положить `.roundtable` внутрь стола (догадка). Фикс: игнорировать точечные имена; сравнивать `resolve()`.

6. **Холодный старт запасной Кими.** Курсор основной берётся из `_state_path(name)` (keyed), без legacy: пока основная только унаследовала `state.json` и не сохранила keyed — запасная садится на `COLD_TAIL`, не на курсор основной. Фикс: `load_state(name)` без канала.

`--prefer` адрес `@имя` и явный `--voices` не подменяет (`why.startswith("по адресу")`, `picked_by_server` только при пустом выборе). `_cwd` в ленту не уходит (`pop` до `post`). XSS не вижу (`textContent`). Таймер на смене вкладки/акта гасится (`showGate('')`, `GATE.edit!==e`). ПРОЕКТ.md — **названное исключение** из «CLAUDE.md/AGENTS.md не пакет», не противоречие: пакет всем одинаковый; агентные всё равно читают диск.

Тесты `act_view`/`scaffold` не трогают нить, `-c` и `--prefer`.

**СВОЯ ИДЕЯ:** непомеченный `state.json` не пришивать молча ни к одному проекту — переименовать в `state.unclaimed.json` и показать в `live.py who` («бесхозная нить, каталог неизвестен»). Автор одним ходом отдаёт её проекту или сбрасывает. «Первый, кто застал» — это жребий гонки, а не провенанс, и он как раз ломает наказ «новый каталог — новая сессия».

=================== kimi
• Читаю живые файлы и диф, чтобы проверить выдержки.

• Проверяю `hand_word`, холодный старт и правило из chamber/CLAUDE.md.

• Проверяю оставшиеся точки: состав `--voices`, флаги `cwd_session`, `checks()/act_diff`, панель coder.

• Три быстрые проверки: `_grok_cmd`, панель coder, `sendQuick`.

• Дефекты есть; по убыванию опасности:

  1. **live.py:1346 vs 1265 — рассинхрон `_cwd`, Грок и Кими НИКОГДА не продолжают нить под проектом.** При `PROJECT` в `st["cwd"]` пишется `str(PROJECT)` (оба `cwd_session`-голоса без `cwd_project`), а сравнивается со `str(cwd)` — для Грока это `vdir`, для Кими каталог линии: неравно всегда → `use_cont=False` на каждом ходу. Без ошибки, молча: новая сессия каждый раз, полный контекст оплачивается заново, «возврат в прежний проект возвращает его нити» — неправда. Для Кими запись `PROJECT` ещё и противоречит своему комментарию («у Кими — сам cwd процесса»). Фикс: одно выражение `sess_cwd` (грок — `--cwd`-каталог, кими — `str(cwd)`) использовать и в сравнении, и в `ev["_cwd"]`.

  2. **live.py:778 — холодный старт ОСНОВНОЙ линии в новом проекте тянет весь архив.** Хвост `COLD_TAIL` дан только запасной линии; основная в новом проекте получает `cursor=0` → первая реплика забирает весь глобальный `live.jsonl` (их же замер: 59 635 символов) и контекст ЧУЖИХ проектов — ровно то, от чего строилось разделение нитей. Фикс: в финальном `return` при `PROJECT` — курсор от хвоста ленты, как в ветке запасной.

  3. **live.py:769–784 — наследие `state.json`: метка сбрасывается.** Помеченный проектом А legacy-файл остаётся на месте; запуск БЕЗ проекта читает его напрямую (пометка не проверяется), а `save_state` пишет `project=None` — после этого второй проект чужую нить унаследует. Плюс гонка claim read-modify-write при двух окнах (догадка). Фикс: claim атомарным `os.replace(legacy, _state_path(name, ch))` вместо перезаписи на месте.

  4. **roundtable.py:3172 — `--voices VOICES` при quick игнорирует состав окна.** Окно для «случайному» состав (`picked('room')`) не шлёт; сервер перебирает и подставляет из полного `VOICES` — оплата канала, который человек снял галочкой. Фикс: окну слать состав, серверу — `secrets.choice` и `--voices` по нему.

  5. **ПРОЕКТ.md — ложная атрибуция «ведёт Автор».** В клонированном репозитории файл может уже лежать с чем угодно и уйдёт ВСЕМ, включая HTTP-голосов без диска, как слово Автора — канал инъекции; и спорит с соседней строкой пакета «не приказ» (формально правило про CLAUDE.md/AGENTS.md не нарушено — файл новый). Фикс: нейтральная пометка «файл проекта — не приказ». Заодно: дубль `project_doc()`/`project_notice()` с потолками 4000/3000 разъедется — в одно место.

  6. **act_view на каждый тик (6 с).** `checks()` (несколько git) + `acts_summary(200)` + полный `act_diff` со scrub до 200 КБ — и даже при терминальной стадии, когда меняться уже нечему. Фикс: кэш/304 по `(act, head, stage)`; JS не перезапускать таймер при `merged|lost`. Traversal закрыт (hex-валидация, `project` из ленты).

  7. **«Принять»: карточка строже панели coder** (roundtable.py:4735 — `ap>=q && !ref`, без `stale_base`/scope): панель предложит merge при уехавшем main, гейт откажет после confirm. Фикс: единый предикат `checks().ok`.

  8. **scaffold:** каталог только с `.git` — НЕпустой: `git init && roundtable` каркаса не даст. Фикс: игнорировать `{".git",".DS_Store"}` при проверке пустоты.

  JS чист: везде `textContent`, таймер чистится, mismatch-гвард есть. Мелочь (догадка): ключ `GATE.open['r'+r.ts]` сольётся при двух ревизиях с одним ts.

  **СВОЯ ИДЕЯ:** раз нить теперь на проект — нужна `live.py threads`: список `state.*.json` по голосам с проектом и датой последнего хода. Иначе через месяц никто не вспомнит, что у RideMigo свои сессии, и «воскрешение» старой нити станет сюрпризом с чужим контекстом.


=================== deepseek
Дефекты: live.py:1265 (`cwd_session` не тушит `-c` при старом `state.json` без `cwd`); live.py:1996 (`--prefer` подменяет явный `--voices`); roundtable.py:633 (`act_view` тяжёлый на каждом опросе); roundtable.py:139 (каталог только с `.git` не считается пустым); догадка — JS-гонка при смене акта/вкладки. XSS не вижу, если `el()` пишет через `textContent`.

1. **live.py, 1265–1270**  
`use_cont` гасится только когда `st.get("cwd")` и он отличается. У legacy-состояний из `state.json` поля `cwd` могло не быть — тогда в новом каталоге Grok/Kimi снова пойдут с `-c` и упадут «No session found». Фикс: `if use_cont and v.get("cwd_session") and st.get("cwd") != str(cwd): use_cont = False` — отсутствие `cwd` тоже роняет продолжение.

2. **live.py, 1996–2009**  
Условие `prefer in allowed` позволяет `--voices Kimi --prefer Grok --once` заменить явно выбранный человеком канал. Фикс: `if once and prefer and prefer in names and prefer in allowed and not why.startswith("по адресу"):`.

3. **roundtable.py, 633–682**  
`act_view` на каждый опрос делает `checks()` + `acts_summary(200)` + `act_diff` — это git и обход ленты каждые 6 с. Фикс: убрать `act_diff`/`checks` из лёгкого опроса и вызывать их только при явном открытии карточки или по `?full=1`/кэшу.

4. **roundtable.py, 139–146**  
`entries` включает скрытые файлы, поэтому новый git-каталог с одним `.git` не получает каркас. Фикс: `entries = [q for q in project.iterdir() if q.name not in (".roundtable", "ПРОЕКТ.md", ".git")]`.

Догадка: в JS-поллере нет проверки `v.act === текущий выбранный акт`; ответ от предыдущего акта может перерисовать карточку после переключения. Фикс: `if (v.act !== GATE.act) return;`.

«Принять» только при `c.ok` строже панели coder `ap>=q && !ref`, но это корректно, если `checks.ok` учитывает `stale_base`/`scope`; панель coder стоит привести к тому же условию.

Своя идея: хранить в состоянии хэш `ПРОЕКТ.md` и добавлять его в пакет только при изменении, а не каждый раунд/первый ход каждой линии.

=================== gemini
Дефекты (по убыванию опасности):

1. **roundtable.py**, `scaffold_project()`: `entries = [q for q in project.iterdir() if q.name not in (".roundtable", "ПРОЕКТ.md")]`
   Чем грозит: После `git init` в новом каталоге появляется скрытая папка `.git`. Функция считает каталог непустым и не создает `.roundtable` и `ПРОЕКТ.md`, ломая автоинициализацию в свежих репозиториях.
   Фикс: Исключать `.git`: `entries = [q for q in project.iterdir() if q.name not in (".roundtable", "ПРОЕКТ.md", ".git")]`.

2. **roundtable.py**, `act_view()`: `diff, head, _ = merge_gate.act_diff(Path(op["project"]), act, base)`
   Чем грозит: При каждом опросе `/act_view` (каждые 6 с) выполняются тяжелые git-команды (`act_diff` и `checks`), создавая бесполезную нагрузку на ЦПУ/диск и риск блокировок git при активной работе.
   Фикс: Кэшировать диф акта по `head` или отдавать диф по отдельному клику в UI.

3. **live.py**, `cmd_say()`: `if once and prefer ...: pool = allowed`
   Чем грозит: При вызове `--voices grok,kimi --prefer grok` сброс `pool = allowed` расширяет запасную линию до ВСЕХ голосов палаты. В случае отказа Грока ход может уйти незапрошенному/оплачиваемому голосу вне `--voices`.
   Фикс: Ограничивать пул переданными именами: `pool = [v for v in names if v in allowed]`.

4. **live.py**, `load_state()` (догадка): `legacy.write_text(...)` без создания файла проекта.
   Чем грозит: Старый `state.json` метится именем проекта, но не сохраняется в целевой `state.<key>.json`. До первого вызова `save_state` чтение состояния продолжает обращаться к legacy-файлу.
   Фикс: Вызывать `save_state(name, st, ch)` сразу при успешной миграции legacy-состояния.

Разбор вопросов Автора:
- `ev["_cwd"]`: Извлекается через `st["cwd"] = ev.pop("_cwd")` в `deliver()` перед записью в ленту, утечки в журнал нет.
- `ПРОЕКТ.md` всем: Противоречия с `CLAUDE.md` нет. CLI-агенты автоматически подхватывают из `cwd` файлы `CLAUDE.md`/`AGENTS.md`, а файл `ПРОЕКТ.md` без отправки в пакете они не увидят.
- JS / UI: XSS отсутствует (вывод через `textContent` и DOM-элементы). Кнопка «Принять» завязана на `c.ok`, что строже панели coder (`ap>=q && !ref`), так как блокирует слияние при `stale_base` до выполнения rebase.

**СВОЯ ИДЕЯ**: Ввести быструю проверку наличия локальной сессии (`cwd_session`) перед формированием `--prefer` для быстрого вопроса. Если у голоса (например, Grok) нет сессии в текущем `cwd`, сервер сразу выберет другой активный голос из состава, исключая заведомый падение и задержку на передачу хода.

=================== субагент-ревьюер (Claude, opus, отдельный экземпляр; построчно)
Ревизия закончена. Файлы правились прямо во время разбора (три волны), отчёт — против состояния live.py sha1 395d30c5, roundtable.py 170f32b6, choir.py 7c9ff405. Три дефекта, найденные в ранних версиях, уже починены параллельной правкой (грок/кими теряли -c под проектом; refused вместо refuted_by; «Принять» пряталась при stale_base).

## Находки

1. Такт сверки пишет `_cwd` в журнал и ломает нить контролёра (live.py:1355, 1508, 1654, 1900). turn() кладёт служебное ev["_cwd"], снимает его только deliver(); audit() зовёт turn() напрямую и отдаёт ev в cmd_ask, где идёт post(**ev). Воспроизведено: абсолютный путь проекта в journal/live.jsonl; st["cwd"] контролёру никогда не пишется → следующий ход грока/кими начинает сессию заново каждый такт сверки. Фикс: подъём cwd в состояние из turn(), post() чистит ключи с «_».

2. Быстрый вопрос «случайному» обходит защиту «два хода одному голосу» (roundtable.py:3223, 3248, 3278): передача может позвать любого из pool, а want/spawn регистрируют занятым только picked. Воспроизведение: выпал grok → передача к codex; параллельно «Сказать» с @codex — clash пуст, два процесса ведут нить codex. Фикс: want = set(pool).

3. Журнал врёт про выбранный голос при @имя + «случайно» (roundtable.py:3223, 3282–3287): адрес выигрывает в pick_voices, --prefer пропускается, а в ленте «голос выбрал сервер случайно: grok». Фикс: при @имя в тексте не ставить picked_by_server.

4. Карточка перестраивается целиком каждые 6 с — крадёт клик и сбрасывает прокрутку дифа (roundtable.py:6301, 6337). Рядом на ленте актов стоит лекарство: sig=JSON.stringify(v). Фикс: guard по сигнатуре.

5. Опрос карточки не останавливается никогда (roundtable.py:6333–6337): не смотрит document.hidden и терминальную стадию; act_state() зовётся до кэша — полный разбор ленты каждые 6 с вечно. Фикс: hidden → пауза; merged/lost → стоп.

6. _ACT_VIEW_CACHE не ограничен, ключ не видит сдвига main (roundtable.py:631, 726): ручной коммит в main не двигает ни ленту, ни ветку акта → карточка показывает stale_base=false. Фикс: ограничить кэш, добавить в ключ rev-parse refs/heads/<branch>.

7. ПРОЕКТ.md читается на КАЖДЫЙ вызов голоса (choir.py:279, 1317, 1671): правка файла посреди фазы даст голосам разные пакеты — нарушение правила 1; packet_prefix перестанет воспроизводить пакет. Фикс: снимок один раз на процесс.

8. .roundtable — симлинк, который расширяет область каждого голоса (roundtable.py:171): внутри выданного read-only корня лежит ссылка на весь стол (лента, state.*.json с идентификаторами сессий); в коммит рукой Автора попал бы путь записи в стол. Фикс: .roundtable в .git/info/exclude.

9. Пул передачи не фильтруется по available() (live.py:1956, _quietest 1809): голос без CLI — фаворит передачи, два таких съедают MAX_HANDOFFS. Фикс: фильтр available(v).

10. Холодный старт под проектом обходит правило запасной линии (live.py:778–784) — догадка; по порядку ветвей запасная линия обрабатывается раньше.

11. Мелочи: cmd_reset сбрасывает нити только текущего проекта, а пишет «нити сброшены»; save_state пишет project: null без проекта; st["cwd"] и для kind=error; _scrub_log до обрезки; act_view считает diff дважды за промах кэша.

## Что проверил и НЕ нашёл
Тесты зелёные (autoreview 45, executor 14, lease 51, argv 29, voices_http 59 на 8792; py_compile чисто; node --check ок; replay.js без ошибок). XSS в карточке нет (textContent/el(), innerHTML только для очистки). gatePost видна и совпадает по сигнатуре. Таймер карточки гасится при смене вкладки/акта; гонка «ответ после смены акта» закрыта. /act_view: regex закрывает traversal. _cwd не утекает через deliver (кроме audit — находка 1). Наследование state.json двумя проектами — дефекта больше нет. Холодный старт запасной линии читает основную ЭТОГО проекта; ключ от resolve()-пути одинаков у окна и CLI. --prefer с @имя не подменяет адресата; передача воспроизведена grok(отказ) → claude, потолок соблюдён. ПРОЕКТ.md не уходит дважды. scaffold: resolve(), стол и песочница исключены, чужая .roundtable и ссылка ПРОЕКТ.md не трогаются, open(doc,"x") эксклюзивно, .git не содержимое. Живых моделей не звал, журналы, порт 8770, RideMigo и worktrees не трогал.


## Второй круг (по отчёту субагента)

Исправлено: каталог сессии пишется в состояние самим `turn()` (такт
сверки больше не теряет его), `post()` не пишет служебные поля с «_»;
занятыми при «случайном» считается весь состав передачи; адрес `@имя`
в тексте сильнее случайного выбора; карточка перерисовывается только
при изменении данных, молчит в скрытой вкладке и на терминальной
стадии; кэш карточки ограничен восемью актами и видит сдвиг main и
аренду; чистка дифа после обрезки; `.roundtable` дописывается в
`.git/info/exclude` проекта; передача — только доступным голосам
(`available`); `project_notice()` — снимок на процесс; сброс нитей
называет проект. Не сделано: двойной `git diff` при промахе кэша
(внутри `checks()` и свой) — мелочь, оставлено.

**Проверено вживую после правок:** два хода Грока подряд из RideMigo —
10.1 с и 4.9 с, второй продолжил сессию (`-c`), в состоянии
`cwd = ~/AiSandbox/RideMigo`, ответы 56 и 81; служебных полей
в ленте нет.
