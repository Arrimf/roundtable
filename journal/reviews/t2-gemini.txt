gemini-http: ключ #2 исчерпан по квоте, пробую следующий
gemini-http: ключ #3: 503 у шлюза, повтор через 4 с
gemini-http: ключ #3: 503 у шлюза, повтор через 4 с
gemini-http: ключ #3 исчерпан по квоте, пробую следующий
gemini-http: ключ #4: 503 у шлюза, повтор через 4 с
gemini-http: ключ #4: 503 у шлюза, повтор через 4 с
gemini-http: шлюз кладёт gemini-3.7-flash на втором ключе с 5xx — остальные ключи не жгу, ухожу на запасную
gemini-http: ⇄ модель gemini-3.7-flash не ответила (gateway) → пробую gemini-3.6-flash. Это ТОТ ЖЕ голос, но ДРУГАЯ модель — учитывайте при сравнении ответов
gemini-http: ключ #2, модель gemini-3.6-flash, 2322 токенов, 23.3 с, история: ~/.gemini/choir-http/20260901T113357-3a605b7073d9146d.jsonl
**Вывод: За столом B1 должен сидеть `deepseek-http`.**

1. **Почему HTTP**: Канон 14.5 требует чистого читателя пакета за столом, так как `dsh` уже работает агентом-исполнителем. HTTP обеспечивает биллинг, `exit 3` при исчерпании квоты и механизм `recover`.
2. **Потери и компенсация**: Без агентности HTTP не открывает файлы сам, но за столом B1 это делает `choir.py`, упаковывая контекст в пакет. HTTP теряет 5 секунд скорости, но дает предсказуемый контракт отказов и рычаг `--effort`.
3. **Дешёвый рычаг**: **Context Caching** от DeepSeek. Закрепление системного промпта и контекста раунда в кэше снизит задержку HTTP с 12 до 5–6 секунд (быстрее `dsh`) и уменьшит стоимость входных токенов на 90%.
4. **СВОЯ ИДЕЯ**: **«Аварийный каскад B1» (Smart HTTP-Fallback)**. За столом всегда работает `deepseek-http`. Если он падает с `exit 3` (нет баланса) или таймаутом, легкий wrapper мгновенно перенаправляет текущий ход в `dsh --profile headless`, подставляя в журнал расчетную метрику стоимости по длине промпта. Стол B1 получает неразрывную выживаемость без нарушения правила 14.5 в штатном режиме.
