# Ревизия: индекс гипотез Cursor_W (KanBan/index.py) и сторож окна — три круга (2026-09-22…23)

Повод: наказ Автора («делай индекс и дальше по порядку»). Шаг 1 проекта
KanBan: `KanBan/index.py` только читает Cursor_W и строит один граф из
четырёх несвязанных слоёв гипотез (борд, ветки research/*, реестр
идей, паспорт селекции) → `KanBan/index/{nodes,edges,events}.jsonl` и
`report.md`. Попутно в окне стола: карта покрытия в карточке раунда до
свода (`_coverage_now`) и сторож POST /access, считающий только нашу
клетку. Определения индекса — в шапке index.py (вторая редакция).

Формат: вер1 — параллельные read-only вызовы по пакету (codex, grok,
gemini-http, deepseek-http, kimi через ворота) плюс субагент построчно
с правом гонять тесты и пробники по КОПИИ данных; три круга, потому что
первая редакция определений врала на живых данных, а вторая — в двух
местах. Ответы дословно (правило 3).

## Круг 1 (индексатор, первая редакция)

Стол: codex — упал (модель gpt-6-astra не поддержана аккаунтом), gemini
— 503 на всех ключах, grok — не уложился в 10 минут (только преамбула),
deepseek 7 пунктов, kimi 9 пунктов; субагент 11 пунктов.

### deepseek

ДЕФЕКТЫ:

1. `index.py:~340` (в `report()`, блок висяков): `verdict_branches` включает ветки со стадией `instrument`, поэтому линии, привязанные к таким веткам, исключаются из висяков. Но `instrument` означает неработающий прибор, а не вердикт по гипотезе; такие линии должны оставаться висяками. В живом отчёте это даёт 60 висяков вместо ожидаемых 64 (расхождение в описании). Воспроизвести: ветка с `verdict: "PIPELINE-FAIL"`, тред старше 30 дней с `branch_of` на неё; в отчёте тред не попадёт в висяки.

2. `index.py:~235` (в `read_ideas()`): условие `if m and section and stage != "rule"` молча пропускает идеи в виде жирных пунктов в разделах со стадией `rule`, хотя определение идеи не исключает такие разделы. Нарушает правило 4. Воспроизвести: добавить в ИДЕИ-СТОЛА.md раздел `## Правила` и строку `- **Идея** (автор): ...`; идея не появится ни в nodes.jsonl, ни в unparsed.

3. `index.py:~195` (в `read_branches()`): противоречие определяется как `{"alive", "refuted"} <= cats`, но не учитывает категорию `killed`. Если в ветке есть вердикт `ЖИВА` и `KILL`, противоречие не будет отмечено. Воспроизвести: два results-файла с такими вердиктами — узел ветки получит `contradiction=False`.

4. `index.py:~190` (в `read_branches()`): `raw_verdicts[(raw[:80], cat)] += 1` обрезает сырую формулировку до 80 символов, из-за чего разные длинные вердикты, различающиеся после 80-го символа, считаются одной записью, занижая число «сырых формулировок» в отчёте. Воспроизвести: два файла с длинными строками вердикта, совпадающими в первых 80 символах, — в report.md они будут одной строкой.

5. `index.py:~350` (в `report()`, «веток без треда»): множество `linked_b` строится из всех рёбер `branch_of`, включая рёбра от семейств. Если ветка связана только с семейством, а не с тредом напрямую, она не попадёт в список «без треда», хотя фактически не имеет связи с тредом. Воспроизвести: семейство с тредом, привязанным к ветке, — ветка исчезнет из списка, хотя прямой связи с тредом нет.

6. `index.py:~310` (в `link()`): шумовой токен `"retest"` не отфильтровывается (в noise есть `"test"`, но не `"retest"`), из-за чего возможны ложные рёбра `branch_of` между веткой с `retest` и тредом, имеющим лишь общий первый токен. Воспроизвести: ветка `papa_retest_pilot` и тред `papa-cases` получат ребро, хотя сходство только по `papa`.

7. `test_index.py`: отсутствуют тесты для сценариев 1–3, 5, 6, что позволяет регрессиям оставаться незамеченными. В частности, не проверяется влияние веток `instrument` на висяки и пропуск идей в разделах `rule`.

СВОЯ ИДЕЯ: проверить, что у всех линий, привязанных к веткам с категорией `instrument`, последняя карточка старше 30 дней и стадия не `verdict`; если да — они должны быть висяками, и их отсутствие в отчёте является ошибкой (подтверждает дефект 1 на живых данных).

### kimi

• Проверяю по живым файлам: слаги семейств, реестр идей, registry, mtime вердиктов.

• Реестр прочитан. Теперь проверяю mtime вердиктов, ИДЕИ-СТОЛА, паспорт и карточки figury.

• Проверяю номера строк в index.py и два живых среза данных (read-only).

• ДЕФЕКТЫ:

  1. `index.py:329` — нумерованный пункт считается идеей без проверки `stage != "rule"` (проверка есть только в жирной ветке, строка 334). В ИДЕИ-СТОЛА.md:251–260 пункты «1. Сырой коридор…», «2. Floor гейта…», «3. Композитный гейт…» — это примеры-доказательства внутри правила, а не идеи. Воспроизведение: отчёт «Идеи по стадии: rule: 3»; ручной счёт идей — 0 в этом разделе. Отсюда и 52 против ~45+5.

  2. `index.py:333–335` — идеи-абзацы вида `**1. Соседний ключ уже содержит ответ** (Грок)` не матчатся ни `^(\d+)\.` (строка начинается с `**`), ни `^-\s+\*\*`. Молча потеряны 9 содержательных идей разделов «Проверено» и «В очереди» (ИДЕИ-СТОЛА.md:45–136) — включая калибровку p и мягкий KILL; в «Не разобрано» их нет, правило 4 нарушено. Воспроизведение: ни одного узла `idea:` с section «Проверено» в nodes.jsonl.

  3. `index.py:366–367` — `split("|")` не уважает код-спаны с пайпом. Строка 24 паспорта (`A|NIGHT|T0`) и строка 32 (`|k|`) режутся, `cells[2:3]` смещается, обе строки — «неясно». Воспроизведение: ручной счёт ДО=14 против «ДО 12, неясно 2» в отчёте; потеряна именно строка winner's curse.

  4. `index.py:414–419` — `branch_of` по первому токену: `anomaly_phase` (ключевые токены `{anomaly}`) и `papa_retest_pilot` (`{papa, retest}`) ловят всё с токеном anomaly/papa. В edges.jsonl: `thread:anomaly-first-seed → branch:anomaly_quiet1` (разные гипотезы), `thread:papa-cases → branch:papa_retest_pilot`. Побочка: ветка papa с вердиктом выводит papa-wrnr-vector и papa-short-candles-recon из висяков — в списке их нет, хотя последняя карточка 2026-08-13/14 и вердикта по линии нет.

  5. `index.py:77` + правило висяков — текстовые закрытия невидимы: «закрыты/отозван» не в FAMILY_STRIP, стадии «closed» нет. `figury-zakryty` (ветка ЗАКРЫТА, ИДЕИ-СТОЛА:227, гейт FAIL) и `gate4-otozvan` числятся висяками. Воспроизведение: раздел «Висяки», строки 2026-08-20 figury-zakryty и 2026-08-19 gate4-otozvan.

  6. `index.py:288–292` + `node()` — registry `p2hero_E1` и `p2hero_E2` оба мапятся на `branch:p2hero` (`{p2hero} ⊆ {p2hero,e1}`); `node()` пишет только в пустые поля, поэтому статус E2 «СИГНАЛА НЕТ» молча теряется. Воспроизведение: registry.py:34–39 против узла branch:p2hero — один registry_status.

  7. `index.py:97` — «КАНДИДАТ» → alive: «КАНДИДАТЫ (top-1): ['A|NIGHT|T0']» — шортлист ячеек, не вердикт «жива». Отчёт, таблица словаря, строка alive.

  8. `index.py:267` — противоречие для hypo_duel ложное: 12 вердиктов там — разные суб-гипотезы (grok_report, wave3, g2…), смесь alive/refuted заложена дизайном дуэли. Раздел «Противоречия» отчёта.

  9. Тест (`test_index.py`) не покрывает: пайп в ячейке паспорта, нумерованные пункты в rule-секции, жирные абзацы-идеи, двойной маппинг registry, ветку с одним ключевым токеном.

  СВОЯ ИДЕЯ: пересечь стадии слоёв — семейство/тред со стадией verdict, не имеющий ни ветки с вердиктом, ни события verdict («бумажный вердикт»): на живых jsonl таких 34 (absorb-night-t0, figury-i-speka, pattern-lab…), т.е. треть «закрытых» линий закрыта только словом в слаге, и эту цифру не выдаёт ни один раздел отчёта.


### субагент (11)

1) крах без каталога research; 2) идеи-абзацы `**N.` потеряны (13), примеры правила посчитаны идеями, ячейки с `|` резались, «ЗАКРЫТА» не наследовалась; 3) паспорт: `A|NIGHT|T0` резался — ДО 14/ПОСЛЕ 6 вместо 12/6/2; 4) маркер signoff = исход (4 из 6 лгали), svod/final = вердикт (11 из 31); 5) семейства рвут линии (fade/reg6, temp/temp-itog, wrb-nr/wrbnr…) и склеивают (ib0991); 6) branch_of по одному общему токену прячет висяки (papa, anomaly); 7) вердикт по mtime без секунд, плацебо p2hero делал ветку alive; 8) реестр: второе совпадение теряется; 9) цитаты по 13-символьному префиксу неоднозначны; 10) «противоречия» hypo_duel — артефакт «ветка = каталог»; 11) молчаливые пропуски: каталоги без results, заголовки LEDGER без даты, обрезки списков.

## Круг 2 (вторая редакция индексатора + окно)


### grok

Читаю полный запрос и параллельно разбираю код обеих правок.Дальше читаю `index.py` и параллельно сверяю живые семейства, рёбра и сторож bwrap.Сверяю `link`/`paper_verdict`, правило «ЗАКРЫТА» и живые карточки бумажных вердиктов.Проверяю пропуски `results` у «бумажных» веток и дыры в `branch_of`, плюс сторож bwrap и coverage схемы 1.Нашёл расхождение у `anomaly_quiet1`: проверяю, считает ли «бумажный» по стадии ветки, а не по событиям вердикта.Проверяю ещё один край: ложное ребро `ib0991→ib0991_port` и покрытость тестом.**ДЕФЕКТЫ:**
- `KanBan/index.py:567-571` — «бумажный вердикт» смотрит на `branch.stage ∈ HYPOTHESIS_VERDICTS`, а комментарий и определение требуют отсутствия *события* `verdict` гипотезы; при последнем файле-приборе линия врёт.

Что ломается: `family:anomaly-quiet1` / `thread:anomaly-quiet1-verdict` помечены `paper_verdict` и попали в отчёт («18 из 20»), хотя у `branch:anomaly_quiet1` уже 10 событий `verdict` с `alive`/`inconclusive`/`refuted` (PASS, INCONCLUSIVE, STOP: BH…); `stage=instrument` только потому, что последний по mtime — `bias_theories_nq_k40.json` («B — trimming…»), строка 357. Воспроизведение: `python3 index.py` → в «Бумажные вердикты» есть `anomaly-quiet1`; сверка: `events.jsonl` по `branch:anomaly_quiet1` + `results/*.json`. Тест `test_index.py:157-159` этого случая не покрывает (только «нет ветки» / «ветка с hyp-стадией»).

Остальное по запрошенным векторам. Семейства `takens`, `wrb-nr`↔`wrbnr`, `absorb-night-t0` сходятся с карточками. Рёбра `wrb_nr`, `vpoc_approach`, `m0b_stage1` — по правилу токенов; `papa-wrb-nr→wrb_nr` намеренно. Идеи: 58 = ручной разбор; «ЗАКРЫТА» закрыла ровно 12 из раздела фигур «В РАБОТЕ», лишнего нет. У прочих «бумажных» часто вердикт есть в тексте карточки, но не в `results/` привязанной ветки — это совпадает с определением «без вердикта *ветки*», не ложь метки (кроме `anomaly-quiet1`).

B: перестановка аргументов сигнатуру не обходит; песочница Грока (`--cap-drop`/`--bind / /`) не матчится; вложенный own внутри нашей клетки ловится по внешнему bwrap. `_coverage_now` для пустых фаз — `None`; схема 1 (`phase` отсутствует → `blind`) на `chat-v1` и раунде `1` в `room.jsonl` даёт нормальную строку. Гонка pgrep→запись — прежний TOCTOU, нового обхода не видно.

СВОЯ ИДЕЯ: для `paper_verdict` и висяков держать у ветки отдельно `stage_hypothesis` = последний non-instrument/non-screening вердикт (или «есть ли hyp-событие»), а текущий `stage` оставить «последний файл как есть» — иначе любой поздний diag снова сделает живую линию «бумажной».

### deepseek

<pre>
ДЕФЕКТЫ:

1. KanBan/index.py:343 — _branch_for допускает пересечение пустых множеств: если name и title не имеют общих токенов (score=0), best всё равно записывается; ветки с score=0 связываются при наличии общего токена в слаге, а не по семантике. Пример: registry.py имя "sweep_reclaim" → ветка "sweep_reclaim_killtest" (set(want) <= set(have) истинно), но score=0 для пустого пересечения при name="sweep_reclaim" и title="p2hero_E1" не выполнится (условие подмножества), однако если name="sweep" (короткое) и title="sweep_reclaim", условие set(want) <= set(have) истинно, score=1. Это не так страшно. Но если name="sweep_reclaim_killtest" (длиннее) и title="p2hero" — условие set(have) <= set(want) может быть ложным, но set(want) <= set(have) тоже ложным, тогда best не обновится. Ошибки с score=0 быть не должно, т.к. score=len(set(have)&set(want)) минимум 0, но оба множества не пустые, поэтому score=0 возможно только если нет общих токенов, но тогда условие подмножества не выполняется. Так что дефекта нет. Снимаю пункт.

2. KanBan/index.py:688 — строки вроде "|" в split_cells: если строка начинается с "|", она отрезается, но если строка заканчивается "|" и не экранирована, она отрезается. Для пустой строки "|" результатом будет [''] — это может ошибочно считаться идеей? В read_ideas проверяется if len(cells)>=2 and cells[0] ... — cells[0] пустая строка пропускается, так что не дефект.

3. KanBan/index.py: read_ideas: обход по self.nodes.values() внутри цикла по sections при обработке ЗАКРЫТА — модификация n["stage"] и n["closed_by"] на узле, который может быть обработан позже в том же цикле? Нет, цикл по sections и отдельный цикл по nodes.

4. KanBan/index.py: family_stem: удаляются токены из FAMILY_STRIP, включая "verdict", "idea", "prereg" и т.д. Но слаги типа "ib0991-primary" → токены ["ib0991","primary"], primary не в стоп-листе, остаётся. А слаги "wrb-nr" → токены ["wrb","nr"], оба остаются. В результате семейства могут склеиваться по общему ключу "wrb" или "nr"? Ключ stem.replace("-","") = "wrbnr". Для другого слага "wrb-something" ключ "wrbsomething" — не совпадает. Так что семейство только для слагов, дающих одинаковый ключ после удаления стоп-токенов и без разделителей. Это может склеить "wrb-nr" и "wrb"+"nr"? Но "wrb" как слага даст ключ "wrb", а "wrb-nr" — "wrbnr", не совпадает. Разрывов не вижу.

Конкретно проверим на данных.

Посмотрим фактические данные и запустим проверочный скрипт. Используем read-only команды.
</pre>

Давайте исследуем. Запустим python скрипт для анализа index.py и данных.

<pre>
import os, re, json, collections
(далее — самоопровержение пунктов и код проверки; дефектов не осталось)

### gemini

ДЕФЕКТОВ НЕТ

Проведена ревизия изменений второго круга в `KanBan/index.py` и `RoundTable/roundtable.py`:

1. Индексатор `KanBan/index.py`:
- Семейства и линии: алгоритм `family_stem` с фильтрацией токенов `FAMILY_STRIP` и сравнением без разделителей корректно склеивает родственные треды (например, `wrb-nr` / `wrbnr`, `vpoc-approach`, `takens`) и оставляет одиночные треды обособленными. Служебные треды (`den`, `vahta`, `itog`) изолированы признаком `service` и исключены из подсчёта 116 линий гипотез.
- Связи `branch_of`: сопоставление по первому токену и порогу ключевых токенов (`need = len(bset) if len(bset) == 2 else len(bset) - 1`) исключило паразитные склейки по одиночным маркерам (`papa`) и точно привязывает треды и семейства к каталогам веток.
- Реестр идей: 58 идей разобраны полностью; регулярные выражения корректно обрабатывают нумерацию, варианты Ф1–Ф3 и строки markdown-таблиц с экранированием `\|` и обратными кавычками. Секции-правила вынесены в блок «Не разобрано». Правило «ЗАКРЫТА» переводит в статус `closed` только активные идеи (`in_progress`, `queued`) целевой ветки по ключевым словам раздела.
- Бумажные вердикты: 18 линий со стадией `verdict` обоснованно определены как `paper_verdict`, так как в соответствующих каталогах `strategy_engine/research/` нет подтверждённых json-вердиктов гипотез.

2. Окно `RoundTable/roundtable.py` и `coverage.py`:
- Сторож `_bwrap_alive`: проверка сигнатуры `JAIL_SIGNATURE` (`--ro-bind / /` и `--unshare-pid`) через предикат `all(sig in line ...)` инвариантна к порядку аргументов. Вложенные bwrap-процессы внутри клетки не маскируют внешний процесс bwrap стола. При `CHOIR_RT_NO_BWRAP=1` сторож штатно отключается с выводом уведомления при запуске.
- Карта покрытия: `_coverage_now` возвращает `None` для раундов без зарегистрированных фаз, исключая отображение пустых шаблонов, а `cover.head` при отсутствии автора свода корректно завершает строку точкой без артефактов `None` или `?`.

СВОЯ ИДЕЯ: Добавить в KanBan генерацию автономного графового представления (HTML/SVG) в каталог KanBan/index/graph.html для наглядной визуализации связей карточек, семейств и висяков прямо из браузера.

### kimi — УПАЛ

rc=1 за 560 с: не уложился в предел вызова, ответа нет (правило 4).

### субагент, круг 2 (6)

1) ОКНО, высокий: сигнатура клетки искалась по всей командной строке — промпт Грока с дифом (цитата `--ro-bind / /`) делал его песочницу «нашей» → 409 по чужому процессу; 2) высокий: «бумажные вердикты 17 из 20» в основном ложны — 12 из 17 непривязанность (reg6-verdict ↔ reg6_absorb_fade и др.), вердикты под ключами gate/verdicts/final_verdict; 3) «ЗАКРЫТА» закрыла 12 вместо 4 названных; 4) идеи: `**4. …** (Кодекс, …). Append-only` и `**9-бис.**` не разобраны молча; 5) ложные висяки: fade, gas-geometry (вердикт в заголовке), papa-wrnr-svod откалывался как service, claude-ideas/next-tests — мета; 6) «Не разобрано» неполно: CONTOUR_DRAFT, labs борда, json не-словари, retriever с gate_results.

## Что починено после кругов 1–2

- Определения переписаны (шапка index.py): круг vs исход маркера; вердикт из заголовка карточки (`verdict_from=title`); семейства по ключу без разделителей и расширенному списку служебных токенов; служебные треды вне линий; ветки и без results (LEDGER/SPEC), `gate_results/`; вердикт под ключами verdict/final_verdict/gate/verdicts; контроли и плацебо — события `control`, не вердикт; mtime с секундами, tie-break по имени, равные mtime помечены; категория screening; `stage_hypothesis` ветки (grok, круг 2) — последний вердикт не о приборе; противоречие только по ключу под-гипотезы; реестр списком; цитаты по полному имени или уникальному префиксу, неоднозначные считаются; идеи: `**N.` с тире и без, `9-бис`, `**Ф1 (…) — …**`, ячейки по `|` вне кавычек, раздел-правило считается и называется, дубли — один узел, «ЗАКРЫТА» — только названные в тексте раздела (4, как в источнике); branch_of — первый токен ветки и все ключевые токены кроме одного при ≥3, семейный токен с цифрой (reg6, reg5, ib0991), одиночный токен — только при равенстве стеблей; «с вердиктом ветки» — по branch_of ИЛИ mentions; instrument — не вердикт для висяков; «Не разобрано» полное.
- Окно: `_is_our_jail` — сигнатура только до « -- » и с началом `bwrap --ro-bind / / --dev /dev --proc /proc`; `jail.disabled()` со строкой при старте; `_coverage_now` → None без фаз.
- Тесты: `KanBan/test_index.py` 29; `test/access_guard_test.py` 11; окно 111; карта 35; доступ 31.
- Живой прогон (23.09): 264 карточки, 195 тредов (11 служебных), 67 семейств, 118 линий, 60 идей (closed 4), 23 ветки, 63 вердикта гипотез + 1 контроль, паспорт ДО 14 / ПОСЛЕ 6, «вердикт словом без ветки» 11 из 29, висяков 58.

## Круг 3 (финальная проверка субагентом)

Первый запуск круга оборвался ошибкой авторизации (403, /login), перезапущен.
Субагент, круг 3: тесты 11 и 29 зелёные, живой прогон совпал. Дефекты: 1) ещё
4 из 11 «вердикт словом без ветки» — непривязанность: карточки называют файл
`results/e16_report.json`, а ребра по имени файла не было; 2) вердикт из
заголовка искался только в последней карточке (g2-screen, w4xver — вердикт во
второй, последняя sign-off; wave3 «обе мертвы» не ловился регэкспом);
3) цифровой fallback свяжет `reg6-*` с любой веткой `reg6_*` — сейчас веток по
одной на номер, защиты нет (известная граница); 4) путь ACCESS.txt с « -- »
внутри обрежет строку сторожа (гипотетично, не чинилось). Закрыто из круга 2:
окно — да (jail.py:500 подтверждает начало префикса, « -- » после него);
ЗАКРЫТА — да (ровно четыре идеи источника); «Не разобрано» — да. Вердикт из
заголовка — ложных нет; рёбра по токену с цифрой — все верные.

Починено после круга 3: `mentions` по имени файла results (длиннее 8 символов) в
тексте карточки; стадия verdict — по заголовку любой карточки треда;
регэксп заголовка ловит CONFIRMED и «мертв…». Тест 31. Живой прогон 23.09:
«вердикт словом без ветки» 10 из 40, висяков 45.

Не закрыто: цифровой fallback при нескольких ветках одного номера; « -- » в
пути ACCESS.txt; codex не участвует в ревизиях, пока в окне стоит модель,
которую его аккаунт отвергает; gemini 503 шестой раз, kimi не укладывается в
десять минут на пакетах больше ~60 КБ.
