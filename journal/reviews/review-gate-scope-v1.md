# Ревизия: скоуп правки каталогом («dir/») и два долга кресел макетов (2026-09-23)

Повод: кресла макетов KanBan открыты дирижёром со скоупом
`KanBan/mockups/<голос>/`; гейт сверяет пути fnmatch'ем, литерал со
слэшем ничему не равен — акт Codex с пятью одобрениями получил «выход из
скоупа» по своим же трём файлам.

Сделано: `merge_gate.scope_ok(path, declared)` — маска fnmatch ИЛИ
заявка со слэшем на конце покрывает всё под каталогом; подсказка поля
скоупа в окне и шапка модуля называют форму `dir/`; тест gate_test +3.

Ревизия: субагент построчно (стол в этот час недоступен: codex — кредиты
до 19:23, kimi — аккаунт приостановлен за баланс, gemini — квота).
Дефектов в коде нет; замечания: неверная посылка в описании («dir/* не
покрывает подкаталоги» — покрывает, `*` в fnmatch матчит слэш),
документация формы `dir/` не обновлена, мёртвый `import fnmatch`,
строка вместо списка в `declared` итерирует символы (пре-существующее,
оба входа пишут список), тест одной булевой цепочкой. Учтено всё, кроме
строки-вместо-списка (вход только рукой в ленту).

Долги, найденные тем же днём:
1. Чистильщик секретов пакета ревизии (`_DIFF_SECRET_RE`, ветка
   `\bkey…`) режет обычное присваивание `key = …` в коде на «‹вырезано›»:
   Джемини отказал акту DeepSeek f6a685f4a0d1 за «синтаксический дефект»,
   которого в ветке нет (`build.py` компилируется). Рецензент судит текст,
   которого нет, — правило 8.5 наоборот. Чинить: не резать слова кода, а
   только формы ключей; и помечать в пакете, ЧТО вырезано и где.
2. Раунд с заданием «нарисуй» снимает голоса сторожем тишины (300 с);
   кресло этой болезни не имеет — задачи с долгой молчаливой работой
   идут креслом, не раундом (зафиксировано в KanBan/mockups/README.md).
